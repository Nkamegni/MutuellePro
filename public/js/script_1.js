    
    const translations = {
        fr: {
            "topbar.clientSpace": "Espace Client",
            "nav.home": "Accueil",
            "nav.solutions": "Nos Solutions",
            "nav.about": "Le Cabinet",
            "nav.agencies": "Agences & Contact",
            "nav.helpdesk": "Assistance",
            "helpdesk.tag": "Assistance",
            "helpdesk.title": "Une question ? Un souci ? On est là.",
            "helpdesk.subtitle": "Notre équipe répond directement à vos questions — pas de robot, pas de file d'attente interminable.",
            "helpdesk.ctaTitle": "Parlez-nous directement",
            "helpdesk.ctaDesc": "Un problème avec le site, votre compte, ou une question sur nos services ? Contactez-nous par le canal qui vous convient.",
            "helpdesk.callBtn": "Appeler maintenant",
            "helpdesk.whatsappBtn": "Discuter sur WhatsApp",
            "helpdesk.tipsTitle": "Avant d'appeler, ça peut aider",
            "helpdesk.tip1": "Ayez votre numéro de police ou de devis sous la main, si vous en avez un — ça accélère tout.",
            "helpdesk.tip2": "Pour un sinistre auto ou une urgence, utilisez plutôt la ligne prioritaire 24h/24 — plus rapide pour ces cas précis.",
            "helpdesk.tip3": "WhatsApp est souvent plus rapide qu'un appel aux heures de pointe — n'hésitez pas à y joindre une photo ou un document.",
            "helpdesk.sinistreBtn": "Déclarer un sinistre",
            "sinistre.title": "Déclarer un sinistre",
            "sinistre.subtitle": "Quelques informations essentielles suffisent pour ouvrir votre dossier — nous vous recontactons rapidement pour la suite.",
            "sinistre.urgenceNote": "Situation d'urgence (accident en cours, danger immédiat) ? Appelez directement plutôt que de remplir ce formulaire.",
            "sinistre.brancheLabel": "Branche concernée",
            "sinistre.policeLabel": "N° de police",
            "sinistre.optionnel": "(si vous le connaissez)",
            "sinistre.dateLabel": "Date du sinistre",
            "sinistre.descLabel": "Que s'est-il passé ?",
            "sinistre.nomLabel": "Nom complet",
            "sinistre.emailLabel": "Adresse email",
            "sinistre.submitBtn": "Envoyer ma déclaration",
            "sinistre.envoiNote": "Votre déclaration sera transmise par WhatsApp et par e-mail, avec un numéro de référence à conserver.",
            "espace.title": "Espace Client",
            "espace.loginTab": "Connexion",
            "espace.registerTab": "Créer un compte",
            "espace.identifiantLabel": "Email ou téléphone",
            "espace.passwordLabel": "Mot de passe",
            "espace.loginBtn": "Se connecter",
            "espace.registerEmailLabel": "Adresse email",
            "espace.registerPhoneLabel": "Téléphone portable",
            "espace.registerBtn": "Créer mon compte",
            "espace.logoutBtn": "Se déconnecter",
            "espace.myTicketsTitle": "Mes demandes",
            "espace.noTickets": "Vous n'avez encore aucune demande enregistrée.",
            "espace.errorGeneric": "Une erreur est survenue. Veuillez réessayer.",
            "espace.errorAuthRequired": "Veuillez vous reconnecter.",
            "espace.loginSuccess": "Connexion réussie.",
            "espace.registerSuccess": "Compte créé avec succès !",
            "espace.logoutSuccess": "Vous êtes déconnecté.",
            "espace.switchToRegister": "Pas encore de compte ? Créer un compte",
            "espace.switchToLogin": "Déjà un compte ? Se connecter",
            "espace.statutRecu": "Reçu",
            "espace.statutEnCours": "En cours",
            "espace.statutResolu": "Résolu",
            "espace.statutClientInscrit": "Client déjà inscrit",
            "espace.statutNouveauContact": "Nouveau contact",
            "espace.statutAVerifier": "Statut du compte non vérifié",
            "espace.registerPasswordLabel": "Mot de passe (10 caractères min., 1 lettre, 1 chiffre, 1 caractère spécial)",
            "espace.confirmPasswordLabel": "Confirmer le mot de passe",
            "espace.passwordMismatch": "Les mots de passe ne correspondent pas",
            "topbar.loggedInAs": "Connecté",
            "nav.getQuote": "Demander un Devis",
            "hero.badge": "Courtier d'Assurance Agréé par le MINFI",
            "hero.slogan": "« Parce que l’imprévu n’a pas sa place dans vos plans »",
            "hero.familyNote": "Votre famille, vos projets, votre entreprise — protégés par des gens qui vous connaissent vraiment.",
            "hero.description": "Depuis plus de 30 ans, nous veillons sur ce qui compte vraiment pour vous.",
            "hero.description2": "Le monde est entré dans la « digitalisation » — nous avons choisi d'y répondre en restant à vos côtés : nos équipes interviennent directement sur le terrain, avec la rapidité et la rigueur qui font toute la différence le jour où vous en avez besoin.",
            "hero.description3": "Ce que vous découvrez aujourd'hui est déjà votre quotidien, et nous continuons, brique après brique, à vous faciliter le parcours — de la souscription au règlement du sinistre. Sans hésiter, allez-y : cet espace est le vôtre.",
            "hero.ctaSolutions": "Découvrir nos Garanties",
            "hero.ctaWhatsapp": "Échanger sur WhatsApp",
            "devis.title": "De quoi souhaitez-vous vous protéger ?",
            "devis.subtitle": "Sélectionnez le risque pour obtenir une cotation adaptée en 2 minutes.",
            "devis.auto": "Automobile",
            "devis.health": "Santé",
            "devis.home": "Habitation",
            "devis.other": "Autres",
            "devis.phoneLabel": "Téléphone portable",
            "devis.emailLabel": "Adresse Email",
            "devis.cityLabel": "Ville de résidence",
            "devis.submitBtn": "Obtenir mon Devis Sur-Mesure",
            "solutions.tag": "Protections Sur-Mesure",
            "solutions.title": "Des solutions adaptées à chaque étape de votre vie et de votre activité",
            "solutions.subtitle": "Que vous soyez un particulier soucieux de sa famille ou un dirigeant protégeant ses actifs, Mutuelle Pro Assurances négocie les meilleures garanties du marché CIMA.",
            "field.auto.vin": "N° de Châssis (VIN)",
            "field.auto.plate": "N° d'Immatriculation",
            "field.auto.make": "Marque",
            "field.auto.model": "Modèle",
            "field.auto.power": "Puissance",
            "field.auto.fuel": "Énergie",
            "field.auto.genre": "Genre du véhicule",
            "field.auto.dualControls": "Double Commande",
            "field.auto.flammable": "Quantité de matière inflammable transportée",
            "field.auto.specifyGenre": "Précisez le genre",
            "field.auto.usage": "Usage précis",
            "field.common.estimatedValue": "Valeur estimée",
            "field.sante.target": "Cible",
            "field.sante.ageRange": "Tranche d'âge",
            "field.habitation.status": "Qualité",
            "field.habitation.contentsValue": "Valeur contenu",
            "field.voyage.destination": "Destination",
            "field.voyage.duration": "Durée",
            "field.rc.type": "Type de RC",
            "field.pro.premisesType": "Nature du local",
            "field.pro.area": "Surface (m²)",
            "field.pro.assetsValue": "Valeur des biens à assurer",
            "field.common.notes": "Notes / Précisions complémentaires",
            "partners.tag": "Un Marché Entier, Un Seul Interlocuteur",
            "partners.title": "Le Marché Camerounais de l'Assurance, à Votre Service",
            "partners.subtitle": "Nous sommes votre meilleur partenaire et votre meilleur interlocuteur auprès de toutes les compagnies du marché. Grâce à nous, vous obtenez toujours le meilleur d'entre elles — sans avoir à démarcher vous-même chaque compagnie.",
            "solutions.tabIndividuals": "Particuliers & Familles",
            "solutions.tabBusinesses": "Entreprises & Professionnels",
            "solutions.inviteToBusiness": "Vous dirigez une entreprise ? Découvrez aussi nos solutions dédiées.",
            "solutions.inviteToBusinessBtn": "Voir les offres Entreprises",
            "solutions.inviteToIndividuals": "Besoin d'une couverture pour vous-même ou votre famille ?",
            "solutions.inviteToIndividualsBtn": "Voir les offres Particuliers",
            "contact.tag": "Proximité & Réactivité",
            "contact.title": "À votre écoute partout au Cameroun",
            "contact.agenciesTitle": "Nos Agences",
            "contact.geoBtn": "Trouver l'agence la plus proche de moi",
            "contact.selectLabel": "Choisir une agence :",
            "contact.directPhone": "Téléphone Direct",
            "contact.formTitle": "Envoyez-nous un message",
            "contact.fullName": "Nom complet *",
            "contact.email": "Adresse Email *",
            "contact.phone": "Téléphone *",
            "contact.city": "Ville *",
            "contact.object": "Objet de votre demande *",
            "contact.subjectCategory": "Sujet *",
            "contact.subjectChoose": "-- Choisissez un sujet --",
            "contact.message": "Votre Message *",
            "contact.sendBtn": "Envoyer la demande",
            "cabinet.badge": "Courtier d'Assurance Agréé",
            "cabinet.cardDesc": "Un cabinet indépendant fondé sur trois décennies d'expertise opérationnelle et technologique au service de la protection des biens et des personnes au Cameroun.",
            "cabinet.headquarters": "Siège Social",
            "cabinet.compliance": "Conformité",
            "cabinet.badgeYears": "30 Ans",
            "cabinet.badgeExp": "d'Expertise Métier",
            "cabinet.tag": "À propos de nous",
            "cabinet.title": "Votre défenseur de confiance face aux aléas de la vie",
            "cabinet.quote": "« Souscrire une assurance ne devrait jamais être une formalité complexe. Notre rôle de courtier est d'être votre avocat conseil : nous analysons les contrats du marché et veillons à ce que vous soyez indemnisé au franc près, sans délai abusif. »",
            "cabinet.quoteAuthor": "La Direction — Mutuelle Pro Assurances",
            "cabinet.pillar1Title": "Négociation",
            "cabinet.pillar1Desc": "Mise en concurrence des compagnies pour le meilleur tarif.",
            "cabinet.pillar2Title": "Gestion<br>Sinistres",
            "cabinet.pillar2Desc": "Prise en charge à 100% de vos démarches d'indemnisation.",
            "cabinet.pillar3Title": "Efficacité<br>Digitale",
            "cabinet.pillar3Desc": "Des outils numériques qui accélèrent chaque étape de votre demande.",
            "solutions.autoTitle": "Assurance Automobile",
            "solutions.autoDesc": "Responsabilité civile obligatoire, tiers étendu ou tous risques. Attestation rapide et assistance panne 24h/7 à Yaoundé.",
            "solutions.autoF1": "Défense & recours inclus",
            "solutions.autoF2": "Bris de glaces & vol",
            "solutions.autoF3": "Remorquage d'urgence",
            "solutions.autoBtn": "Obtenir un Devis Auto",
            "solutions.healthTitle": "Santé & Mutuelle Famille",
            "solutions.healthDesc": "Prise en charge des consultations, pharmacie, hospitalisation et soins dentaires/optiques pour vous et vos ayants droit.",
            "solutions.healthF1": "Tiers payant clinique",
            "solutions.healthF2": "Maternité & pédiatrie",
            "solutions.healthF3": "Remboursements rapides",
            "solutions.healthBtn": "Protéger ma Famille",
            "solutions.homeTitle": "Multirisque Habitation",
            "solutions.homeDesc": "Protection complète de votre domicile (propriétaire ou locataire) contre l'incendie, les dégâts des eaux, le vol et les catastrophes.",
            "solutions.homeF1": "Incendie & explosion",
            "solutions.homeF2": "Vol & vandalisme",
            "solutions.homeF3": "RC Vie Privée",
            "solutions.homeBtn": "Assurer mon Logement",
            "solutions.travelTitle": "Voyage & Évacuation",
            "solutions.travelDesc": "Attestation d'assurance pour visas Schengen/international, frais médicaux à l'étranger et rapatriement sanitaire d'urgence.",
            "solutions.travelF1": "Conforme exigences ambassades",
            "solutions.travelF2": "Rapatriement 24h/24",
            "solutions.travelF3": "Bagages perdus",
            "solutions.travelBtn": "Obtenir l'Attestation",
            "solutions.fleetTitle": "Flottes de Véhicules",
            "solutions.fleetDesc": "Gestion centralisée des parcs automobiles de PME et grandes entreprises. Tarification ajustée et gestion simplifiée.",
            "solutions.fleetF1": "Gestion parc unique",
            "solutions.fleetF2": "Véhicules de remplacement",
            "solutions.fleetF3": "Optimisation des primes",
            "solutions.fleetBtn": "Audit de Flotte",
            "solutions.rcTitle": "Responsabilité Civile (RC)",
            "solutions.rcDesc": "Couverture des dommages corporels, matériels ou immatériels causés à des tiers dans le cadre de vos activités.",
            "solutions.rcF1": "RC Exploitation",
            "solutions.rcF2": "RC Professionnelle / Conseil",
            "solutions.rcF3": "Protection juridique",
            "solutions.rcBtn": "Protéger mon Activité",
            "solutions.groupHealthTitle": "Santé & Prévoyance Groupe",
            "solutions.groupHealthDesc": "Fidélisez vos collaborateurs grâce à une couverture médicale collective compétitive avec réseau de soin étendu au Cameroun.",
            "solutions.groupHealthF1": "Couverture employés & familles",
            "solutions.groupHealthF2": "Tiers payant national",
            "solutions.groupHealthF3": "Prévoyance décès / invalidité",
            "solutions.groupHealthBtn": "Devis Santé Groupe",
            "solutions.proMultiTitle": "Multirisque Professionnelle",
            "solutions.proMultiDesc": "Assurance de vos locaux professionnels, entrepôts, stocks, machines et pertes d'exploitation consécutives à un sinistre.",
            "solutions.proMultiF1": "Incendie & risques annexes",
            "solutions.proMultiF2": "Vol de marchandise & matériel",
            "solutions.proMultiF3": "Pertes d'exploitation",
            "solutions.proMultiBtn": "Assurer mes Locaux",
            "footer.about": "Courtier d'assurance agréé par le Ministère des Finances (MINFI). Nous accompagnons particuliers et entreprises pour sécuriser leurs risques au Cameroun.",
            "footer.colNav": "Navigation",
            "footer.colHQ": "Siège Social",
            "footer.hours": "Lun - Ven : 08h00 - 17h00",
            "footer.rights": "Tous droits réservés.",
            "cookies.text": "Nous utilisons des cookies pour mesurer l'audience de ce site (Google Analytics). Vos données ne sont utilisées qu'à cette fin. Consultez notre",
            "cookies.link": "politique de confidentialité",
            "cookies.refuse": "Refuser",
            "cookies.accept": "Accepter",
            "footer.legal": "Mentions Légales",
            "footer.privacy": "Politique de Confidentialité",
            "footer.cima": "Code CIMA",
            "howitworks.brandTitle": "Assurances Digitales",
            "howitworks.tag": "Comment ça marche",
            "howitworks.title": "Quatre étapes, un dossier prêt",
            "howitworks.step1Title": "Décrivez votre besoin",
            "howitworks.step1Desc": "Choisissez votre risque et remplissez vos informations en quelques minutes.",
            "howitworks.step2Title": "Détection intelligente",
            "howitworks.step2Desc": "Votre véhicule et l'agence la plus proche de vous sont identifiés automatiquement.",
            "howitworks.step3Title": "Dossier complet en un clic",
            "howitworks.step3Desc": "Photos compressées, mémo structuré : tout est prêt à transmettre, sans ressaisie.",
            "howitworks.step4Title": "Réponse humaine, rapidement",
            "howitworks.step4Desc": "Votre demande part directement vers l'agence concernée, prête à être traitée.",
            "howitworks.step5Title": "Attestation dématérialisée",
            "howitworks.step5Desc": "Votre démarche amorce le processus de dématérialisation qui vous permettra d'obtenir votre attestation numérique.",
            "sim.brandTitle": "Agissez maintenant pour votre protection",
            "sim.tag": "Simulateur rapide",
            "sim.title": "Trouvez votre besoin en 10 secondes",
            "sim.subtitle": "Une orientation immédiate — le devis précis se fait ensuite avec un conseiller.",
            "sim.step1Question": "Quel risque souhaitez-vous assurer ?",
            "sim.branchAuto": "Auto",
            "sim.branchHealth": "Santé",
            "sim.branchHome": "Habitation",
            "sim.branchTravel": "Voyage",
            "sim.branchRc": "RC Diverses",
            "sim.branchProMulti": "Multirisque Pro",
            "sim.step2Question": "Vous êtes plutôt...",
            "sim.profileIndividual": "Particulier",
            "sim.profileBusiness": "Professionnel",
            "sim.restart": "Recommencer",
            "sim.ctaBtn": "Obtenir mon devis maintenant",
            "assistant.brandTitle": "L'IA à votre service 24/7 !",
            "assistant.badge": "En construction",
            "assistant.title": "Bientôt : une réponse avant même d'avoir fini de poser la question",
            "assistant.desc": "Nous construisons, en ce moment même, une intelligence dédiée à Mutuelle Pro — formée sur nos garanties, le Code CIMA, et votre dossier. Comprendre une clause, savoir quoi faire après un sinistre, poser la question qui vous tracasse à 23h : la réponse arrivera en quelques secondes, jour et nuit.",
            "assistant.commitment": "Ce n'est pas une simple promesse : c'est un engagement. Nous ne le lancerons que lorsqu'il sera digne de votre confiance.",
            "assistant.cta": "En attendant, nous vous répondons !"
        },
        en: {
            "topbar.clientSpace": "Client Area",
            "nav.home": "Home",
            "nav.solutions": "Our Solutions",
            "nav.about": "About Us",
            "nav.agencies": "Branches & Contact",
            "nav.helpdesk": "Support",
            "helpdesk.tag": "Support",
            "helpdesk.title": "A question? A problem? We're here.",
            "helpdesk.subtitle": "Our team answers your questions directly — no bots, no endless queue.",
            "helpdesk.ctaTitle": "Talk to us directly",
            "helpdesk.ctaDesc": "An issue with the site, your account, or a question about our services? Reach us through whichever channel suits you.",
            "helpdesk.callBtn": "Call now",
            "helpdesk.whatsappBtn": "Chat on WhatsApp",
            "helpdesk.tipsTitle": "Before you call, this can help",
            "helpdesk.tip1": "Have your policy or quote number handy, if you have one — it speeds everything up.",
            "helpdesk.tip2": "For a car claim or an emergency, use our 24/7 priority line instead — faster for these specific cases.",
            "helpdesk.tip3": "WhatsApp is often faster than a call during peak hours — feel free to attach a photo or document.",
            "helpdesk.sinistreBtn": "Report a Claim",
            "sinistre.title": "Report a Claim",
            "sinistre.subtitle": "A few essential details are enough to open your case — we'll get back to you quickly.",
            "sinistre.urgenceNote": "Emergency situation (accident in progress, immediate danger)? Call directly instead of filling out this form.",
            "sinistre.brancheLabel": "Branch concerned",
            "sinistre.policeLabel": "Policy No.",
            "sinistre.optionnel": "(if known)",
            "sinistre.dateLabel": "Date of incident",
            "sinistre.descLabel": "What happened?",
            "sinistre.nomLabel": "Full name",
            "sinistre.emailLabel": "Email address",
            "sinistre.submitBtn": "Send my declaration",
            "sinistre.envoiNote": "Your declaration will be sent via WhatsApp and email, with a reference number to keep.",
            "espace.title": "Client Area",
            "espace.loginTab": "Log in",
            "espace.registerTab": "Create an account",
            "espace.identifiantLabel": "Email or phone number",
            "espace.passwordLabel": "Password",
            "espace.loginBtn": "Log in",
            "espace.registerEmailLabel": "Email address",
            "espace.registerPhoneLabel": "Mobile phone",
            "espace.registerPasswordLabel": "Password (min. 10 characters, 1 letter, 1 digit, 1 special character)",
            "espace.registerBtn": "Create my account",
            "espace.logoutBtn": "Log out",
            "espace.myTicketsTitle": "My requests",
            "espace.noTickets": "You don't have any requests yet.",
            "espace.errorGeneric": "Something went wrong. Please try again.",
            "espace.errorAuthRequired": "Please log in again.",
            "espace.loginSuccess": "Logged in successfully.",
            "espace.registerSuccess": "Account created successfully!",
            "espace.logoutSuccess": "You are now logged out.",
            "espace.switchToRegister": "No account yet? Create one",
            "espace.switchToLogin": "Already have an account? Log in",
            "espace.statutRecu": "Received",
            "espace.statutEnCours": "In progress",
            "espace.statutResolu": "Resolved",
            "espace.statutClientInscrit": "Existing registered client",
            "espace.statutNouveauContact": "New contact",
            "espace.statutAVerifier": "Account status unverified",
            "espace.confirmPasswordLabel": "Confirm password",
            "espace.passwordMismatch": "Passwords do not match",
            "topbar.loggedInAs": "Logged in",
            "nav.getQuote": "Get a Quote",
            "hero.badge": "MINFI Licensed Insurance Broker",
            "hero.slogan": "“Because unexpected events have no place in your plans”",
            "hero.familyNote": "Your family, your plans, your business — protected by people who truly know you.",
            "hero.description": "For over 30 years, we have watched over what truly matters to you.",
            "hero.description2": "The world has entered the age of digitalization — we chose to respond by staying by your side: our teams work directly in the field, with the speed and rigor that make all the difference the day you need us.",
            "hero.description3": "What you discover today is already part of your everyday life, and we keep building, brick by brick, to make your journey easier — from taking out a policy to settling a claim. Don't hesitate, come on in: this space is yours.",
            "hero.ctaSolutions": "Discover Our Coverage",
            "hero.ctaWhatsapp": "Chat on WhatsApp",
            "devis.title": "What do you want to protect?",
            "devis.subtitle": "Select a risk to receive a tailored quotation within 2 minutes.",
            "devis.auto": "Automotive",
            "devis.health": "Health",
            "devis.home": "Home",
            "devis.other": "Others",
            "devis.phoneLabel": "Mobile phone",
            "devis.emailLabel": "Email Address",
            "devis.cityLabel": "City of residence",
            "devis.submitBtn": "Get My Custom Quote",
            "solutions.tag": "Tailored Protection",
            "solutions.title": "Solutions designed for every stage of your life and business",
            "solutions.subtitle": "Whether you are an individual caring for your family or a business leader protecting assets, Mutuelle Pro negotiates top CIMA market policies.",
            "field.auto.vin": "Chassis No. (VIN)",
            "field.auto.plate": "Registration No.",
            "field.auto.make": "Make",
            "field.auto.model": "Model",
            "field.auto.power": "Power",
            "field.auto.fuel": "Fuel Type",
            "field.auto.genre": "Vehicle Type",
            "field.auto.dualControls": "Dual Controls",
            "field.auto.flammable": "Quantity of Flammable Material Carried",
            "field.auto.specifyGenre": "Specify the Type",
            "field.auto.usage": "Specific Use",
            "field.common.estimatedValue": "Estimated Value",
            "field.sante.target": "Target",
            "field.sante.ageRange": "Age Range",
            "field.habitation.status": "Status",
            "field.habitation.contentsValue": "Contents Value",
            "field.voyage.destination": "Destination",
            "field.voyage.duration": "Duration",
            "field.rc.type": "Liability Type",
            "field.pro.premisesType": "Premises Type",
            "field.pro.area": "Floor Area (m²)",
            "field.pro.assetsValue": "Value of Assets to Insure",
            "field.common.notes": "Notes / Additional Details",
            "partners.tag": "One Market, One Point of Contact",
            "partners.title": "The Whole Cameroonian Insurance Market, At Your Service",
            "partners.subtitle": "We are your best partner and your best point of contact with every insurance company on the market. Through us, you always get the best they have to offer — without having to approach each company yourself.",
            "solutions.tabIndividuals": "Individuals & Families",
            "solutions.tabBusinesses": "Businesses & Professionals",
            "solutions.inviteToBusiness": "Running a business? Discover our dedicated solutions too.",
            "solutions.inviteToBusinessBtn": "See Business Solutions",
            "solutions.inviteToIndividuals": "Need coverage for yourself or your family?",
            "solutions.inviteToIndividualsBtn": "See Individual Solutions",
            "contact.tag": "Proximity & Responsiveness",
            "contact.title": "At your service across Cameroon",
            "contact.agenciesTitle": "Our Branches",
            "contact.geoBtn": "Find closest branch to me",
            "contact.selectLabel": "Select a branch:",
            "contact.directPhone": "Direct Phone",
            "contact.formTitle": "Send us a message",
            "contact.fullName": "Full Name *",
            "contact.email": "Email Address *",
            "contact.phone": "Phone *",
            "contact.city": "City *",
            "contact.object": "Subject line *",
            "contact.subjectCategory": "Topic *",
            "contact.subjectChoose": "-- Choose a topic --",
            "contact.message": "Your Message *",
            "contact.sendBtn": "Send Request",
            "cabinet.badge": "Licensed Insurance Broker",
            "cabinet.cardDesc": "An independent firm built on three decades of operational and technological expertise dedicated to protecting people and assets in Cameroon.",
            "cabinet.headquarters": "Head Office",
            "cabinet.compliance": "Compliance",
            "cabinet.badgeYears": "30 Years",
            "cabinet.badgeExp": "of Industry Expertise",
            "cabinet.tag": "About Us",
            "cabinet.title": "Your trusted advocate against life's uncertainties",
            "cabinet.quote": "\"Taking out insurance should never be a complicated formality. Our role as a broker is to be your advocate: we review the market's contracts and make sure you're compensated to the last franc, without undue delay.\"",
            "cabinet.quoteAuthor": "Management — Mutuelle Pro Assurances",
            "cabinet.pillar1Title": "Negotiation",
            "cabinet.pillar1Desc": "We put insurers in competition to secure the best rate for you.",
            "cabinet.pillar2Title": "Claims<br>Handling",
            "cabinet.pillar2Desc": "Full support with your compensation process from start to finish.",
            "cabinet.pillar3Title": "Digital<br>Efficiency",
            "cabinet.pillar3Desc": "Digital tools that speed up every step of your request.",
            "solutions.autoTitle": "Auto Insurance",
            "solutions.autoDesc": "Mandatory third-party liability, extended third-party, or comprehensive cover. Fast certificate and 24/7 breakdown assistance in Yaoundé.",
            "solutions.autoF1": "Legal defense & recourse included",
            "solutions.autoF2": "Glass breakage & theft",
            "solutions.autoF3": "Emergency towing",
            "solutions.autoBtn": "Get an Auto Quote",
            "solutions.healthTitle": "Health & Family Cover",
            "solutions.healthDesc": "Coverage for consultations, pharmacy, hospitalization, and dental/optical care for you and your dependents.",
            "solutions.healthF1": "Direct billing at partner clinics",
            "solutions.healthF2": "Maternity & pediatric care",
            "solutions.healthF3": "Fast reimbursements",
            "solutions.healthBtn": "Protect My Family",
            "solutions.homeTitle": "Home Multi-Risk Insurance",
            "solutions.homeDesc": "Full protection for your home (owner or tenant) against fire, water damage, theft, and natural disasters.",
            "solutions.homeF1": "Fire & explosion",
            "solutions.homeF2": "Theft & vandalism",
            "solutions.homeF3": "Personal liability",
            "solutions.homeBtn": "Insure My Home",
            "solutions.travelTitle": "Travel & Evacuation",
            "solutions.travelDesc": "Insurance certificate for Schengen/international visas, medical expenses abroad, and emergency medical repatriation.",
            "solutions.travelF1": "Meets embassy requirements",
            "solutions.travelF2": "24/7 repatriation",
            "solutions.travelF3": "Lost luggage",
            "solutions.travelBtn": "Get My Certificate",
            "solutions.fleetTitle": "Vehicle Fleets",
            "solutions.fleetDesc": "Centralized fleet management for SMEs and large companies. Tailored pricing and simplified administration.",
            "solutions.fleetF1": "Single fleet management",
            "solutions.fleetF2": "Replacement vehicles",
            "solutions.fleetF3": "Premium optimization",
            "solutions.fleetBtn": "Request a Fleet Audit",
            "solutions.rcTitle": "Civil Liability",
            "solutions.rcDesc": "Coverage for bodily, material, or intangible damage caused to third parties in the course of your business activities.",
            "solutions.rcF1": "Operating liability",
            "solutions.rcF2": "Professional / Consulting liability",
            "solutions.rcF3": "Legal protection",
            "solutions.rcBtn": "Protect My Business",
            "solutions.groupHealthTitle": "Group Health & Welfare",
            "solutions.groupHealthDesc": "Retain your employees with competitive collective medical coverage and an extensive care network across Cameroon.",
            "solutions.groupHealthF1": "Employee & family coverage",
            "solutions.groupHealthF2": "Nationwide direct billing",
            "solutions.groupHealthF3": "Death / disability provisions",
            "solutions.groupHealthBtn": "Get a Group Health Quote",
            "solutions.proMultiTitle": "Business Multi-Risk Insurance",
            "solutions.proMultiDesc": "Insurance for your business premises, warehouses, stock, machinery, and business interruption losses following a claim.",
            "solutions.proMultiF1": "Fire & related risks",
            "solutions.proMultiF2": "Theft of goods & equipment",
            "solutions.proMultiF3": "Business interruption",
            "solutions.proMultiBtn": "Insure My Premises",
            "footer.about": "Insurance broker licensed by the Ministry of Finance (MINFI). We help individuals and businesses secure their risks in Cameroon.",
            "footer.colNav": "Navigation",
            "footer.colHQ": "Head Office",
            "footer.hours": "Mon - Fri: 8:00 AM - 5:00 PM",
            "footer.rights": "All rights reserved.",
            "cookies.text": "We use cookies to measure this site's audience (Google Analytics). Your data is only used for this purpose. See our",
            "cookies.link": "privacy policy",
            "cookies.refuse": "Decline",
            "cookies.accept": "Accept",
            "footer.legal": "Legal Notice",
            "footer.privacy": "Privacy Policy",
            "footer.cima": "CIMA Code",
            "howitworks.brandTitle": "Digital Insurance",
            "howitworks.tag": "How It Works",
            "howitworks.title": "Four steps, one ready-to-go file",
            "howitworks.step1Title": "Describe your need",
            "howitworks.step1Desc": "Choose your risk and fill in your details in just a few minutes.",
            "howitworks.step2Title": "Smart detection",
            "howitworks.step2Desc": "Your vehicle and the branch closest to you are identified automatically.",
            "howitworks.step3Title": "Complete file in one click",
            "howitworks.step3Desc": "Compressed photos, structured memo: everything is ready to send, nothing to retype.",
            "howitworks.step4Title": "A human response, fast",
            "howitworks.step4Desc": "Your request goes straight to the relevant branch, ready to be handled.",
            "howitworks.step5Title": "Digital Certificate",
            "howitworks.step5Desc": "Your request kicks off the digitalization process that will lead to your digital insurance certificate.",
            "sim.brandTitle": "Act Now to Protect Yourself",
            "sim.tag": "Quick Simulator",
            "sim.title": "Find what you need in 10 seconds",
            "sim.subtitle": "An instant pointer — the precise quote comes next with an advisor.",
            "sim.step1Question": "Which risk would you like to insure?",
            "sim.branchAuto": "Auto",
            "sim.branchHealth": "Health",
            "sim.branchHome": "Home",
            "sim.branchTravel": "Travel",
            "sim.branchRc": "Civil Liability",
            "sim.branchProMulti": "Business Multi-Risk",
            "sim.step2Question": "You are...",
            "sim.profileIndividual": "An individual",
            "sim.profileBusiness": "A business",
            "sim.restart": "Start Over",
            "sim.ctaBtn": "Get My Quote Now",
            "assistant.brandTitle": "AI, at Your Service 24/7!",
            "assistant.badge": "Under construction",
            "assistant.title": "Coming soon: an answer before you even finish asking",
            "assistant.desc": "Right now, we're building an intelligence dedicated to Mutuelle Pro — trained on our coverage, the CIMA Code, and your file. Understanding a clause, knowing what to do after a claim, asking the question keeping you up at night: the answer will arrive in seconds, day or night.",
            "assistant.commitment": "This isn't just a promise: it's a commitment. We won't launch it until it's worthy of your trust.",
            "assistant.cta": "In the meantime, we answer you right away!"
        }
    };

    // -----------------------------------------------------------------
    // CONTENU "EN SAVOIR PLUS" — méthode 5E (Exemple, Explication, Enjeu,
    // Éléments clés, Étape suivante), un objet par produit.
    //
    // IMPORTANT — sur les prix (formules) : ce sont des valeurs INDICATIVES
    // de départ, à ajuster par Roger. Structurées ici en JS en attendant
    // la construction du tableau de bord d'administration (chantier
    // "Back-office" déjà identifié) qui permettra de les piloter depuis
    // une vraie table PostgreSQL, comme les autres listes du site — sans
    // avoir besoin de retoucher ce fichier à chaque changement de tarif.
    // -----------------------------------------------------------------
    const PRODUITS_5E = {
        auto: {
            titre: "Assurance Automobile",
            icone: "fa-car",
            image: "images_produits/auto_carte.jpg",
            imageModal: "images_produits/auto_modal.jpg",
            branche: "Auto",
            exemple: "Ahmadou roulait sur la route de Douala quand un véhicule lui a coupé la priorité. Sa voiture était hors d'usage, les réparations dépassaient 800 000 FCFA. Grâce à sa garantie Tous Risques, il n'a rien payé — et a eu un véhicule de courtoisie le temps des réparations.",
            explication: "L'assurance automobile prend en charge les conséquences financières d'un accident, d'un vol ou d'un incendie touchant votre véhicule — ou celui d'un tiers si vous en êtes responsable. Sans elle, un accrochage banal peut représenter plusieurs mois de salaire à rembourser de votre poche.",
            enjeuIntro: "Rouler sans assurance est illégal au Cameroun — mais au-delà de l'obligation, c'est votre protection financière qui est en jeu à chaque trajet.",
            couverts: ["Dommages causés à un tiers (RC, obligatoire)", "Vol et incendie du véhicule (Tiers étendu)", "Dommages à votre propre véhicule, même en tort (Tous Risques)", "Assistance dépannage 24h/24"],
            nonCouverts: ["Usure normale du véhicule", "Conduite sans permis valide", "Usage non déclaré (ex: taxi non déclaré comme tel)"],
            questions: [
                { q: "Que se passe-t-il si je change de véhicule en cours d'année ?", r: "Le contrat se transfère sur le nouveau véhicule après simple déclaration, sans repartir de zéro." },
                { q: "Le conducteur secondaire est-il couvert ?", r: "Oui, s'il est déclaré au contrat — une simple mention suffit, gratuite dans la plupart des cas." },
                { q: "L'assistance couvre-t-elle une panne loin de Yaoundé ?", r: "Oui, le remorquage jusqu'au garage le plus proche est pris en charge, partout au Cameroun." }
            ],
            formules: [
                { nom: "RC Seule", prix: "dès 35 000 FCFA/an", desc: "Minimum légal — dommages aux tiers" },
                { nom: "Tiers Étendu", prix: "dès 65 000 FCFA/an", desc: "RC + vol, incendie, bris de glace" },
                { nom: "Tous Risques", prix: "dès 120 000 FCFA/an", desc: "Protection complète, y compris vos torts" }
            ],
            etape: "Apportez votre CNI et la carte grise du véhicule — nous établissons votre devis en moins de 5 minutes, et votre attestation est délivrée sous 24h.",
        },
        sante: {
            titre: "Santé & Mutuelle Famille",
            icone: "fa-heart-pulse",
            image: "images_produits/sante_carte.jpg",
            imageModal: "images_produits/sante_modal.jpg",
            branche: "Sante",
            exemple: "La fille de Marie a dû être hospitalisée trois jours pour une crise de paludisme sévère. Grâce au tiers payant de sa mutuelle, la famille n'a rien avancé à la clinique — la facture est partie directement à l'assureur.",
            explication: "La mutuelle santé prend en charge tout ou partie de vos frais médicaux — consultations, pharmacie, hospitalisation — pour vous éviter d'avoir à choisir entre vous soigner et payer vos autres charges.",
            enjeuIntro: "Une hospitalisation imprévue peut représenter plusieurs centaines de milliers de FCFA — un choc financier que la mutuelle absorbe à votre place.",
            couverts: ["Consultations et pharmacie", "Hospitalisation et chirurgie", "Maternité et pédiatrie", "Soins dentaires et optiques (selon formule)"],
            nonCouverts: ["Maladies préexistantes non déclarées à la souscription", "Soins esthétiques non médicalement justifiés", "Chirurgie hors réseau sans accord préalable (selon formule)"],
            questions: [
                { q: "Y a-t-il un délai de carence avant d'être couvert ?", r: "Oui, généralement 30 jours pour les soins courants — 0 jour en cas d'accident." },
                { q: "Quel est le réseau d'hôpitaux partenaires ?", r: "Un réseau national de cliniques agréées pratiquant le tiers payant, dont la liste vous est remise à la souscription." },
                { q: "Puis-je résilier si je ne suis pas satisfait ?", r: "Oui, à l'échéance annuelle du contrat, avec un préavis à respecter — nous vous accompagnons dans cette démarche." }
            ],
            formules: [
                { nom: "Essentielle", prix: "dès 8 000 FCFA/mois", desc: "Consultations, pharmacie, urgences" },
                { nom: "Confort", prix: "dès 15 000 FCFA/mois", desc: "+ hospitalisation, maternité" },
                { nom: "Famille+", prix: "dès 28 000 FCFA/mois", desc: "+ dentaire, optique, jusqu'à 5 ayants droit" }
            ],
            etape: "Un conseiller évalue avec vous la formule adaptée à votre foyer — apportez les CNI des personnes à couvrir, le devis est immédiat.",
        },
        habitation: {
            titre: "Multirisque Habitation",
            icone: "fa-house",
            image: "images_produits/habitation_carte.jpg",
            imageModal: "images_produits/habitation_modal.jpg",
            branche: "Habitation",
            exemple: "Paul loue deux locaux attenants dans son immeuble de Yaoundé : un studio à un étudiant, une boutique à un commerçant. Une nuit, un tuyau mal fixé chez l'étudiant a lâché — l'eau s'est infiltrée à travers le mur mitoyen et a détruit une bonne partie du stock du boutiquier au petit matin. Les deux locataires se rejetaient la faute, et Paul se retrouvait pris entre les deux. Grâce à la Responsabilité Civile de son contrat Multirisque Habitation, son assureur a indemnisé le boutiquier directement — Paul n'a perdu ni argent, ni la relation avec ses deux locataires.",
            explication: "L'assurance habitation protège votre logement et son contenu contre les grands risques du quotidien : incendie, dégât des eaux, vol. Elle couvre aussi ce que vous pourriez, sans le vouloir, causer à un voisin.",
            enjeuIntro: "Un sinistre domestique arrive sans prévenir — la question n'est jamais 'si', mais 'quand', et l'assurance transforme un drame financier en simple formalité.",
            couverts: ["Incendie et explosion", "Dégât des eaux", "Vol avec effraction", "RC Vie Privée (dommages causés à un tiers)"],
            nonCouverts: ["Usure normale, moisissures liées à un défaut d'entretien", "Objets de valeur non déclarés spécifiquement", "Logement inoccupé plus de 90 jours sans déclaration"],
            questions: [
                { q: "Suis-je couvert si je suis locataire ?", r: "Oui, une formule locataire existe, généralement moins chère que celle du propriétaire." },
                { q: "Le contenu est-il couvert, ou juste les murs ?", r: "Les deux — le contenu peut être assuré à hauteur d'un capital que vous choisissez à la souscription." },
                { q: "Un cambriolage sans trace d'effraction visible, ça compte ?", r: "Une déclaration à la police reste nécessaire — le contrat précise les conditions exactes de prise en charge." }
            ],
            formules: [
                { nom: "Locataire", prix: "dès 25 000 FCFA/an", desc: "RC vie privée + contenu essentiel" },
                { nom: "Propriétaire", prix: "dès 45 000 FCFA/an", desc: "Murs + contenu + RC" },
                { nom: "Tous Risques Habitation", prix: "dès 80 000 FCFA/an", desc: "Protection maximale, capital élevé" }
            ],
            etape: "Une simple estimation de la valeur de votre logement et de son contenu suffit pour démarrer — devis en 5 minutes.",
        },
        voyage: {
            titre: "Voyage & Évacuation",
            icone: "fa-plane-departure",
            image: "images_produits/voyage_carte.jpg",
            imageModal: "images_produits/voyage_modal.jpg",
            branche: "Sante",
            exemple: "En déplacement professionnel à Paris, Sophie a dû être hospitalisée en urgence pour une appendicite. Sans son assurance voyage, la facture aurait dépassé 2 millions de FCFA — elle n'a rien payé, tout est passé par l'assureur.",
            explication: "L'assurance voyage couvre vos frais médicaux à l'étranger et organise, si nécessaire, votre retour au Cameroun en cas d'urgence — un système de santé étranger peut coûter très cher pour un non-résident sans couverture.",
            enjeuIntro: "Pour un visa, c'est souvent une pièce obligatoire — mais au-delà de la formalité, c'est une vraie protection si l'imprévu survient loin de chez vous.",
            couverts: ["Frais médicaux d'urgence à l'étranger", "Rapatriement sanitaire", "Bagages perdus ou retardés", "Attestation conforme aux exigences consulaires"],
            nonCouverts: ["Affection déjà connue avant le départ", "Sports extrêmes non déclarés", "Séjour dépassant la durée assurée"],
            questions: [
                { q: "L'attestation est-elle acceptée par toutes les ambassades ?", r: "Elle respecte les exigences standard Schengen et la plupart des autres destinations — à confirmer selon le pays visé." },
                { q: "Combien de temps avant le départ dois-je souscrire ?", r: "L'attestation peut être délivrée en 24h, mais mieux vaut prévoir quelques jours avant le dépôt du dossier de visa." },
                { q: "Le rapatriement est-il pris en charge à 100% ?", r: "Oui, dans les conditions du contrat — organisation et coût du retour sanitaire sont couverts." }
            ],
            formules: [
                { nom: "Schengen Standard", prix: "dès 15 000 FCFA", desc: "Couverture visa, durée courte" },
                { nom: "Voyage Confort", prix: "dès 28 000 FCFA", desc: "+ bagages, plafond médical élevé" },
                { nom: "Longue Durée", prix: "dès 45 000 FCFA", desc: "Séjours prolongés, études à l'étranger" }
            ],
            etape: "Indiquez simplement votre destination et vos dates de voyage — votre attestation est prête sous 24h.",
        },
        flottes: {
            titre: "Flottes de Véhicules",
            icone: "fa-truck-front",
            image: "images_produits/flottes_carte.jpg",
            imageModal: "images_produits/flottes_modal.jpg",
            branche: "Auto",
            exemple: "Un des dix camions d'une PME de transport à Douala est tombé en panne moteur en pleine période de forte demande de fin d'année — le pire moment pour annuler des livraisons déjà engagées. Parce que l'entreprise avait choisi la formule Tous Risques de son contrat flotte (pas la simple RC au tiers, qui ne l'aurait pas couverte), la garantie Véhicule de Remplacement a joué : un camion de courtoisie livré sous 48h, le temps de la réparation — sans perdre un seul client, ni un seul jour de chiffre d'affaires.",
            explication: "Une assurance flotte couvre l'ensemble de vos véhicules professionnels sous un seul contrat, plutôt qu'une police séparée par véhicule — plus simple à gérer, et souvent plus avantageux financièrement.",
            enjeuIntro: "Un parc automobile immobilisé, c'est une activité à l'arrêt — la bonne couverture transforme un incident isolé en simple contretemps géré.",
            couverts: ["Tous les véhicules du parc sous un contrat unique", "Véhicules de remplacement en cas d'immobilisation", "Tarification ajustée à la sinistralité réelle", "Gestion administrative centralisée"],
            nonCouverts: ["Véhicules non déclarés au contrat", "Usage non conforme à la déclaration initiale", "Défaut d'entretien avéré"],
            questions: [
                { q: "Puis-je ajouter ou retirer un véhicule en cours d'année ?", r: "Oui, un simple avenant au contrat suffit, sans attendre l'échéance annuelle." },
                { q: "La prime baisse-t-elle si notre sinistralité est faible ?", r: "Oui, c'est l'un des principaux avantages du contrat flotte — la tarification s'ajuste à votre historique réel." },
                { q: "Un seul interlocuteur gère-t-il tout le parc ?", r: "Oui, un conseiller dédié suit l'ensemble de votre flotte, du premier devis à la gestion des sinistres." }
            ],
            formules: [
                { nom: "Flotte Légère", prix: "dès 300 000 FCFA/an", desc: "Jusqu'à 10 motos/triporteurs" },
                { nom: "PME (5-15 véhicules)", prix: "sur devis personnalisé", desc: "Tarification groupée" },
                { nom: "Grande flotte (15+ véhicules)", prix: "sur devis personnalisé", desc: "Conditions négociées" }
            ],
            etape: "Un audit gratuit de votre parc actuel nous permet de vous proposer une tarification précise — contactez-nous avec la liste de vos véhicules.",
        },
        rc: {
            titre: "Responsabilité Civile Professionnelle",
            icone: "fa-briefcase",
            image: "images_produits/rc_carte.jpg",
            imageModal: "images_produits/rc_modal.jpg",
            branche: "Habitation",
            exemple: "Un client a glissé dans les locaux d'une PME de conseil à Yaoundé et menaçait une action en justice pour plusieurs millions de FCFA. Dès la déclaration du sinistre, Mutuelle Pro Assurances a mobilisé son réseau de cabinets d'avocats partenaires pour prendre en charge la défense de l'entreprise, sans attendre que la procédure ne s'enlise. Résultat : un accord trouvé à l'amiable en quelques semaines, entièrement pris en charge par la RC Professionnelle — l'entreprise n'a jamais eu à gérer seule ni le stress, ni les frais d'un contentieux.",
            explication: "La Responsabilité Civile Professionnelle couvre les conséquences financières d'un dommage que votre activité causerait à un tiers — client, fournisseur, visiteur — par une erreur, un accident, ou un défaut de conseil.",
            enjeuIntro: "Une seule réclamation non couverte peut représenter plusieurs mois, voire années, de résultat pour une PME — la RC Pro est souvent la garantie la plus sous-estimée, jusqu'au jour où elle devient nécessaire.",
            couverts: ["Dommages corporels causés à un tiers dans le cadre de l'activité", "Dommages matériels ou immatériels", "Erreurs de conseil ou de prestation", "Défense par nos cabinets d'avocats partenaires, dès la déclaration"],
            nonCouverts: ["Fautes intentionnelles", "Dommages à vos propres biens (voir Multirisque Pro)", "Activités non déclarées à la souscription"],
            questions: [
                { q: "La RC couvre-t-elle mes employés en cas de faute ?", r: "Oui, dans le cadre de leurs fonctions et sous votre responsabilité d'employeur." },
                { q: "Suis-je couvert si le litige va jusqu'au tribunal ?", r: "Oui, la protection juridique incluse prend en charge les frais de défense." },
                { q: "Le montant de couverture est-il suffisant pour mon secteur ?", r: "Le plafond se choisit selon votre activité et votre exposition au risque — nous vous conseillons le bon niveau." }
            ],
            formules: [
                { nom: "RC Exploitation", prix: "dès 150 000 FCFA/an", desc: "Activité courante" },
                { nom: "RC Professionnelle/Conseil", prix: "dès 250 000 FCFA/an", desc: "Métiers de conseil et service" },
                { nom: "RC Étendue", prix: "sur devis", desc: "Secteurs à risque élevé" }
            ],
            etape: "Décrivez-nous votre activité — nous identifions ensemble les risques réels et le bon niveau de couverture.",
        },
        santeGroupe: {
            titre: "Santé & Prévoyance Groupe",
            icone: "fa-users-gear",
            image: "images_produits/santeGroupe_carte.jpg",
            imageModal: "images_produits/santeGroupe_modal.jpg",
            branche: "Sante",
            exemple: "M. Kamga, chef d'une entreprise à Douala, voyait d'un mauvais œil la mise en place d'une mutuelle de santé groupe — un coût de plus, pensait-il, pour un bénéfice difficile à chiffrer. Depuis que son entreprise a souscrit chez Mutuelle Pro Assurances, deux choses l'ont fait changer d'avis : son meilleur technicien, courtisé par un concurrent, a choisi de rester — « la mutuelle de mes enfants, je ne la lâche pas », lui a-t-il confié. Et quand la fille de sa comptable a été hospitalisée en urgence, la prise en charge s'est faite sans qu'elle n'avance un seul FCFA. Aujourd'hui, c'est M. Kamga lui-même qui présente la mutuelle aux nouvelles recrues, en argument de poids.",
            explication: "La Santé & Prévoyance Groupe couvre l'ensemble de vos collaborateurs (et souvent leurs familles) sous un contrat collectif — un vrai avantage social, et souvent plus abordable qu'une couverture individuelle.",
            enjeuIntro: "Dans un marché de l'emploi compétitif, une bonne couverture santé est devenue un critère de choix — et de fidélisation — aussi important que le salaire pour beaucoup de candidats.",
            couverts: ["Consultations, pharmacie, hospitalisation des employés", "Extension possible aux familles", "Tiers payant national", "Garantie décès/invalidité (volet Prévoyance)"],
            nonCouverts: ["Employés non déclarés à l'effectif assuré", "Pathologies exclues au contrat collectif", "Anciens employés (sauf portabilité prévue)"],
            questions: [
                { q: "Tous les employés doivent-ils être inclus ?", r: "En général, l'ensemble de l'effectif éligible est inclus — c'est ce qui permet une tarification groupe avantageuse." },
                { q: "Que se passe-t-il en cas de départ d'un employé ?", r: "Sa couverture s'arrête à la date de sortie, sauf disposition de portabilité prévue au contrat." },
                { q: "Les familles peuvent-elles être ajoutées ?", r: "Oui, en option, généralement avec une participation employé/employeur à définir ensemble." }
            ],
            formules: [
                { nom: "Groupe Essentiel", prix: "dès 6 000 FCFA/employé/mois", desc: "Soins courants" },
                { nom: "Groupe Confort", prix: "dès 12 000 FCFA/employé/mois", desc: "+ hospitalisation, familles en option" },
                { nom: "Groupe Premium", prix: "dès 20 000 FCFA/employé/mois", desc: "+ dentaire, optique, prévoyance renforcée" }
            ],
            etape: "Communiquez-nous la taille de votre effectif — nous construisons une proposition sur mesure sous 48h.",
        },
        multirisquePro: {
            titre: "Multirisque Professionnelle",
            icone: "fa-warehouse",
            image: "images_produits/multirisquePro_carte.jpg",
            imageModal: "images_produits/multirisquePro_modal.jpg",
            branche: "Autres",
            branchePersonnalisee: "Multirisque Pro",
            brancheI18nKey: "MultirisquePro",
            exemple: "Un incendie dans l'entrepôt d'un grossiste a détruit une partie importante du stock. Au-delà des marchandises, l'assurance a compensé la perte de chiffre d'affaires pendant les trois mois de remise en état — sans quoi l'entreprise aurait pu ne jamais rouvrir.",
            explication: "La Multirisque Professionnelle protège vos locaux, votre stock et votre matériel contre les grands sinistres (incendie, vol), et va plus loin avec la garantie Pertes d'Exploitation, qui compense la perte de revenus pendant l'interruption d'activité.",
            enjeuIntro: "Beaucoup d'entreprises assurent leurs biens matériels, mais oublient que c'est souvent l'arrêt d'activité qui coûte le plus cher — pas seulement les dégâts eux-mêmes.",
            couverts: ["Incendie et risques annexes", "Vol de marchandises et de matériel", "Pertes d'exploitation suite à un sinistre couvert", "Bris de machine (selon formule)"],
            nonCouverts: ["Stock non déclaré à sa valeur réelle", "Vétusté du matériel non entretenu", "Interruption d'activité non liée à un sinistre couvert"],
            questions: [
                { q: "La perte d'exploitation, ça couvre quoi exactement ?", r: "La marge/le chiffre d'affaires que vous auriez normalement réalisé pendant l'arrêt forcé, dans la limite prévue au contrat." },
                { q: "Dois-je réévaluer mon stock chaque année ?", r: "C'est fortement recommandé — un stock sous-évalué peut réduire votre indemnisation en cas de sinistre." },
                { q: "Le matériel en leasing est-il couvert ?", r: "Oui, généralement, mais à déclarer spécifiquement au contrat." }
            ],
            formules: [
                { nom: "Local Simple", prix: "dès 100 000 FCFA/an", desc: "Petit local, stock limité" },
                { nom: "Commerce/Entrepôt", prix: "dès 250 000 FCFA/an", desc: "+ pertes d'exploitation" },
                { nom: "Industrielle", prix: "sur devis", desc: "Sites de production, machines" }
            ],
            etape: "Une visite ou un descriptif détaillé de vos locaux nous permet d'établir une proposition précise sous 48h.",
        },
    };

    function openProduitModal(cle) {
        const p = PRODUITS_5E[cle];
        if (!p) return;

        document.getElementById('produit-titre').textContent = p.titre;
        document.getElementById('produit-icone-titre').innerHTML = `<i class="fa-solid ${p.icone}"></i>`;
        const imgEl = document.getElementById('produit-image');
        if (imgEl) {
            imgEl.src = p.imageModal || p.image || '';
            imgEl.alt = p.titre;
            imgEl.style.aspectRatio = '600/657'; // ratio unique, identique aux 8 cartes
        }
        document.getElementById('produit-exemple').textContent = p.exemple;
        document.getElementById('produit-explication').textContent = p.explication;
        document.getElementById('produit-enjeu-intro').textContent = p.enjeuIntro;

        document.getElementById('produit-couverts').innerHTML = p.couverts
            .map((c) => `<li class="flex items-start gap-1.5"><i class="fa-solid fa-check text-emerald-500 mt-0.5 text-[10px]"></i><span>${c}</span></li>`).join('');
        document.getElementById('produit-non-couverts').innerHTML = p.nonCouverts
            .map((c) => `<li class="flex items-start gap-1.5"><i class="fa-solid fa-xmark text-red-400 mt-0.5 text-[10px]"></i><span>${c}</span></li>`).join('');

        document.getElementById('produit-questions').innerHTML = p.questions.map((qa) => `
            <div>
                <p class="text-xs font-bold text-gray-700">${qa.q}</p>
                <p class="text-xs text-gray-500">${qa.r}</p>
            </div>
        `).join('');

        document.getElementById('produit-formules').innerHTML = p.formules.map((f) => `
            <div class="bg-slate-50 border border-gray-200 rounded-xl p-3 text-center">
                <div class="text-xs font-extrabold text-navy mb-0.5">${f.nom}</div>
                <div class="text-[11px] font-bold text-brandPurple mb-1">${f.prix}</div>
                <div class="text-[10px] text-gray-500 leading-snug">${f.desc}</div>
            </div>
        `).join('');

        document.getElementById('produit-etape').textContent = p.etape;

        const ctaBtn = document.getElementById('produit-cta-btn');
        ctaBtn.onclick = () => {
            closeProduitModal();
            if (p.branchePersonnalisee) {
                selectDevisRisk(p.branche, p.branchePersonnalisee, p.brancheI18nKey);
            } else {
                selectDevisRisk(p.branche);
            }
            document.getElementById('devis')?.scrollIntoView({ behavior: 'smooth' });
            openDevisModal();
        };

        document.getElementById('produitModal').classList.remove('hidden');
    }

    function closeProduitModal() {
        document.getElementById('produitModal').classList.add('hidden');
    }

    // Compagnies partenaires — logos fournis par Roger le 13/08/2026.
    // Atlantique Assurances Cameroun a changé de nom pour AFG Assurances
    // Cameroun (mis à jour). Samiris Insurance Company et Alpha Assurances
    // SA retirées de la liste (décision du 13/08/2026).
    // Chargées depuis site.partenaires_assurance via /api/listes-reference
    // (décidé le 15/08/2026 : configuration pilotable par l'administrateur
    // du site -- ajout d'une compagnie, actif/inactif, poids d'apparition --
    // sans jamais avoir à modifier ce fichier). Remplie par loadListesReference().
    let COMPAGNIES_PARTENAIRES = [];

    function initPartnersBar() {
        // Taille réduite de moitié le 20/08/2026, à la demande de Roger :
        // double la densité de logos visibles simultanément dans la bande
        // défilante, sans toucher aux fichiers eux-mêmes (aucune perte de
        // qualité -- le navigateur réduit juste l'affichage d'une image
        // déjà là, aucune requête réseau supplémentaire, donc aucun
        // impact sur la vitesse de rendu).
        const carte = (c) => `
            <div class="flex items-center justify-center border border-transparent hover:border-brandPurple/40 hover:bg-white hover:shadow-sm rounded-lg px-1.5 h-10 shrink-0 transition-all" title="${c.nom}">
                <img src="${c.logo}" alt="${c.nom}" style="width:28px; height:28px;" class="object-contain">
            </div>
        `;
        const html = COMPAGNIES_PARTENAIRES.map(carte).join('');
        const listA = document.getElementById('partners-list-a');
        const listB = document.getElementById('partners-list-b');
        if (listA) listA.innerHTML = html;
        if (listB) listB.innerHTML = html; // copie identique, pour la boucle continue
    }

    // -----------------------------------------------------------------
    // ANIMATION ALÉATOIRE DES LOGOS PARTENAIRES — cahier des charges du
    // 13/08/2026 :
    //   1. Zone plein écran (100dvh), positionnée avant la bande défilante
    //   2. Logos à position ET durée aléatoires (apparition/disparition)
    //   3. Garantie : tous les logos passent au moins une fois par cycle
    //   4. Garantie : jamais deux fois de suite au même endroit
    //   5. Garantie : jamais d'écran totalement vide
    //   6. Rythme pensé pour rester léger en CPU (peu d'emplacements,
    //      transitions CSS -- accélérées matériellement -- plutôt que
    //      des recalculs JS répétés, minuteurs individuels plutôt qu'une
    //      boucle serrée)
    // -----------------------------------------------------------------
    // -----------------------------------------------------------------
    // MOTEUR DE TIRAGE — révisé le 15/08/2026 selon le nouveau cahier des
    // charges de Roger. Deux ensembles suivis en permanence :
    //   - partnerActuellementVisibles : compagnies AFFICHÉES là, tout de
    //     suite (garantie n°3 : jamais deux fois le même logo en même
    //     temps à l'écran, quel que soit l'emplacement)
    //   - partnerCycleVus : compagnies déjà passées depuis la dernière
    //     réinitialisation (garantie n°5 : un "cycle" = les 18 compagnies
    //     sont toutes passées au moins une fois ; dès que c'est le cas,
    //     réinitialisation automatique et un nouveau cycle démarre)
    // -----------------------------------------------------------------
    let partnerActuellementVisibles = new Set();
    let partnerCycleVus = new Set();

    function partnerNext() {
        // Bassin pondéré (poids_apparition, ex: GMC/Belife/Royal Onyx à 3x
        // depuis le 15/08/2026), en excluant tout ce qui est déjà visible
        // ailleurs à l'instant T.
        const construireBassin = (avecExclusion) => {
            const pool = [];
            COMPAGNIES_PARTENAIRES.forEach((c) => {
                if (avecExclusion && partnerActuellementVisibles.has(c.nom)) return;
                const poids = Math.max(1, c.poids || 1);
                for (let i = 0; i < poids; i++) pool.push(c);
            });
            return pool;
        };

        let pool = construireBassin(true);
        // Garde-fou : si jamais le bassin exclu était vide (cas limite --
        // plus d'emplacements actifs que de compagnies), on retombe sur
        // toutes les compagnies pour ne jamais bloquer l'animation.
        if (pool.length === 0) pool = construireBassin(false);

        const compagnie = pool[Math.floor(Math.random() * pool.length)];

        partnerActuellementVisibles.add(compagnie.nom);
        partnerCycleVus.add(compagnie.nom);

        // Cycle complet : réinitialise pour repartir sur un nouveau cycle
        // de 18 (garantie n°5).
        if (partnerCycleVus.size >= COMPAGNIES_PARTENAIRES.length) {
            partnerCycleVus = new Set();
        }

        return compagnie;
    }

    function partnerLibere(nom) {
        partnerActuellementVisibles.delete(nom);
    }

    function partnerRandom(min, max) {
        return min + Math.random() * (max - min);
    }

    // Positionnement en grille de cellules dédiées (correctif du
    // 13/08/2026) : chaque emplacement possède SA PROPRE région
    // rectangulaire, jamais partagée avec un autre -- deux logos ne
    // peuvent donc JAMAIS se chevaucher, par construction géométrique,
    // plutôt que par un calcul de collision a posteriori (plus fiable,
    // moins coûteux en CPU). Le tirage aléatoire ne joue qu'À L'INTÉRIEUR
    // de la cellule assignée, pour garder un effet organique.
    function partnerGridPour(n) {
        if (n <= 4) return { cols: 2, rows: 2 };
        if (n <= 6) return { cols: 3, rows: 2 };
        return { cols: 3, rows: 3 };
    }

    function partnerPositionSlot(el, container, cellIndex, cols, rows) {
        const w = container.clientWidth;
        const h = container.clientHeight;
        const cellW = w / cols;
        const cellH = h / rows;
        const col = cellIndex % cols;
        const row = Math.floor(cellIndex / cols);

        const taille = parseFloat(el.style.width) || 110;
        // Le logo reste TOUJOURS entièrement à l'intérieur de sa cellule --
        // son coin ne peut se déplacer que dans l'espace qui garantit ça.
        const maxDecalageX = Math.max(0, cellW - taille);
        const maxDecalageY = Math.max(0, cellH - taille);

        el.style.left = `${(col * cellW) + partnerRandom(0, maxDecalageX)}px`;
        el.style.top = `${(row * cellH) + partnerRandom(0, maxDecalageY)}px`;
    }

    function partnerScheduleSlot(el, cellIndex, cols, rows, rythme) {
        const delaiCache = partnerRandom(rythme.cacheMin, rythme.cacheMax);
        setTimeout(() => {
            const container = document.getElementById('partners-animation-zone');
            if (!container) return; // page quittée / section retirée

            const compagnie = partnerNext();
            el.dataset.dernierNom = compagnie.nom;
            partnerPositionSlot(el, container, cellIndex, cols, rows);
            el.innerHTML = `<img src="${compagnie.logo}" alt="" class="max-w-full max-h-full object-contain drop-shadow-md">`;
            el.classList.remove('opacity-0');
            el.classList.add('opacity-100');

            const dureeVisible = partnerRandom(rythme.visibleMin, rythme.visibleMax);
            setTimeout(() => {
                el.classList.remove('opacity-100');
                el.classList.add('opacity-0');
                // Libère le nom seulement APRÈS la transition de fondu
                // (700ms, cf. classe CSS) -- sinon un autre emplacement
                // pourrait piocher la même compagnie pendant qu'elle est
                // encore visuellement visible en train de disparaître.
                setTimeout(() => partnerLibere(compagnie.nom), 700);
                partnerScheduleSlot(el, cellIndex, cols, rows, rythme); // relance son propre cycle indépendamment
            }, dureeVisible);
        }, delaiCache);
    }

    // -----------------------------------------------------------------
    // AJUSTEMENT DE LA HAUTEUR DE LA SECTION PARTENAIRES — corrige le
    // 13/08/2026 : l'en-tête du site est "sticky" (toujours visible en
    // haut de l'écran), donc sa hauteur doit être SOUSTRAITE de la
    // hauteur "plein écran" de la section, sinon celle-ci déborde
    // exactement de la hauteur de l'en-tête sous la ligne de flottaison
    // (le bandeau du bas se retrouve alors hors de vue, comme observé).
    // Mesurée en JS (plutôt que codée en dur) pour rester juste même si
    // l'en-tête change un jour (texte plus long, logo redimensionné...).
    // -----------------------------------------------------------------
    function ajusterHauteurSectionPartenaires() {
        const section = document.getElementById('partenaires');
        const header = document.querySelector('header');
        if (!section || !header) return;

        // Plafonne la hauteur d'en-tête soustraite à 150px -- garde-fou :
        // si jamais la mesure réelle était anormalement grande (bug futur,
        // en-tête modifié plus tard...), la zone ne doit JAMAIS pouvoir
        // s'effondrer à une taille invisible. 150px est très généreux pour
        // un en-tête normal (~60-65px mesuré en pratique).
        const hauteurEntete = Math.min(header.offsetHeight || 0, 150);
        section.style.height = `calc(100vh - ${hauteurEntete}px)`;
        section.style.height = `calc(100dvh - ${hauteurEntete}px)`; // écrase la ligne au-dessus si dvh supporté
    }

    // Même principe que ci-dessus, appliqué à la section Assistance
    // (ajoutée le 20/08/2026) -- Roger jugeait l'ancienne version trop
    // grande ("presque une page de blanc à elle seule"). Fonction séparée
    // plutôt que généralisée à un id variable : chacune reste indépendante,
    // une panne sur l'une n'affecte jamais l'autre (même philosophie que
    // les blocs try/catch déjà séparés pour Partenaires).
    function ajusterHauteurSectionAssistance() {
        const section = document.getElementById('assistance');
        const header = document.querySelector('header');
        if (!section || !header) return;
        const hauteurEntete = Math.min(header.offsetHeight || 0, 150);
        section.style.height = `calc(100vh - ${hauteurEntete}px)`;
        section.style.height = `calc(100dvh - ${hauteurEntete}px)`;
    }

    function initPartnersAnimation() {
        const container = document.getElementById('partners-animation-zone');
        if (!container) return;

        // Réglages différenciés mobile/desktop (corrigé le 13/08/2026) --
        // avant cette correction, seuls le nombre et la taille des
        // emplacements changeaient, jamais le TEMPO -- d'où l'impression de
        // vide et de lenteur sur mobile (moins d'emplacements, même rythme
        // que le desktop). Mobile : plus d'emplacements ET cycle plus
        // rapide, pour combler l'écran plus étroit mais plus haut.
        //
        // Tailles doublées le 13/08/2026 (66/110px -> 132/220px), à la
        // demande de Roger. IMPORTANT : le nombre d'emplacements est réduit
        // en conséquence (6->4 mobile, 9->6 desktop) -- avec des logos deux
        // fois plus grands, garder le même nombre de cellules les aurait
        // rendues trop petites pour les contenir, risquant exactement le
        // chevauchement qu'on avait pris soin d'éliminer.
        // Emplacements mobile doublés le 20/08/2026 (4 -> 8), à la demande
        // de Roger -- avec le même garde-fou déjà appliqué le 13/08 :
        // deux fois plus d'emplacements exige des logos plus petits, sinon
        // les cellules de la grille deviennent trop petites pour les
        // contenir sans chevauchement. 132px -> 84px sur mobile (grille
        // 3x3 au lieu de 2x2, cf. partnerGridPour).
        const largeur = window.innerWidth;
        const estMobile = largeur < 640;
        const nbSlots = estMobile ? 8 : (largeur < 1024 ? 4 : 6);
        const { cols, rows } = partnerGridPour(nbSlots);
        const tailleLogo = estMobile ? 84 : 220;
        // Rythme accéléré le 15/08/2026 (points 1 et 2) : durée maximum
        // d'apparition réduite de moitié (visibleMax), et délais raccourcis
        // dans l'ensemble pour augmenter le nombre de cycles complets
        // (les 18 compagnies passées) par tranche de 60 secondes.
        const rythme = estMobile
            ? { cacheMin: 300, cacheMax: 900, visibleMin: 900, visibleMax: 1600 }
            : { cacheMin: 600, cacheMax: 2200, visibleMin: 1400, visibleMax: 2750 };

        const slots = [];
        for (let i = 0; i < nbSlots; i++) {
            const el = document.createElement('div');
            el.className = 'absolute opacity-0 transition-opacity duration-700 ease-in-out flex items-center justify-center pointer-events-none';
            el.style.width = `${tailleLogo}px`;
            el.style.height = `${tailleLogo}px`;
            container.appendChild(el);
            slots.push(el);
            partnerScheduleSlot(el, i, cols, rows, rythme);
        }

        // Garde-fou (garantie n°5 de la version précédente, toujours valable) :
        // vérifie périodiquement qu'au moins un logo est visible -- sinon en
        // force un immédiatement. Intervalle volontairement peu fréquent
        // (2,5s) pour rester léger en CPU.
        setInterval(() => {
            const visibles = slots.filter((s) => s.classList.contains('opacity-100'));
            if (visibles.length === 0 && slots.length > 0) {
                const idx = Math.floor(Math.random() * slots.length);
                const el = slots[idx];
                const compagnie = partnerNext();
                el.dataset.dernierNom = compagnie.nom;
                partnerPositionSlot(el, container, idx, cols, rows);
                el.innerHTML = `<img src="${compagnie.logo}" alt="" class="max-w-full max-h-full object-contain drop-shadow-md">`;
                el.classList.remove('opacity-0');
                el.classList.add('opacity-100');
            }
        }, 2500);
    }


    // Déclaration globale -- retrouvée manquante le 15/08/2026 (effacée
    // par erreur lors d'une modification précédente), causant un plantage
    // JS si une fonction lisait currentLang avant le premier changement de
    // langue (ex: badge de proximité déclenché tôt par la géolocalisation).
    let currentLang = 'fr';

    function setLanguage(lang) {
        currentLang = lang;
        document.querySelectorAll('[data-i18n]').forEach(element => {
            const key = element.getAttribute('data-i18n');
            if (translations[lang] && translations[lang][key]) {
                // innerHTML (et non textContent) : permet de stocker un <br>
                // directement dans le dictionnaire pour les titres à deux
                // lignes (ex. "Gestion<br>Sinistres"), sans cas particulier
                // à gérer — le retour à la ligne survit désormais aux
                // changements de langue. Contenu entièrement maîtrisé
                // (dictionnaire interne, jamais de saisie utilisateur ici).
                element.innerHTML = translations[lang][key];
            }
        });
        if(lang === 'fr') {
            document.getElementById('btn-lang-fr').className = 'text-purple-300 font-extrabold';
            document.getElementById('btn-lang-en').className = 'text-gray-300 hover:text-white transition font-normal';
        } else {
            document.getElementById('btn-lang-en').className = 'text-purple-300 font-extrabold';
            document.getElementById('btn-lang-fr').className = 'text-gray-300 hover:text-white transition font-normal';
        }

        // Si l'affichette de proximité est ouverte au moment du changement de
        // langue, on la régénère immédiatement dans la nouvelle langue —
        // plutôt que d'attendre sa prochaine apparition.
        const openBadge = document.getElementById('agence-proximite-badge');
        if (openBadge && resolvedAgencyKey && resolvedAgencyDistanceKm != null) {
            showNearestAgencyBadge(resolvedAgencyKey, resolvedAgencyDistanceKm);
        }
    }

    function toggleMobileMenu() {
        document.getElementById('mobile-menu').classList.toggle('hidden');
    }

    function setActiveMenuLink(id) {
        document.querySelectorAll('.nav-link').forEach(link => {
            const href = link.getAttribute('href');
            if (href === '#' + id) {
                link.className = 'nav-link py-1 transition text-brandPurple border-b-2 border-brandPurple';
            } else {
                link.className = 'nav-link py-1 hover:text-brandPurple transition text-navy';
            }
        });
    }

    // -----------------------------------------------------------------
    // FLÈCHES DE DÉFILEMENT ENTRE SECTIONS
    // Règle d'affichage :
    //   Page 1            : aucune flèche
    //   Page 2            : flèche BAS uniquement
    //   Page 3 à N-1      : flèches HAUT + BAS
    //   Dernière page (N) : flèche HAUT uniquement
    // Très discrètes (opacité faible), et se masquent après 3s d'inactivité
    // (réapparaissent au moindre mouvement/toucher/scroll).
    // -----------------------------------------------------------------
    const scrollPageOrder = ['accueil', 'comment-ca-marche', 'simulateur', 'assistant-ia', 'solutions', 'cabinet', 'contact'];
    let currentSectionIndex = 0;
    let arrowsDimmed = false;
    let arrowsInactivityTimer = null;

    const ARROW_OPACITY_VISIBLE = '0.35';
    const ARROW_OPACITY_HOVER_HINT = '0.35'; // le hover (CSS non nécessaire ici) reste géré par la couleur du texte

    function updateScrollArrows() {
        const upBtn = document.getElementById('scroll-arrow-up');
        const downBtn = document.getElementById('scroll-arrow-down');
        if (!upBtn || !downBtn) return;

        const total = scrollPageOrder.length;
        const idx = currentSectionIndex;
        let showUp, showDown;

        if (idx === 0) {
            showUp = false; showDown = false;                 // Page 1 : rien
        } else if (idx === total - 1) {
            showUp = true; showDown = false;                  // Dernière page : haut seul
        } else if (idx === 1) {
            showUp = false; showDown = true;                  // Page 2 : bas seul
        } else {
            showUp = true; showDown = true;                   // Pages intermédiaires : les deux
        }

        upBtn.style.display = showUp ? 'block' : 'none';
        downBtn.style.display = showDown ? 'block' : 'none';

        applyArrowsOpacity();
    }

    function applyArrowsOpacity() {
        const opacity = arrowsDimmed ? '0' : ARROW_OPACITY_VISIBLE;
        const pointerEvents = arrowsDimmed ? 'none' : 'auto';
        ['scroll-arrow-up', 'scroll-arrow-down'].forEach((id) => {
            const btn = document.getElementById(id);
            if (btn && btn.style.display !== 'none') {
                btn.style.opacity = opacity;
                btn.style.pointerEvents = pointerEvents;
            }
        });
    }

    function resetArrowsInactivityTimer() {
        if (arrowsDimmed) {
            arrowsDimmed = false;
            applyArrowsOpacity();
        }
        clearTimeout(arrowsInactivityTimer);
        arrowsInactivityTimer = setTimeout(() => {
            arrowsDimmed = true;
            applyArrowsOpacity();
        }, 3000);
    }

    function scrollToAdjacentSection(direction) {
        const targetIndex = currentSectionIndex + direction;
        if (targetIndex < 0 || targetIndex >= scrollPageOrder.length) return;
        const targetEl = document.getElementById(scrollPageOrder[targetIndex]);
        if (targetEl) targetEl.scrollIntoView({ behavior: 'smooth' });
    }

    // Mémorise la branche réelle visée quand elle diffère du radio physique
    // coché (ex. "Multirisque Pro" utilise le radio "Autres" avec un libellé
    // personnalisé) — openDevisModal() s'en sert en priorité si présent.
    let heroFormIntendedBranch = null;

    function selectDevisRisk(value, customLabel = null, realBranchValue = null) {
        // Réinitialise les libellés personnalisés avant d'en appliquer un
        // nouveau, pour ne jamais laisser un ancien texte détourné en place.
        document.getElementById('label-risk-habitation').textContent = "Habitation";
        const autresLabel = document.getElementById('label-risk-autres');
        if (autresLabel) autresLabel.textContent = "Autres";

        if (value === 'Habitation' && customLabel) {
            document.getElementById('label-risk-habitation').textContent = customLabel;
        } else if (value === 'Autres' && customLabel && autresLabel) {
            autresLabel.textContent = customLabel;
        }

        heroFormIntendedBranch = realBranchValue;

        if(value === 'Auto') document.getElementById('radio-auto').checked = true;
        else if(value === 'Sante') document.getElementById('radio-sante').checked = true;
        else if(value === 'Habitation') document.getElementById('radio-habitation').checked = true;
        else if(value === 'Autres') document.getElementById('radio-autres').checked = true;
    }

    function switchTab(tab) {
        const partBtn = document.getElementById('tab-particuliers-btn');
        const entBtn = document.getElementById('tab-entreprises-btn');
        const partContent = document.getElementById('tab-particuliers');
        const entContent = document.getElementById('tab-entreprises');
        const inviteBtnPart = document.getElementById('invite-btn-particuliers');
        const inviteBtnEnt = document.getElementById('invite-btn-entreprises');

        // Le bouton du bandeau qui reçoit la mise en avant est celui de
        // l'onglet PAS encore consulté -- l'action naturelle à proposer.
        const MIS_EN_AVANT = "inline-flex items-center gap-2 font-bold text-xs px-5 py-2.5 rounded-xl transition bg-navy hover:bg-purple-900 text-white shadow-md";
        const SECONDAIRE = "inline-flex items-center gap-2 font-bold text-xs px-5 py-2.5 rounded-xl transition bg-gray-100 hover:bg-gray-200 text-navy";

        if (tab === 'particuliers') {
            partBtn.className = "px-6 py-2.5 rounded-xl font-bold text-xs sm:text-sm transition-all duration-200 bg-white text-navy shadow-md";
            entBtn.className = "px-6 py-2.5 rounded-xl font-bold text-xs sm:text-sm transition-all duration-200 text-gray-600 hover:text-navy";
            partContent.classList.remove('hidden');
            entContent.classList.add('hidden');
            if (inviteBtnPart) inviteBtnPart.className = SECONDAIRE;
            if (inviteBtnEnt) inviteBtnEnt.className = MIS_EN_AVANT;
        } else {
            entBtn.className = "px-6 py-2.5 rounded-xl font-bold text-xs sm:text-sm transition-all duration-200 bg-white text-navy shadow-md";
            partBtn.className = "px-6 py-2.5 rounded-xl font-bold text-xs sm:text-sm transition-all duration-200 text-gray-600 hover:text-navy";
            entContent.classList.remove('hidden');
            partContent.classList.add('hidden');
            if (inviteBtnEnt) inviteBtnEnt.className = SECONDAIRE;
            if (inviteBtnPart) inviteBtnPart.className = MIS_EN_AVANT;
        }
    }

    function handlePhonePrefixChange(selectElem, targetCitySelectId) {
        const citySelect = document.getElementById(targetCitySelectId);
        if (!citySelect) return;
        if (selectElem.value !== '+237') {
            citySelect.value = 'Autre';
        } else if (citySelect.value === 'Autre') {
            citySelect.value = 'Yaoundé';
        }
    }

    // CARTE LEAFLET ET GEOLOCALISATION
    let map, marker;
    let userGeoPosition = null;   // {lat, lng} dès que la géolocalisation aboutit
    let userPositionMarker = null;
    let routeLine = null;
    // Repli minimal, utilisé UNIQUEMENT si /api/agences est indisponible
    // (DB inaccessible, etc.) -- pour que le site continue de fonctionner
    // au moins avec le siège, plutôt que de planter complètement.
    let agencesData = {
        "yaounde_siege": { lat: 3.866667, lng: 11.516667, nom: "Direction Générale & Agence Centrale", adr: "B.P. 7136 Yaoundé, Cameroun", adrDetail: "Rue Manguier, entre la Pharmacie et la Station-Service", tel: "+237 6 97 71 73 34", email: "mpro-manguier@mutuelleproassurances.com" }
    };

    // Charge les agences (et futurs pôles d'attraction) depuis PostgreSQL,
    // via l'API -- remplace l'ancien objet codé en dur. Transforme le
    // tableau JSON reçu en objet indexé par "cle", pour rester compatible
    // avec tout le code existant qui fait agencesData["yaounde_siege"].
    // Charge les 3 listes de référence du formulaire (constructeurs,
    // unités de puissance, catégories d'usage CIMA) depuis PostgreSQL,
    // et remplit les éléments correspondants. Même principe que
    // loadAgencesData() : un repli minimal si l'API est indisponible,
    // pour que le formulaire reste utilisable même sans base accessible.
    // Données du système Genre → État → Usage, conservées globalement pour
    // être filtrées dynamiquement au fil des sélections de l'utilisateur.
    let carrosseriesData = [];
    let genresEtatsData = [];
    let genresUsagesData = [];
    let carrosserieCombinaisonsData = [];

    // Registre des instances Tom Select actives (par id d'élément). Un
    // <select> déjà "habillé" par Tom Select ne peut plus être repeuplé
    // via innerHTML directement — il faut détruire puis recréer
    // l'instance à chaque changement d'options (ex: Usage qui se filtre
    // selon le Genre choisi).
    const tomSelectInstances = {};

    function initTomSelect(elementId, options = {}) {
        const el = document.getElementById(elementId);
        if (!el) return null;
        if (tomSelectInstances[elementId]) {
            tomSelectInstances[elementId].destroy();
        }
        tomSelectInstances[elementId] = new TomSelect(`#${elementId}`, {
            placeholder: '-- Choisissez --',
            allowEmptyOption: true,
            ...options,
        });
        return tomSelectInstances[elementId];
    }

    async function loadListesReference() {
        const datalistConstructeurs = document.getElementById('constructeurs-datalist');
        const selectUnite = document.getElementById('auto_power_unit');
        const selectGenreAlias = document.getElementById('auto_genre_alias');

        try {
            const response = await fetch('/api/listes-reference');
            if (!response.ok) throw new Error(`HTTP ${response.status}`);
            const data = await response.json();

            if (datalistConstructeurs && Array.isArray(data.constructeurs)) {
                datalistConstructeurs.innerHTML = data.constructeurs
                    .map((nom) => `<option value="${nom}">`).join('');
            }

            if (selectUnite && Array.isArray(data.unites_puissance)) {
                selectUnite.innerHTML = data.unites_puissance
                    .map((u) => `<option value="${u.code}">${u.code}</option>`).join('');
            }

            // Système Genre → État → Usage. "Genre" reste le terme affiché à
            // l'utilisateur ; en interne, le point d'entrée réel est la
            // Carrosserie (ce que l'agent observe sur le véhicule), qui
            // détermine le Genre canonique — voir échange du 09/08/2026.
            if (Array.isArray(data.carrosseries)) {
                carrosseriesData = data.carrosseries;
                if (selectGenreAlias) {
                    selectGenreAlias.innerHTML = '<option value="">-- Choisissez --</option>' +
                        carrosseriesData.map((c) => `<option value="${c.nom}">${c.nom}</option>`).join('') +
                        '<option value="__AUTRE__">Autre / Non listé</option>';
                    // create: false -- verrouille strictement la sélection à la liste
                    // proposée (empêche toute saisie libre qui contournerait le
                    // choix "Autre"). L'utilisateur peut chercher/filtrer, mais
                    // jamais valider une valeur qui n'est pas dans la liste.
                    initTomSelect('auto_genre_alias', { placeholder: 'Rechercher un genre...', create: false });
                }
            }
            if (Array.isArray(data.genres_etats)) {
                genresEtatsData = data.genres_etats;
            }
            if (Array.isArray(data.genres_usages)) {
                genresUsagesData = data.genres_usages;
            }
            if (Array.isArray(data.carrosserie_combinaisons)) {
                carrosserieCombinaisonsData = data.carrosserie_combinaisons;
            }

            // Compagnies partenaires — pilotables par l'administrateur du
            // site (actif, poids d'apparition) depuis le 15/08/2026, plus
            // besoin de modifier ce fichier pour ajuster la liste.
            if (Array.isArray(data.partenaires_assurance)) {
                COMPAGNIES_PARTENAIRES = data.partenaires_assurance.map((p) => ({
                    nom: p.nom, logo: p.logo_path, poids: p.poids_apparition,
                }));
                try { initPartnersBar(); } catch (e) { console.error('Erreur bande partenaires (ignorée) :', e); }
                try { initPartnersAnimation(); } catch (e) { console.error('Erreur animation partenaires (ignorée) :', e); }
            }

            if (selectUnite) {
                initTomSelect('auto_power_unit', { placeholder: 'Unité', create: false });
            }

            console.log('✅ Listes de référence chargées depuis PostgreSQL.');
        } catch (err) {
            console.warn('⚠️ Impossible de charger /api/listes-reference, repli minimal :', err);
            // Repli minimal : au moins l'unité CV par défaut, pour que le
            // formulaire reste fonctionnel même sans base accessible.
            if (selectUnite) selectUnite.innerHTML = '<option value="CV">CV</option>';
        }
    }

    // Étape 1 → 2 : dès qu'un alias de Genre est choisi, résout le Genre
    // canonique, affiche l'État si cet alias en a un, puis filtre
    // immédiatement les options d'Usage (l'État ne les influence jamais).
    // Reconstruit le libellé final du Genre à partir de l'alias et de
    // l'État choisi (ex: "Pick-up" + "Avec" -> "Pick-up double cabine").
    // Règle générique : "Avec" ajoute le nom de l'État en minuscule,
    // "Sans" l'ajoute précédé de "sans", toute autre valeur (ex: Tonnage,
    // "Léger (< 3,5 t)") s'ajoute telle quelle.
    // Reconstruit le libellé final du Genre à partir de l'alias et de LA
    // LISTE des états actuellement choisis (ex: "Camion" + Double Cabine
    // "Avec" + Double Commande "Avec" -> "Camion double cabine, avec
    // double commande"). Chaque état suit la même règle : "Avec" ajoute
    // le nom en minuscule, "Sans" l'ajoute précédé de "sans", toute autre
    // valeur (ex: Tonnage) s'ajoute telle quelle.
    // Convention confirmée le 10/08/2026 :
    // - Un état "Sans" (valeur par défaut, non-existence) reste SILENCIEUX
    //   dans le libellé -- "Camion" seul veut déjà dire "sans double cabine,
    //   sans remorque". Seul un état "Avec" (existence, hors défaut) est
    //   mentionné, explicitement préfixé "avec".
    // - EXCEPTION Double Commande : toujours mentionné explicitement, dans
    //   les deux cas -- "Simple Commande" (Sans) ou "Double Commande" (Avec)
    //   -- car la distinction elle-même est structurante pour l'auto-école,
    //   jamais un simple détail secondaire comme les autres états.
    // Correction du 10/08/2026 : Double/Simple Commande ne se mentionne QUE
    // lorsque la Catégorie effectivement résolue est 07ARC ou 07SRC --
    // "Camion Simple Commande" n'existe nulle part ailleurs. Dans tous les
    // autres cas (02, 03, 06, 08...), c'est juste "Camion", peu importe la
    // valeur de Double Commande.
    function computeGenreFinal(alias, doubleCommandeValeur, categorieResolue, etatsChoisis) {
        const morceaux = [];
        const estCategorie07 = categorieResolue === '07ARC' || categorieResolue === '07SRC';

        if (doubleCommandeValeur && estCategorie07) {
            morceaux.push(doubleCommandeValeur === 'Avec' ? 'Double Commande' : 'Simple Commande');
        }

        (etatsChoisis || []).forEach(({ nom, valeur }) => {
            if (valeur === 'Avec') morceaux.push(`avec ${nom.toLowerCase()}`);
            // "Sans" -- silencieux, jamais mentionné (valeur par défaut).
        });

        return morceaux.length > 0 ? `${alias} ${morceaux.join(', ')}` : alias;
    }

    // Met à jour le champ caché (valeur transmise) et l'aperçu visible en
    // lisant Double Commande + la Catégorie déjà résolue (si applicable) +
    // tous les blocs État actuellement affichés (0, 1 ou plusieurs).
    function updateGenreFinalPreview() {
        const aliasSelect = document.getElementById('auto_genre_alias');
        const alias = aliasSelect ? aliasSelect.value : '';

        const dcWrapper = document.getElementById('auto_double_commande_wrapper');
        const dcVisible = dcWrapper && !dcWrapper.classList.contains('hidden');
        const dcValeur = dcVisible ? document.getElementById('auto_double_commande')?.value : null;

        const categorieResolue = document.getElementById('auto_categorie')?.value || null;

        const etatsChoisis = [];
        document.querySelectorAll('#auto_genre_etats_container [data-etat-select]').forEach((sel) => {
            if (sel.value) etatsChoisis.push({ nom: sel.dataset.etatNom, valeur: sel.value });
        });

        const final = alias ? computeGenreFinal(alias, dcValeur, categorieResolue, etatsChoisis) : '';

        const hiddenField = document.getElementById('auto_genre_final');
        if (hiddenField) hiddenField.value = final;

        const preview = document.getElementById('auto_genre_final_preview');
        const previewText = document.getElementById('auto_genre_final_text');
        if (preview && previewText) {
            if (final && final !== alias) {
                previewText.textContent = final;
                preview.classList.remove('hidden');
            } else {
                preview.classList.add('hidden');
            }
        }
    }

    function handleGenreAliasChange() {
        const select = document.getElementById('auto_genre_alias');
        const alias = select ? select.value : '';
        const etatsContainer = document.getElementById('auto_genre_etats_container');
        const autreWrapper = document.getElementById('auto_genre_autre_wrapper');
        const autreInput = document.getElementById('auto_genre_autre');

        // Détruit toutes les instances Tom Select d'État précédentes avant
        // de reconstruire (nombre de blocs variable selon l'alias).
        Object.keys(tomSelectInstances)
            .filter((id) => id.startsWith('auto_genre_etat_'))
            .forEach((id) => { tomSelectInstances[id].destroy(); delete tomSelectInstances[id]; });
        if (etatsContainer) etatsContainer.innerHTML = '';

        // Réinitialise systématiquement Double Commande / Matière
        // inflammable — seront ré-affichés plus bas si applicables au
        // nouveau choix. Évite un état résiduel de la sélection précédente.
        currentCarrosserie = null;
        currentGenreCanonique = null;
        document.getElementById('auto_double_commande_wrapper')?.classList.add('hidden');
        document.getElementById('auto_matiere_inflammable_wrapper')?.classList.add('hidden');
        document.getElementById('auto_combinaison_exclue')?.classList.add('hidden');
        tomSelectInstances['auto_double_commande']?.destroy();
        delete tomSelectInstances['auto_double_commande'];
        tomSelectInstances['auto_matiere_inflammable']?.destroy();
        delete tomSelectInstances['auto_matiere_inflammable'];

        // Cas "Autre / Non listé" -- champ libre, pas de résolution
        // automatique d'Usage/Catégorie (genre inconnu du référentiel).
        if (alias === '__AUTRE__') {
            autreWrapper?.classList.remove('hidden');
            autreInput?.focus();
            updateUsageOptions(null);
            handleGenreAutreChange();
            return;
        }
        autreWrapper?.classList.add('hidden');
        if (autreInput) autreInput.value = '';

        const infoAlias = carrosseriesData.find((c) => c.nom === alias);

        if (!infoAlias) {
            updateUsageOptions(null);
            updateGenreFinalPreview();
            return;
        }

        // Génère un bloc indépendant par état applicable à cet alias
        // (0, 1 ou plusieurs — ex: Camion peut cumuler Double Cabine ET
        // Double Commande, deux dimensions jamais fusionnées).
        const etatsPourAlias = genresEtatsData.filter((e) => e.alias === alias);
        etatsPourAlias.forEach((etat, index) => {
            const blockId = `auto_genre_etat_${index}`;
            const block = document.createElement('div');
            block.innerHTML = `
                <label class="block text-[10px] font-semibold text-gray-500 mb-1">${etat.etat_nom}</label>
                <select id="${blockId}" data-etat-select data-etat-nom="${etat.etat_nom}" data-skip-memo="1" class="w-full px-3 py-2 text-xs border rounded-lg bg-white"></select>
            `;
            etatsContainer.appendChild(block);
            const selectEl = block.querySelector('select');
            selectEl.innerHTML = etat.etat_options.map((o) => `<option value="${o}">${o}</option>`).join('');
            // "Sans" par défaut pour les états binaires Avec/Sans (convention
            // du 10/08/2026 : la non-existence est la valeur par défaut).
            // Tonnage n'a pas de "non-existence" possible (le véhicule est
            // forcément l'un ou l'autre) -- reste donc à choisir explicitement.
            if (etat.etat_options.includes('Sans')) {
                selectEl.value = 'Sans';
            }
            selectEl.addEventListener('change', handleGenreEtatChange);
            initTomSelect(blockId, { placeholder: etat.etat_nom, create: false });
        });

        updateCombinaisonsEtUsage(alias, infoAlias.genre_canonique);
        updateGenreFinalPreview();
    }

    // Carrosserie et Genre canonique actuellement résolus -- mémorisés pour
    // que handleCombinaisonChange() (déclenché par Double Commande / Matière
    // inflammable) puisse recalculer l'Usage sans tout redemander.
    let currentCarrosserie = null;
    let currentGenreCanonique = null;

    // Double Commande et Matière inflammable (dérivée de la quantité) sont
    // les 2 SEULS états qui déterminent l'Usage. Règle confirmée le
    // 10/08/2026 : dès que Double Commande = Avec (véhicule d'auto-école),
    // Double Cabine, Tonnage, Remorque attachée ET Matière inflammable
    // deviennent tous inutiles — masqués, quelle que soit la carrosserie.
    function updateCombinaisonsEtUsage(carrosserie, genreCanonique) {
        currentCarrosserie = carrosserie;
        currentGenreCanonique = genreCanonique;

        const dcWrapper = document.getElementById('auto_double_commande_wrapper');
        const dcSelect = document.getElementById('auto_double_commande');
        const miWrapper = document.getElementById('auto_matiere_inflammable_wrapper');

        tomSelectInstances['auto_double_commande']?.destroy();
        delete tomSelectInstances['auto_double_commande'];

        const lignesPourCarrosserie = carrosserieCombinaisonsData.filter((c) => c.carrosserie === carrosserie);
        const besoinDC = lignesPourCarrosserie.some((l) => l.double_commande !== null);
        const besoinMI = lignesPourCarrosserie.some((l) => l.matiere_inflammable !== null);

        if (besoinDC) {
            // Libellés explicites dans les options elles-mêmes (pas juste
            // "Avec"/"Sans") -- exception confirmée le 10/08/2026 : Double
            // Commande se nomme toujours explicitement, jamais silencieux
            // même à sa valeur par défaut.
            dcSelect.innerHTML = '<option value="Sans">Simple Commande</option><option value="Avec">Double Commande</option>';
            dcSelect.value = 'Sans'; // valeur par défaut (non-existence)
            dcWrapper?.classList.remove('hidden');
            initTomSelect('auto_double_commande', { placeholder: 'Simple ou double commande ?', create: false });
        } else {
            dcWrapper?.classList.add('hidden');
        }

        // Mémorise si Matière inflammable est pertinente pour cette
        // carrosserie (indépendamment de l'état actuel de Double Commande) --
        // utilisé par applyMaskingDoubleCommande() ci-dessous.
        miWrapper.dataset.applicable = besoinMI ? '1' : '0';

        applyMaskingDoubleCommande();
        resolveUsageFromCombinaison();
    }

    // Cas 1 (confirmé 10/08/2026) : Double Commande = Avec masque Double
    // Cabine, Tonnage, Remorque attachée (états descriptifs génériques) ET
    // Matière inflammable -- inutiles pour un véhicule d'auto-école.
    function applyMaskingDoubleCommande() {
        const dcSelect = document.getElementById('auto_double_commande');
        const dcVisible = !document.getElementById('auto_double_commande_wrapper')?.classList.contains('hidden');
        const estAvec = dcVisible && dcSelect && dcSelect.value === 'Avec';

        const etatsContainer = document.getElementById('auto_genre_etats_container');
        if (etatsContainer) {
            etatsContainer.classList.toggle('hidden', estAvec);
            if (estAvec) {
                // Réinitialise les états masqués -- sinon une ancienne valeur
                // (ex: Double Cabine = "Avec") resterait invisible mais
                // active, polluant le libellé Genre reconstruit. Passe par
                // l'API Tom Select (pas juste .value) pour rester synchronisé
                // visuellement avec le widget affiché.
                etatsContainer.querySelectorAll('[data-etat-select]').forEach((sel) => {
                    tomSelectInstances[sel.id]?.setValue('', true) ?? (sel.value = '');
                });
                updateGenreFinalPreview();
            } else {
                // Ré-affichés : reviennent à "Sans" par défaut (convention du
                // 10/08/2026), pas vides -- sauf Tonnage, sans défaut possible.
                etatsContainer.querySelectorAll('[data-etat-select]').forEach((sel) => {
                    const aOptionSans = Array.from(sel.options).some((o) => o.value === 'Sans');
                    if (!sel.value && aOptionSans) {
                        tomSelectInstances[sel.id]?.setValue('Sans', true) ?? (sel.value = 'Sans');
                    }
                });
            }
        }

        const miWrapper = document.getElementById('auto_matiere_inflammable_wrapper');
        if (miWrapper) {
            const applicable = miWrapper.dataset.applicable === '1';
            miWrapper.classList.toggle('hidden', estAvec || !applicable);
            if (estAvec) {
                // Remet la quantité à 0 -- un véhicule d'auto-école n'a pas
                // vocation à transporter de matière inflammable.
                const qte = document.getElementById('auto_matiere_inflammable_quantite');
                if (qte) qte.value = '';
            }
        }
    }

    // Recalcule la liste d'Usage (ou le message d'exclusion) à chaque
    // changement de Double Commande / Quantité de matière inflammable.
    function resolveUsageFromCombinaison() {
        const exclueMsg = document.getElementById('auto_combinaison_exclue');
        exclueMsg?.classList.add('hidden');

        if (!currentCarrosserie) {
            updateUsageOptions(null);
            return;
        }

        applyMaskingDoubleCommande();

        const dcWrapper = document.getElementById('auto_double_commande_wrapper');
        const miWrapper = document.getElementById('auto_matiere_inflammable_wrapper');
        const dcVisible = dcWrapper && !dcWrapper.classList.contains('hidden');
        const miVisible = miWrapper && !miWrapper.classList.contains('hidden');
        // Double Commande a toujours une valeur (par défaut "Sans") dès que
        // le champ existe -- ne bloque donc plus jamais la résolution,
        // contrairement à avant. Idem pour Matière inflammable (dérivée de
        // la quantité, jamais vide).
        const dcValeur = dcVisible ? document.getElementById('auto_double_commande').value : null;

        // Matière inflammable dérivée de la quantité saisie -- vide ou 0 =
        // "Non", toute valeur positive = "Oui" (règle confirmée 10/08/2026).
        let miValeur = null;
        if (miVisible) {
            const quantite = parseFloat(document.getElementById('auto_matiere_inflammable_quantite')?.value);
            miValeur = (!isNaN(quantite) && quantite > 0) ? 'Oui' : 'Non';
        }

        const ligne = carrosserieCombinaisonsData.find((c) =>
            c.carrosserie === currentCarrosserie &&
            (c.double_commande || null) === (dcValeur || null) &&
            (c.matiere_inflammable || null) === (miValeur || null)
        );

        if (!ligne || ligne.codes_categorie === null) {
            exclueMsg?.classList.remove('hidden');
            updateUsageOptions(null);
            return;
        }

        updateUsageOptions(currentGenreCanonique, ligne.codes_categorie);
    }

    // Déclenché par les champs Double Commande / Matière inflammable.
    function handleCombinaisonChange() {
        resolveUsageFromCombinaison();
        saveFormDraft();
    }

    // Saisie libre pour "Autre / Non listé" -- alimente directement le
    // champ final (pas de reconstruction, pas de résolution de catégorie).
    function handleGenreAutreChange() {
        const autreInput = document.getElementById('auto_genre_autre');
        const hiddenField = document.getElementById('auto_genre_final');
        if (hiddenField) hiddenField.value = autreInput ? autreInput.value.trim() : '';
        saveFormDraft();
    }

    // Les États sont purement descriptifs — vérifié exhaustivement dans la
    // source pour Double Cabine/Tonnage : ils n'influencent jamais l'Usage
    // ni la Catégorie. Cette fonction reconstruit donc juste le libellé
    // affiché (en tenant compte de TOUS les états actifs), et sauvegarde.
    function handleGenreEtatChange() {
        updateGenreFinalPreview();
        saveFormDraft();
    }

    // Étape 2 → 3 : filtre les Usages possibles pour le Genre canonique
    // résolu, restreints en plus à `codesFiltre` quand fourni (résolu par
    // Double Commande / Matière inflammable — voir resolveUsageFromCombinaison).
    // Si un seul Usage existe et qu'il s'agit du cas "(aucune précision
    // nécessaire)", saute l'affichage du menu et résout directement la
    // Catégorie — comme spécifié.
    function updateUsageOptions(genreCanonique, codesFiltre = null) {
        const wrapper = document.getElementById('auto_usage_wrapper');
        const select = document.getElementById('auto_usage');
        const categorieField = document.getElementById('auto_categorie');

        if (!genreCanonique) {
            wrapper?.classList.add('hidden');
            tomSelectInstances['auto_usage']?.destroy();
            delete tomSelectInstances['auto_usage'];
            if (categorieField) categorieField.value = '';
            return;
        }

        let usagesPourCeGenre = genresUsagesData.filter((u) => u.genre_canonique === genreCanonique);
        if (Array.isArray(codesFiltre)) {
            usagesPourCeGenre = usagesPourCeGenre.filter((u) => codesFiltre.includes(u.code_categorie));
        }

        if (usagesPourCeGenre.length === 1 && usagesPourCeGenre[0].usage_libelle.startsWith('(aucune précision')) {
            // Un seul cas possible, rien à choisir — on résout directement.
            wrapper?.classList.add('hidden');
            tomSelectInstances['auto_usage']?.destroy();
            delete tomSelectInstances['auto_usage'];
            if (categorieField) categorieField.value = usagesPourCeGenre[0].code_categorie;
        } else {
            select.innerHTML = '<option value="">-- Choisissez --</option>' +
                usagesPourCeGenre.map((u) => `<option value="${u.code_categorie}">${u.usage_libelle}</option>`).join('');
            wrapper?.classList.remove('hidden');
            initTomSelect('auto_usage', { placeholder: 'Rechercher un usage...', create: false });
            if (categorieField) categorieField.value = ''; // en attente du choix de l'utilisateur
        }
        saveFormDraft();
    }

    function handleUsageChange() {
        const select = document.getElementById('auto_usage');
        const categorieField = document.getElementById('auto_categorie');
        if (categorieField) categorieField.value = select ? select.value : '';
        updateGenreFinalPreview(); // ex: "Camion" -> "Camion Simple Commande" si Usage devient 07SRC
        saveFormDraft();
    }

    async function loadAgencesData() {
        try {
            const response = await fetch('/api/agences');
            if (!response.ok) throw new Error(`HTTP ${response.status}`);
            const rows = await response.json();
            if (!Array.isArray(rows) || rows.length === 0) throw new Error('Réponse vide ou invalide');

            const loaded = {};
            rows.forEach((row) => {
                loaded[row.cle] = {
                    lat: parseFloat(row.latitude),
                    lng: parseFloat(row.longitude),
                    nom: row.nom,
                    adr: row.adresse || '',
                    adrDetail: row.adresse_detail || '',
                    tel: row.telephone || '',
                    email: row.email || '',
                    type: row.type || 'agence',
                };
            });
            agencesData = loaded;
            console.log(`✅ ${rows.length} agences chargées depuis PostgreSQL.`);
        } catch (err) {
            console.warn('⚠️ Impossible de charger /api/agences, repli sur le siège uniquement :', err);
        }
    }

    // Agence retenue pour l'envoi WhatsApp de la demande de cotation.
    // Par défaut : le siège. Mise à jour dès que la géolocalisation aboutit.
    let resolvedAgencyKey = 'yaounde_siege';
    let resolvedAgencyDistanceKm = null;

    // -----------------------------------------------------------------
    // Distance réelle à vol d'oiseau (formule de Haversine, en km) —
    // remplace l'ancienne approximation Math.hypot() sur les degrés bruts,
    // qui ne donnait pas une distance exploitable en kilomètres.
    // Cette fonction est partagée par TOUTES les recherches d'agence la
    // plus proche (WhatsApp et sélecteur manuel), pour garantir un résultat
    // cohérent partout.
    // -----------------------------------------------------------------
    function distanceKmHaversine(lat1, lng1, lat2, lng2) {
        const toRad = (deg) => (deg * Math.PI) / 180;
        const R = 6371; // rayon moyen de la Terre, en km
        const dLat = toRad(lat2 - lat1);
        const dLng = toRad(lng2 - lng1);
        const a =
            Math.sin(dLat / 2) ** 2 +
            Math.cos(toRad(lat1)) * Math.cos(toRad(lat2)) * Math.sin(dLng / 2) ** 2;
        return R * 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
    }

    function findNearestAgency(userLat, userLng) {
        let closestKey = 'yaounde_siege';
        let minDistance = Infinity;
        for (let key in agencesData) {
            const ag = agencesData[key];
            const dist = distanceKmHaversine(userLat, userLng, ag.lat, ag.lng);
            if (dist < minDistance) {
                minDistance = dist;
                closestKey = key;
            }
        }
        return { key: closestKey, distanceKm: minDistance };
    }

    // Distance affichée à l'utilisateur : +10% de marge (l'à-vol-d'oiseau
    // sous-estime toujours la distance réelle par la route), arrondie au
    // 0,5 km supérieur pour que "moins de X km" reste toujours vrai.
    function formatDistanceWithMargin(distanceKm) {
        const withMargin = distanceKm * 1.1;
        const rounded = Math.ceil(withMargin * 2) / 2;
        return rounded % 1 === 0 ? String(rounded) : rounded.toFixed(1);
    }

    // Met à jour la zone "Nos Agences" (sélecteur + carte + message de statut)
    // avec l'agence la plus proche détectée. Appelée automatiquement dès la
    // première résolution de géolocalisation (chargement du site), ET par
    // le bouton "Trouver l'agence la plus proche de moi" — même résultat
    // affiché immédiatement dans les deux cas, sans attendre un clic manuel.
    function applyNearestAgencyToContactSection(key, distanceKm) {
        const select = document.getElementById('select-agences');
        if (select) select.value = key;
        changerAgence(key);

        const status = document.getElementById('geo-status');
        if (status) {
            const isEn = currentLang === 'en';
            const text = isEn
                ? `Nearest branch already selected! (~${formatDistanceWithMargin(distanceKm)} km as the crow flies)`
                : `Agence la plus proche sélectionnée ! (~${formatDistanceWithMargin(distanceKm)} km à vol d'oiseau)`;
            renderGeoStatus('<i class="fa-solid fa-circle-check text-emerald-500"></i>', text);
        }
    }

    function resolveNearestAgencyForQuote() {
        if (!navigator.geolocation) return; // pas de support → on garde le siège par défaut
        navigator.geolocation.getCurrentPosition(
            (position) => {
                const { key, distanceKm } = findNearestAgency(position.coords.latitude, position.coords.longitude);
                resolvedAgencyKey = key;
                resolvedAgencyDistanceKm = distanceKm;
                userGeoPosition = { lat: position.coords.latitude, lng: position.coords.longitude };
                showNearestAgencyBadge(key, distanceKm);
                applyNearestAgencyToContactSection(key, distanceKm);
                const ag = agencesData[key];
                if (ag) updateMapRoute(ag.lat, ag.lng);
            },
            (err) => {
                console.warn('Géolocalisation refusée ou indisponible (repli sur le siège) :', err.message);
                resolvedAgencyKey = 'yaounde_siege';
                resolvedAgencyDistanceKm = null;
            },
            { timeout: 8000, enableHighAccuracy: false }
        );
    }

    // -----------------------------------------------------------------
    // Affichette signalant l'agence la plus proche détectée — volontairement
    // IMPOSANTE et PERSISTANTE : un assureur ne se cache pas. Elle reste
    // affichée jusqu'à fermeture manuelle (pas de disparition automatique).
    // -----------------------------------------------------------------
    function showNearestAgencyBadge(key, distanceKm) {
        const agence = agencesData[key];
        if (!agence) return;
        const dist = formatDistanceWithMargin(distanceKm);
        const isEn = currentLang === 'en';

        let badge = document.getElementById('agence-proximite-badge');
        if (!badge) {
            badge = document.createElement('div');
            badge.id = 'agence-proximite-badge';
            badge.className =
                'fixed z-[70] bottom-3 inset-x-3 sm:inset-x-auto sm:left-1/2 sm:-translate-x-1/2 sm:bottom-6 sm:w-[520px] ' +
                'bg-gradient-to-r from-brandRed via-brandRed to-brandPurple text-white rounded-2xl shadow-2xl ' +
                'pl-2.5 pr-4 py-4 flex items-center gap-2.5 border-2 border-white/40 badge-cursor-bolt ' +
                'hover:brightness-110 active:scale-[0.98] transition-all duration-500 ease-out translate-y-8 opacity-0 scale-95';
            badge.setAttribute('role', 'button');
            badge.setAttribute('tabindex', '0');
            badge.setAttribute('aria-label', isEn ? "View the nearest branch on the map" : "Voir l'agence la plus proche sur la carte");

            // Toute l'affichette renvoie vers la carte (section Contact) pour
            // que l'utilisateur puisse vérifier visuellement la distance
            // annoncée — sauf clic sur le bouton de fermeture (voir plus bas).
            badge.addEventListener('click', () => {
                const contactSection = document.getElementById('contact');
                if (contactSection) contactSection.scrollIntoView({ behavior: 'smooth' });
            });
            badge.addEventListener('keydown', (e) => {
                if (e.key === 'Enter' || e.key === ' ') {
                    e.preventDefault();
                    badge.click();
                }
            });

            document.body.appendChild(badge);
        }

        const badgeText = isEn
            ? {
                distance: `You're less than ${dist} km from our branch!`,
                awaits: "We're looking forward to it",
                cta: 'Come visit us or get in touch',
                mapHint: 'Tap to see it on the map',
              }
            : {
                distance: `Vous êtes à moins de ${dist} km de notre agence !`,
                awaits: 'Nous vous attendons',
                cta: 'Venez nous voir ou contactez-nous',
                mapHint: 'Toucher pour voir sur la carte',
              };

        badge.innerHTML = `
            <div class="shrink-0 bg-white rounded-xl p-1 flex items-center justify-center">
                <img src="Logo_MPRO.png" alt="MutuellePro" class="h-12 w-auto object-contain">
            </div>
            <div class="flex-1">
                <div class="text-base font-extrabold leading-tight">${badgeText.distance}</div>
                <div class="text-base font-extrabold leading-tight mt-1">${agence.nom.toUpperCase()}</div>
                <div class="text-[13px] leading-snug opacity-80 mt-1"><i class="fa-solid fa-arrow-right"></i> ${badgeText.awaits}</div>
                <div class="text-xs leading-snug opacity-70"><i class="fa-solid fa-comment"></i> ${badgeText.cta}</div>
                <div class="text-[10px] leading-snug opacity-60 mt-1.5 italic"><i class="fa-solid fa-map-location-dot"></i> ${badgeText.mapHint}</div>
            </div>
            <button onclick="event.stopPropagation(); document.getElementById('agence-proximite-badge').remove()"
                    class="text-white/80 hover:text-white text-xl shrink-0 self-start cursor-pointer" style="cursor:pointer;" aria-label="${isEn ? 'Close' : 'Fermer'}">
                <i class="fa-solid fa-xmark"></i>
            </button>
        `;

        requestAnimationFrame(() => {
            badge.classList.remove('translate-y-8', 'opacity-0', 'scale-95');
        });

        // Pas de disparition automatique : reste visible tant que l'utilisateur
        // ne la ferme pas lui-même via le bouton ✕.
    }

    function initMap() {
        const defaultAgence = agencesData["yaounde_siege"];
        map = L.map('map').setView([defaultAgence.lat, defaultAgence.lng], 13);
        L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
            maxZoom: 19,
            attribution: '© OpenStreetMap'
        }).addTo(map);
        const pulseIcon = L.divIcon({ className: '', html: '<div class="pulse-dot"></div>', iconSize: [16, 16] });
        marker = L.marker([defaultAgence.lat, defaultAgence.lng], { icon: pulseIcon }).addTo(map)
            .bindPopup(defaultAgence.nom)
            .openPopup();
    }

    // Trace (ou retrace) le trajet en pointillés entre la position détectée
    // de l'utilisateur et l'agence actuellement affichée sur la carte —
    // pur ajout visuel, n'affecte aucune autre fonctionnalité existante.
    function updateMapRoute(agenceLat, agenceLng) {
        if (!map || !userGeoPosition) return;

        const userIcon = L.divIcon({ className: '', html: '<div class="user-dot"></div>', iconSize: [14, 14] });
        if (userPositionMarker) {
            userPositionMarker.setLatLng([userGeoPosition.lat, userGeoPosition.lng]);
        } else {
            userPositionMarker = L.marker([userGeoPosition.lat, userGeoPosition.lng], { icon: userIcon })
                .addTo(map)
                .bindPopup('Votre position');
        }

        const routeCoords = [[userGeoPosition.lat, userGeoPosition.lng], [agenceLat, agenceLng]];
        if (routeLine) {
            routeLine.setLatLngs(routeCoords);
        } else {
            routeLine = L.polyline(routeCoords, { color: '#8B5CF6', weight: 2, dashArray: '6, 8', opacity: 0.8 }).addTo(map);
        }

        map.fitBounds(routeCoords, { padding: [40, 40], maxZoom: 14 });
    }

    // Affiche le statut de géolocalisation avec l'icône isolée sur la marge
    // gauche (colonne fixe) et le texte dans une colonne flexible à côté —
    // si le texte est assez long pour passer à la ligne, la suite s'aligne
    // proprement sous le texte, pas sous l'icône répétée.
    function renderGeoStatus(icon, text) {
        const status = document.getElementById('geo-status');
        if (!status) return;
        status.classList.remove('hidden');
        status.innerHTML = `<span class="shrink-0">${icon}</span><span>${text}</span>`;
    }

    function changerAgence(key) {
        const agence = agencesData[key];
        if (!agence) return;
        document.getElementById('agence-nom').textContent = agence.nom;
        document.getElementById('agence-adresse').textContent = agence.adr;
        document.getElementById('agence-adresse-detail').textContent = agence.adrDetail || '';
        const telLink = document.getElementById('agence-tel');
        telLink.textContent = agence.tel;
        telLink.setAttribute('href', 'tel:' + agence.tel.replace(/\s+/g, ''));

        if (map && marker) {
            marker.setLatLng([agence.lat, agence.lng]).bindPopup(agence.nom).openPopup();
            if (userGeoPosition) {
                updateMapRoute(agence.lat, agence.lng);
            } else {
                map.setView([agence.lat, agence.lng], 14);
            }
        }

        // Signale la distance de l'agence choisie manuellement — sauf si
        // c'est déjà l'agence la plus proche détectée : le kilométrage a
        // alors déjà été annoncé plus haut, pas besoin de le répéter.
        const status = document.getElementById('geo-status');
        if (status && userGeoPosition) {
            const isEn = currentLang === 'en';
            if (key === resolvedAgencyKey) {
                renderGeoStatus('<i class="fa-solid fa-circle-check text-emerald-500"></i>', isEn ? 'This is the nearest branch!' : "C'est l'agence la plus proche !");
            } else {
                const d = distanceKmHaversine(userGeoPosition.lat, userGeoPosition.lng, agence.lat, agence.lng);
                const text = isEn
                    ? `This branch is about ${formatDistanceWithMargin(d)} km away as the crow flies.`
                    : `Cette agence est à environ ${formatDistanceWithMargin(d)} km à vol d'oiseau.`;
                renderGeoStatus('<i class="fa-solid fa-location-dot text-brandPurple"></i>', text);
            }
        }
    }

    function trouverAgencePlusProche() {
        const status = document.getElementById('geo-status');
        status.classList.remove('hidden');
        status.textContent = "Recherche de votre position...";

        if (!navigator.geolocation) {
            status.textContent = "La géolocalisation n'est pas supportée par votre navigateur.";
            return;
        }

        navigator.geolocation.getCurrentPosition(
            (position) => {
                const { key, distanceKm } = findNearestAgency(position.coords.latitude, position.coords.longitude);
                applyNearestAgencyToContactSection(key, distanceKm);
            },
            (err) => {
                console.warn('Géolocalisation refusée ou indisponible :', err.message);
                status.textContent = "Impossible d'accéder à votre position. Vérifiez que vous naviguez bien sur http://localhost (pas un fichier ouvert directement) et que la géolocalisation est autorisée.";
            },
            { timeout: 8000, enableHighAccuracy: false }
        );
    }

    // FONCTIONNALITES MODALE DEVIS & STEPPER
    function openDevisModal() {
        // Lancée le plus tôt possible pour qu'elle ait le temps d'aboutir
        // avant que l'utilisateur ne soumette sa demande.
        resolveNearestAgencyForQuote();

        const heroPhone = document.getElementById('devis-phone') ? document.getElementById('devis-phone').value : '';
        if (heroPhone) {
            document.getElementById('modal_user_phone').value = heroPhone;
        }

        const selectedRadio = document.querySelector('input[name="risque"]:checked');
        const insuranceSelect = document.getElementById('insurance_type');

        // Priorité à la branche réellement visée (ex. Multirisque Pro via le
        // radio "Autres") — sinon, on retombe sur la valeur littérale du
        // radio physique coché, comme avant.
        const targetValue = heroFormIntendedBranch || (selectedRadio ? selectedRadio.value : null);

        if (targetValue && insuranceSelect) {
            const val = targetValue.toLowerCase();
            for (let option of insuranceSelect.options) {
                if (option.value.toLowerCase() === val) {
                    option.selected = true;
                    insuranceSelect.dispatchEvent(new Event('change'));
                    break;
                }
            }
        }

        const heroCity = document.getElementById('devis-city-select') ? document.getElementById('devis-city-select').value : '';
        if (heroCity && document.getElementById('modal_message')) {
            document.getElementById('modal_message').value = `Ville de résidence : ${heroCity}`;
        }

        goToStep1();
        const modal = document.getElementById('devisModal');
        if (modal) modal.classList.remove('hidden');
    }

    function closeDevisModal() {
        const modal = document.getElementById('devisModal');
        if (modal) modal.classList.add('hidden');
    }

    // -------------------------------------------------------------
    // DÉCLARATION DE SINISTRE -- fiche simplifiée (19/08/2026). Réutilise
    // le mécanisme de référence + envoi WhatsApp/email déjà éprouvé sur
    // le devis, mais en autonome (pas de dépendance à MEMO_I18N, pour ne
    // rien risquer de casser sur le flux devis existant). En attendant le
    // vrai système de comptes/tickets (Volet 2), cette référence reste le
    // seul moyen de suivi -- à conserver par le client.
    // -------------------------------------------------------------
    function openSinistreModal() {
        const modal = document.getElementById('sinistreModal');
        if (modal) modal.classList.remove('hidden');
        const dateInput = document.getElementById('sinistre-date');
        if (dateInput && !dateInput.value) {
            dateInput.max = new Date().toISOString().split('T')[0]; // jamais une date future
            dateInput.value = new Date().toISOString().split('T')[0];
        }
    }

    function closeSinistreModal() {
        const modal = document.getElementById('sinistreModal');
        if (modal) modal.classList.add('hidden');
    }

    // -------------------------------------------------------------
    // ESPACE CLIENT -- Helpdesk Volet 2 (19/08/2026). Session gérée par
    // cookie httpOnly côté serveur (voir auth.routes.js) : ce JS ne lit ni
    // ne stocke jamais le cookie directement, `credentials: 'include'`
    // suffit à le faire voyager automatiquement à chaque requête.
    // -------------------------------------------------------------

    // Libellés de statut affichés (bilingues, cohérents avec data-i18n)
    // en fonction du code_statut_ticket renvoyé par /api/mes-tickets.
    function espaceStatutLabel(codeStatut) {
        const t = translations[currentLang] || translations.fr;
        const map = { recu: t['espace.statutRecu'], en_cours: t['espace.statutEnCours'], resolu: t['espace.statutResolu'] };
        return map[codeStatut] || codeStatut;
    }
    function espaceStatutBadgeClass(codeStatut) {
        if (codeStatut === 'resolu') return 'bg-emerald-50 text-emerald-700 border-emerald-200';
        if (codeStatut === 'en_cours') return 'bg-amber-50 text-amber-700 border-amber-200';
        return 'bg-blue-50 text-blue-700 border-blue-200'; // recu
    }

    // Ouvre la modale et détermine immédiatement l'état de connexion en
    // interrogeant /api/mes-tickets : un 401 signifie "non connecté" (vue
    // anonyme), un succès signifie "connecté" (vue tickets). Évite un
    // endpoint dédié "qui suis-je" -- une seule requête sert les deux buts.
    async function openEspaceClientModal() {
        const modal = document.getElementById('espaceClientModal');
        if (modal) modal.classList.remove('hidden');
        await espaceRefreshState();
    }

    function closeEspaceClientModal() {
        const modal = document.getElementById('espaceClientModal');
        if (modal) modal.classList.add('hidden');
    }

    async function espaceRefreshState() {
        const vueAnonyme = document.getElementById('espace-vue-anonyme');
        const vueConnecte = document.getElementById('espace-vue-connecte');
        refreshTopbarSessionIndicator();
        try {
            const resp = await fetch('/api/mes-tickets', { credentials: 'include' });
            if (resp.status === 401) {
                vueAnonyme.classList.remove('hidden');
                vueConnecte.classList.add('hidden');
                return;
            }
            const data = await resp.json();
            if (!data.succes) throw new Error('mes-tickets: réponse en échec');
            vueAnonyme.classList.add('hidden');
            vueConnecte.classList.remove('hidden');
            espaceRenderTickets(data.tickets);
        } catch (err) {
            console.error('Espace Client — erreur de vérification de session :', err);
            // En cas de doute (erreur réseau), on retombe sur la vue anonyme
            // plutôt que de laisser un état intermédiaire incohérent.
            vueAnonyme.classList.remove('hidden');
            vueConnecte.classList.add('hidden');
        }
    }

    // -------------------------------------------------------------
    // Indicateur "connecté" dans la topbar (19/08/2026, sur demande de
    // Roger) -- utilise /api/session, volontairement distinct de
    // /api/mes-tickets pour ne pas charger la liste complète des tickets
    // sur CHAQUE page vue alors que la topbar a juste besoin de savoir
    // si on est connecté. Appelé au chargement de la page ET à chaque
    // fois que l'état de connexion change (connexion/inscription/
    // déconnexion), via espaceRefreshState().
    // -------------------------------------------------------------
    function refreshTopbarSessionIndicator() {
        const link = document.getElementById('topbar-espace-link');
        const icon = document.getElementById('topbar-espace-icon');
        const label = document.getElementById('topbar-espace-label');
        if (!link || !icon || !label) return;

        fetch('/api/session', { credentials: 'include' })
            .then((r) => (r.status === 200 ? r.json() : null))
            .then((data) => {
                const t = translations[currentLang] || translations.fr;
                if (data && data.succes && data.connecte) {
                    link.classList.remove('bg-brandPurple/40', 'hover:bg-brandPurple', 'border-purple-400/30');
                    link.classList.add('bg-emerald-500', 'hover:bg-emerald-400', 'border-emerald-300', 'shadow-[0_0_8px_rgba(16,185,129,0.6)]');
                    icon.className = 'fa-solid fa-circle-user text-[9px] lg:text-xs';
                    const identifiant = (data.compte && data.compte.email) || '';
                    const court = identifiant.length > 18 ? identifiant.slice(0, 15) + '…' : identifiant;
                    label.removeAttribute('data-i18n'); // évite qu'un changement de langue n'écrase l'email affiché
                    label.textContent = court || t['topbar.loggedInAs'];
                } else {
                    link.classList.add('bg-brandPurple/40', 'hover:bg-brandPurple', 'border-purple-400/30');
                    link.classList.remove('bg-emerald-500', 'hover:bg-emerald-400', 'border-emerald-300', 'shadow-[0_0_8px_rgba(16,185,129,0.6)]');
                    icon.className = 'fa-solid fa-user-lock text-[9px] lg:text-xs';
                    label.setAttribute('data-i18n', 'topbar.clientSpace');
                    label.textContent = t['topbar.clientSpace'];
                }
            })
            .catch((err) => console.error('Indicateur topbar — erreur de vérification de session :', err));
    }

    function espaceRenderTickets(tickets) {
        const t = translations[currentLang] || translations.fr;
        const container = document.getElementById('espace-tickets-list');
        if (!container) return;

        if (!tickets || tickets.length === 0) {
            container.innerHTML = `<p class="text-xs text-gray-400 text-center py-6">${t['espace.noTickets']}</p>`;
            return;
        }

        const labelKey = currentLang === 'en' ? 'type_libelle_en' : 'type_libelle_fr';
        container.innerHTML = tickets.map((ticket) => {
            const dateStr = new Date(ticket.date_creation).toLocaleDateString(
                currentLang === 'en' ? 'en-US' : 'fr-FR', { day: '2-digit', month: 'short', year: 'numeric' }
            );
            return `
                <div class="border border-gray-200 rounded-xl p-3">
                    <div class="flex justify-between items-start gap-2 mb-1">
                        <span class="text-xs font-extrabold text-navy">${ticket[labelKey]}</span>
                        <span class="text-[10px] font-bold px-2 py-0.5 rounded-full border shrink-0 ${espaceStatutBadgeClass(ticket.code_statut_ticket)}">${espaceStatutLabel(ticket.code_statut_ticket)}</span>
                    </div>
                    <p class="text-[11px] text-gray-400">${ticket.code_ticket} — ${dateStr}</p>
                </div>
            `;
        }).join('');
    }

    // -------------------------------------------------------------
    // Évaluateurs discrets de robustesse/correspondance du mot de passe
    // (19/08/2026, sur demande de Roger) -- simples barres de progression
    // colorées de 4px de haut, aucun texte intrusif, mise à jour en direct.
    // -------------------------------------------------------------
    function evaluerForcePassword() {
        const val = document.getElementById('espace-register-password').value;
        const bar = document.getElementById('espace-password-strength-bar');
        if (!bar) return;
        let score = 0;
        if (val.length >= 10) score++;
        if (/[a-zA-Z]/.test(val)) score++;
        if (/[0-9]/.test(val)) score++;
        if (/[^a-zA-Z0-9]/.test(val)) score++;
        const pct = val.length ? (score / 4) * 100 : 0;
        bar.style.width = pct + '%';
        bar.className = 'h-full transition-all duration-200 ' + (
            score <= 1 ? 'bg-red-400' : score === 2 ? 'bg-orange-400' : score === 3 ? 'bg-yellow-400' : 'bg-emerald-500'
        );
        // Le mot de passe de référence a changé -- la correspondance
        // affichée dans le second champ doit se recalculer aussitôt.
        evaluerCorrespondancePassword();
    }

    function evaluerCorrespondancePassword() {
        const val = document.getElementById('espace-register-password').value;
        const confirmEl = document.getElementById('espace-register-password-confirm');
        const bar = document.getElementById('espace-password-match-bar');
        if (!confirmEl || !bar) return;
        const confirm = confirmEl.value;

        if (!confirm) {
            bar.style.width = '0%';
            bar.className = 'h-full w-0 bg-gray-300 transition-all duration-200';
            return;
        }

        // Rouge dès la première divergence de caractère, quelle que soit
        // sa position -- pas d'attente jusqu'à la fin de la saisie.
        let divergence = false;
        for (let i = 0; i < confirm.length; i++) {
            if (confirm[i] !== val[i]) { divergence = true; break; }
        }

        if (divergence) {
            bar.style.width = '100%';
            bar.className = 'h-full transition-all duration-200 bg-red-500';
            return;
        }

        const pct = val.length ? Math.min(100, (confirm.length / val.length) * 100) : 0;
        bar.style.width = pct + '%';
        bar.className = 'h-full transition-all duration-200 ' + (pct >= 100 ? 'bg-emerald-500' : 'bg-blue-400');
    }

    function espaceSwitchTab(tab) {
        const tabLogin = document.getElementById('espace-tab-login');
        const tabRegister = document.getElementById('espace-tab-register');
        const formLogin = document.getElementById('espace-login-form');
        const formRegister = document.getElementById('espace-register-form');

        const activeClasses = ['bg-white', 'text-navy', 'shadow-sm'];
        const inactiveClasses = ['text-gray-500'];

        if (tab === 'register') {
            formLogin.classList.add('hidden');
            formRegister.classList.remove('hidden');
            tabRegister.classList.add(...activeClasses);
            tabRegister.classList.remove(...inactiveClasses);
            tabLogin.classList.remove(...activeClasses);
            tabLogin.classList.add(...inactiveClasses);
        } else {
            formRegister.classList.add('hidden');
            formLogin.classList.remove('hidden');
            tabLogin.classList.add(...activeClasses);
            tabLogin.classList.remove(...inactiveClasses);
            tabRegister.classList.remove(...activeClasses);
            tabRegister.classList.add(...inactiveClasses);
        }
    }

    async function espaceLogout() {
        try {
            await fetch('/api/deconnexion', { method: 'POST', credentials: 'include' });
        } catch (err) {
            console.error('Espace Client — erreur de déconnexion :', err);
        }
        const t = translations[currentLang] || translations.fr;
        showToast(t['espace.logoutSuccess'], 'success');
        await espaceRefreshState();
    }

    document.addEventListener('DOMContentLoaded', function () {
        // Vérifie l'état de connexion dès le chargement de la page, pour
        // que l'indicateur topbar reflète la session dès l'affichage,
        // sans attendre une ouverture de la modale Espace Client.
        refreshTopbarSessionIndicator();

        const loginForm = document.getElementById('espace-login-form');
        if (loginForm) {
            loginForm.addEventListener('submit', async function (event) {
                event.preventDefault();
                const t = translations[currentLang] || translations.fr;
                const submitBtn = document.getElementById('espace-login-submit');
                const original = submitBtn.innerHTML;
                submitBtn.disabled = true;
                submitBtn.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i>';

                const identifiant = document.getElementById('espace-login-identifiant').value.trim();
                const mot_de_passe = document.getElementById('espace-login-password').value;

                try {
                    const resp = await fetch('/api/connexion', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        credentials: 'include',
                        body: JSON.stringify({ identifiant, mot_de_passe }),
                    });
                    const data = await resp.json();
                    if (!data.succes) {
                        showToast((data.erreurs && data.erreurs[0]) || t['espace.errorGeneric'], 'error');
                        return;
                    }
                    showToast(t['espace.loginSuccess'], 'success');
                    loginForm.reset();
                    await espaceRefreshState();
                } catch (err) {
                    console.error('Espace Client — erreur de connexion :', err);
                    showToast(t['espace.errorGeneric'], 'error');
                } finally {
                    submitBtn.disabled = false;
                    submitBtn.innerHTML = original;
                }
            });
        }

        const registerForm = document.getElementById('espace-register-form');
        if (registerForm) {
            registerForm.addEventListener('submit', async function (event) {
                event.preventDefault();
                const t = translations[currentLang] || translations.fr;
                const submitBtn = document.getElementById('espace-register-submit');
                const original = submitBtn.innerHTML;
                submitBtn.disabled = true;
                submitBtn.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i>';

                const email = document.getElementById('espace-register-email').value.trim();
                const prefix = document.getElementById('espace-register-phone-prefix').value;
                const phone = document.getElementById('espace-register-phone').value.trim();
                const mot_de_passe = document.getElementById('espace-register-password').value;
                const mot_de_passe_confirmation = document.getElementById('espace-register-password-confirm').value;

                if (mot_de_passe !== mot_de_passe_confirmation) {
                    showToast(t['espace.passwordMismatch'], 'error');
                    submitBtn.disabled = false;
                    submitBtn.innerHTML = original;
                    return;
                }

                try {
                    const resp = await fetch('/api/inscription', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        credentials: 'include',
                        body: JSON.stringify({ email, telephone: `${prefix}${phone}`, mot_de_passe, mot_de_passe_confirmation }),
                    });
                    const data = await resp.json();
                    if (!data.succes) {
                        showToast((data.erreurs && data.erreurs[0]) || t['espace.errorGeneric'], 'error');
                        return;
                    }
                    showToast(t['espace.registerSuccess'], 'success');
                    registerForm.reset();
                    // .reset() ne déclenche pas 'input' -- les barres de
                    // force/correspondance resteraient visuellement pleines
                    // sans cet appel explicite.
                    evaluerForcePassword();
                    // L'inscription ne connecte pas automatiquement (choix
                    // délibéré côté backend, §6.4) -- on redirige donc vers
                    // l'onglet Connexion plutôt que de tenter d'afficher la
                    // vue "connecté" à tort.
                    espaceSwitchTab('login');
                    document.getElementById('espace-login-identifiant').value = email;
                } catch (err) {
                    console.error('Espace Client — erreur d\'inscription :', err);
                    showToast(t['espace.errorGeneric'], 'error');
                } finally {
                    submitBtn.disabled = false;
                    submitBtn.innerHTML = original;
                }
            });
        }
    });

    function generateSinistreRef() {
        const now = new Date();
        const pad = (n) => String(n).padStart(2, '0');
        const stamp = `${now.getFullYear()}${pad(now.getMonth() + 1)}${pad(now.getDate())}-${pad(now.getHours())}${pad(now.getMinutes())}`;
        return `SIN-${stamp}`;
    }

    const SINISTRE_I18N = {
        fr: {
            locale: 'fr-FR',
            champsManquants: 'Merci de renseigner tous les champs obligatoires.',
            waTitle: '🚨✨ *DÉCLARATION DE SINISTRE* — MutuellePro ✨🚨',
            waRef: '🆔 Réf. :', waBranche: '📁 Branche :', waPolice: '📋 N° de police :',
            waDate: '📅 Date du sinistre :', waDesc: '📝 Description :',
            waClient: '👤 *DÉCLARANT*', waPhone: '📞',
            waFooter: '✅ Merci d\'accuser réception dès que possible. Cette référence permet le suivi de votre dossier.',
            emailSubject: 'Déclaration de sinistre',
            emailIntro: 'Nouvelle déclaration de sinistre reçue via le site.',
            successMsg: (ref) => `Déclaration envoyée — référence ${ref}. Conservez ce numéro, il permettra le suivi de votre dossier.`,
            errorMsg: 'La déclaration WhatsApp a bien été préparée, mais l\'envoi de l\'e-mail a échoué. Votre référence reste valable : ',
        },
        en: {
            locale: 'en-US',
            champsManquants: 'Please fill in all required fields.',
            waTitle: '🚨✨ *CLAIM DECLARATION* — MutuellePro ✨🚨',
            waRef: '🆔 Ref.:', waBranche: '📁 Branch:', waPolice: '📋 Policy No.:',
            waDate: '📅 Date of incident:', waDesc: '📝 Description:',
            waClient: '👤 *DECLARANT*', waPhone: '📞',
            waFooter: '✅ Please confirm receipt as soon as possible. This reference allows tracking of your case.',
            emailSubject: 'Claim Declaration',
            emailIntro: 'New claim declaration received via the website.',
            successMsg: (ref) => `Declaration sent — reference ${ref}. Keep this number, it will allow tracking of your case.`,
            errorMsg: 'The WhatsApp declaration was prepared successfully, but sending the email failed. Your reference remains valid: ',
        },
    };

    async function submitSinistre() {
        const t = SINISTRE_I18N[currentLang] || SINISTRE_I18N.fr;

        const branche = document.getElementById('sinistre-branche');
        const brancheLabel = branche.options[branche.selectedIndex].text;
        const police = document.getElementById('sinistre-police').value.trim();
        const date = document.getElementById('sinistre-date').value;
        const description = document.getElementById('sinistre-description').value.trim();
        const nom = document.getElementById('sinistre-nom').value.trim();
        const prefix = document.getElementById('sinistre-phone-prefix').value;
        const phone = document.getElementById('sinistre-phone').value.trim();
        const email = document.getElementById('sinistre-email').value.trim();

        if (!date || !description || !nom || !phone || !email) {
            showToast(t.champsManquants, 'warning');
            return;
        }

        const ref = generateSinistreRef();
        const telephone = `${prefix}${phone}`;
        const now = new Date();
        const dateStr = now.toLocaleDateString(t.locale, { day: '2-digit', month: 'long', year: 'numeric' });

        // --- Message WhatsApp ---
        const lignesWa = [
            t.waTitle, '',
            `${t.waRef} ${ref}`,
            `${t.waBranche} ${brancheLabel}`,
        ];
        if (police) lignesWa.push(`${t.waPolice} ${police}`);
        lignesWa.push(
            `${t.waDate} ${date}`, '',
            t.waDesc, description, '',
            t.waClient,
            nom,
            `${t.waPhone} ${telephone}`,
            `📧 ${email}`,
            `🗂️ Situation du compte : ${situationCompteLigne(email)}`, '',
            t.waFooter
        );
        const messageWa = lignesWa.join('\n');

        // Même numéro WhatsApp central que le reste du site (section
        // Assistance) -- pas de champ "whatsapp" par agence dans
        // agencesData (seul "tel" existe), donc pas de routage par agence
        // ici, volontairement simple comme le reste de cette fiche.
        window.open(`https://wa.me/237697717334?text=${encodeURIComponent(messageWa)}`, '_blank');

        // Ticket backend (Helpdesk Volet 2, site.tickets) — en parallèle,
        // jamais bloquant (voir commentaire de creerTicketBackend).
        creerTicketBackend('sinistre', email, telephone, {
            branche: brancheLabel,
            police: police || undefined,
            date_sinistre: date,
            description,
            ref_memo: ref,
        });

        // --- E-mail (memo HTML, meme endpoint que le devis -- generique) ---
        const memoHtml = [
            `<strong>${t.emailIntro}</strong><br><br>`,
            `${t.waRef} <strong>${ref}</strong><br>`,
            `${t.waBranche} ${brancheLabel}<br>`,
            police ? `${t.waPolice} ${police}<br>` : '',
            `${t.waDate} ${date}<br><br>`,
            `${t.waDesc}<br>${description.replace(/\n/g, '<br>')}<br><br>`,
            `${t.waClient}<br>${nom}<br>${t.waPhone} ${telephone}<br>Email : ${email}<br>Situation du compte : ${situationCompteLigne(email)}`,
        ].join('');

        try {
            const formData = new FormData();
            formData.append('memo_complet', memoHtml);
            formData.append('subject', `${t.emailSubject} ${ref} — ${brancheLabel}`);
            const resp = await fetch('/api/send-quote-email', { method: 'POST', body: formData });
            const data = await resp.json();
            if (!data.success) throw new Error(data.error || 'Echec envoi email');
            showToast(t.successMsg(ref), 'success');
        } catch (err) {
            console.error('Erreur envoi email sinistre:', err);
            showToast(t.errorMsg + ref, 'warning');
        }

        closeSinistreModal();
        document.getElementById('sinistre-form').reset();
    }

    // Mémorise la branche pour laquelle l'étape 3 a été débloquée (passage
    // effectif par l'étape 2). Si l'utilisateur change de branche, l'accès
    // direct à l'étape 3 doit se reverrouiller : les champs "Détails du
    // Risque" affichés ne correspondraient plus à la nouvelle branche tant
    // qu'on n'y est pas repassé.
    let step3UnlockedForBranch = null;

    const BADGE_ACTIVE = "flex-1 text-center py-1.5 px-3 rounded-lg font-bold bg-brandRed text-white transition-all";
    const BADGE_INACTIVE = "flex-1 text-center py-1.5 px-3 rounded-lg font-bold bg-white/10 text-gray-300 transition-all";
    const BADGE_LOCKED = "flex-1 text-center py-1.5 px-3 rounded-lg font-bold bg-white/5 text-gray-500 transition-all cursor-not-allowed opacity-60";
    const BADGE_UNLOCKED_INACTIVE = "flex-1 text-center py-1.5 px-3 rounded-lg font-bold bg-white/10 text-gray-300 transition-all cursor-pointer hover:bg-white/20";

    function refreshStep3BadgeState() {
        const badge3 = document.getElementById('badgeStep3');
        if (!badge3) return;
        const insurance = document.getElementById('insurance_type').value;
        const unlocked = insurance && step3UnlockedForBranch === insurance;
        if (unlocked) {
            badge3.textContent = '3. Pièces & Envoi';
            if (badge3.className !== BADGE_ACTIVE) badge3.className = BADGE_UNLOCKED_INACTIVE;
        } else {
            badge3.innerHTML = '3. Pièces & Envoi <i class="fa-solid fa-lock text-[11px]"></i>';
            badge3.className = BADGE_LOCKED;
        }
    }

    // -----------------------------------------------------------------
    // PWA : enregistrement du service worker + invite d'installation.
    // Ajout pur — n'affecte aucune autre fonctionnalité du site. Le
    // bouton "Installer l'application" ne s'affiche que si le navigateur
    // propose réellement l'installation (Chrome/Edge/Android notamment ;
    // Safari iOS ne déclenche pas cet événement, le bouton y reste masqué).
    // -----------------------------------------------------------------
    let deferredInstallPrompt = null;

    if ('serviceWorker' in navigator) {
        window.addEventListener('load', () => {
            navigator.serviceWorker.register('sw.js').catch((err) => {
                console.warn('Service worker non enregistré :', err);
            });
        });
    }

    window.addEventListener('beforeinstallprompt', (event) => {
        event.preventDefault();
        deferredInstallPrompt = event;
        const btn = document.getElementById('pwa-install-btn');
        if (btn) btn.style.display = 'flex';
    });

    function installPWA() {
        if (!deferredInstallPrompt) return;
        deferredInstallPrompt.prompt();
        deferredInstallPrompt.userChoice.finally(() => {
            deferredInstallPrompt = null;
            const btn = document.getElementById('pwa-install-btn');
            if (btn) btn.style.display = 'none';
        });
    }

    // -----------------------------------------------------------------
    // ESCORTE DE LA BULLE WHATSAPP — sur la section "Assistant IA", la
    // bulle flottante quitte sa place habituelle pour rejoindre le coin
    // supérieur droit du bouton "En attendant, nous vous répondons !",
    // et l'accompagne tant qu'il reste visible à l'écran. Dès qu'il sort
    // de la page (ou si la place manque pour afficher la bulle en
    // entier), elle reprend sa position par défaut.
    // -----------------------------------------------------------------
    // -----------------------------------------------------------------
    // DIAPORAMA DU HERO — fondu enchaîné automatique entre les 6 images
    // de fond. Une seule image visible à la fois (opacity-100), les
    // autres à 0 — la transition CSS (2s) fait le fondu.
    // -----------------------------------------------------------------
    // -----------------------------------------------------------------
    // BANDEAU COOKIES — Google Analytics n'est chargé qu'après un
    // consentement explicite, jamais par défaut. Le choix (accepté ou
    // refusé) est mémorisé dans localStorage, pour ne plus jamais
    // redemander à un visiteur qui a déjà répondu.
    // -----------------------------------------------------------------
    const COOKIE_CONSENT_KEY = 'mpro_cookie_consent'; // 'accepted' | 'refused'

    function loadGoogleAnalytics() {
        if (document.getElementById('ga-script')) return; // déjà chargé, jamais deux fois
        const script = document.createElement('script');
        script.id = 'ga-script';
        script.async = true;
        script.src = 'https://www.googletagmanager.com/gtag/js?id=G-J2V65EGLTT';
        document.head.appendChild(script);

        window.dataLayer = window.dataLayer || [];
        function gtag(){ window.dataLayer.push(arguments); }
        gtag('js', new Date());
        gtag('config', 'G-J2V65EGLTT');
    }

    function handleCookieChoice(accepted) {
        localStorage.setItem(COOKIE_CONSENT_KEY, accepted ? 'accepted' : 'refused');
        document.getElementById('cookie-banner')?.classList.add('hidden');
        if (accepted) loadGoogleAnalytics();
    }

    function initCookieConsent() {
        const stored = localStorage.getItem(COOKIE_CONSENT_KEY);
        if (stored === 'accepted') {
            loadGoogleAnalytics();
        } else if (stored === 'refused') {
            // Rien à faire — choix déjà connu et respecté, bandeau reste caché.
        } else {
            // Jamais répondu : on affiche le bandeau.
            document.getElementById('cookie-banner')?.classList.remove('hidden');
        }
    }

    function setupHeroSlideshow() {
        const slides = document.querySelectorAll('#accueil .hero-slide');
        if (slides.length < 2) return; // rien à faire tourner

        const SLIDE_DURATION_MS = 5500; // ~5.5s par image, 6 images -> cycle de 33s
        let currentIndex = 0;

        setInterval(() => {
            slides[currentIndex].classList.remove('opacity-100');
            slides[currentIndex].classList.add('opacity-0');
            currentIndex = (currentIndex + 1) % slides.length;
            slides[currentIndex].classList.remove('opacity-0');
            slides[currentIndex].classList.add('opacity-100');
        }, SLIDE_DURATION_MS);
    }

    function setupWhatsAppEscort() {
        const bubble = document.getElementById('whatsapp-bubble');
        const cta = document.getElementById('assistant-whatsapp-cta');
        if (!bubble || !cta) return;

        const BUBBLE_SIZE = 56; // correspond à w-14/h-14 (3.5rem = 56px)
        const OVERLAP = BUBBLE_SIZE / 3; // seul le dernier tiers chevauche le bouton
        const CLEARANCE = BUBBLE_SIZE - OVERLAP; // 2/3 restants, à dégager au-dessus
        const BREATHING_ROOM = 8;
        let docked = false;

        function updateEscortPosition() {
            const rect = cta.getBoundingClientRect();
            const inView = rect.top < window.innerHeight && rect.bottom > 0;
            const enoughRoomAbove = rect.top - CLEARANCE >= 0;

            if (inView && enoughRoomAbove) {
                if (!docked) {
                    docked = true;
                    // Courbe très élastique : dépasse nettement sa position
                    // finale puis "rebondit" en arrière avant de se poser —
                    // simule une vraie plongée, pas un simple glissement.
                    bubble.style.transition = 'top 0.6s cubic-bezier(0.68, -0.6, 0.32, 1.6), left 0.6s cubic-bezier(0.68, -0.6, 0.32, 1.6)';
                    cta.style.transition = 'margin-top 0.5s ease';
                    // Ne libère que la portion NON chevauchante de la bulle
                    // (2/3 de sa hauteur) — le dernier tiers repose volontairement
                    // sur le bouton, pour l'effet de badge posé sur l'angle.
                    cta.style.marginTop = `${CLEARANCE + BREATHING_ROOM}px`;
                }
                // Position finale : seul le tiers inférieur/gauche de la bulle
                // chevauche le coin supérieur droit du bouton — les 2/3
                // restants débordent vers l'extérieur (haut/droite).
                bubble.style.bottom = 'auto';
                bubble.style.right = 'auto';
                bubble.style.top = `${rect.top - CLEARANCE}px`;
                bubble.style.left = `${rect.right - OVERLAP}px`;
            } else if (docked) {
                docked = false;
                bubble.style.top = '';
                bubble.style.left = '';
                bubble.style.bottom = '';
                bubble.style.right = '';
                cta.style.marginTop = '';
            }
        }

        window.addEventListener('scroll', updateEscortPosition, { passive: true });
        window.addEventListener('resize', updateEscortPosition);
        updateEscortPosition();
    }

    // -----------------------------------------------------------------
    // SIMULATEUR RAPIDE — ajout pur, aucune restructuration.
    // Volontairement honnête : aucune estimation de prix inventée, juste
    // une orientation immédiate qui débouche sur le vrai formulaire de
    // devis, avec la branche déjà présélectionnée pour gagner du temps.
    // Le texte de résultat est recalculé dans la langue active au moment
    // du clic (et non figé au chargement), pour rester bilingue.
    // -----------------------------------------------------------------
    let simBranch = null;

    const SIM_BRANCH_ICON = { Auto: 'fa-car', Sante: 'fa-heart-pulse', Habitation: 'fa-house', Voyage: 'fa-plane-departure', Rc: 'fa-shield-halved', MultirisquePro: 'fa-warehouse' };
    const SIM_BRANCH_I18N_KEY = { Auto: 'sim.branchAuto', Sante: 'sim.branchHealth', Habitation: 'sim.branchHome', Voyage: 'sim.branchTravel', Rc: 'sim.branchRc', MultirisquePro: 'sim.branchProMulti' };

    function simSelectBranch(value) {
        simBranch = value;
        document.getElementById('sim-step-1').classList.add('hidden');
        document.getElementById('sim-step-2').classList.remove('hidden');
    }

    function simSelectProfile(profile) {
        document.getElementById('sim-step-2').classList.add('hidden');
        document.getElementById('sim-step-3').classList.remove('hidden');

        const t = translations[currentLang] || translations.fr;
        const brancheTexte = t[SIM_BRANCH_I18N_KEY[simBranch]] || '';
        const iconeClasse = SIM_BRANCH_ICON[simBranch] || 'fa-shield-halved';

        const resultText = currentLang === 'en'
            ? `Perfect! For ${profile === 'particulier' ? 'an individual' : 'a business'} looking to insure: ${brancheTexte}, we recommend a personalized assessment with one of our advisors — it's quick, and there's no obligation.`
            : `Parfait ! Pour ${profile === 'particulier' ? 'un particulier' : 'un professionnel'} souhaitant assurer : ${brancheTexte}, nous vous recommandons une évaluation personnalisée avec l'un de nos conseillers — c'est rapide, et ça engage à rien.`;

        document.getElementById('sim-result-text').innerHTML = `<i class="fa-solid ${iconeClasse} text-brandPurple mr-1"></i> ${resultText}`;
    }

    function simGoToDevis() {
        openDevisModal();
        // Léger délai pour laisser la modale s'ouvrir avant de présélectionner la branche
        setTimeout(() => {
            const select = document.getElementById('insurance_type');
            if (select && simBranch) {
                select.value = simBranch;
                select.dispatchEvent(new Event('change'));
            }
        }, 150);
    }

    function simReset() {
        document.getElementById('sim-step-3').classList.add('hidden');
        document.getElementById('sim-step-2').classList.add('hidden');
        document.getElementById('sim-step-1').classList.remove('hidden');
        simBranch = null;
    }

    function tryGoToStep3() {
        const insurance = document.getElementById('insurance_type').value;
        if (!insurance || step3UnlockedForBranch !== insurance) {
            return; // verrouillé : passage obligé par l'étape 2 au moins une fois pour cette branche
        }
        goToStep3();
    }

    function goToStep1() {
        document.getElementById('wizardStep1').classList.remove('hidden');
        document.getElementById('wizardStep2').classList.add('hidden');
        document.getElementById('wizardStep3').classList.add('hidden');
        document.getElementById('stepTitle').textContent = "Étape 1 : Identification";
        document.getElementById('badgeStep1').className = BADGE_ACTIVE;
        document.getElementById('badgeStep2').className = BADGE_INACTIVE;
        refreshStep3BadgeState();
    }

    function goToStep2() {
        const name = document.getElementById('modal_user_name').value.trim();
        const phone = document.getElementById('modal_user_phone').value.trim();
        const insurance = document.getElementById('insurance_type').value;

        if (!name || !phone || !insurance) {
            showToast('Veuillez remplir tous les champs obligatoires (*) de l\'étape 1.', 'warning');
            return;
        }

        document.getElementById('wizardStep1').classList.add('hidden');
        document.getElementById('wizardStep2').classList.remove('hidden');
        document.getElementById('wizardStep3').classList.add('hidden');
        document.getElementById('stepTitle').textContent = "Étape 2 : Détails du Risque";
        document.getElementById('badgeStep1').className = BADGE_INACTIVE;
        document.getElementById('badgeStep2').className = BADGE_ACTIVE;
        refreshStep3BadgeState();

        document.querySelectorAll('.risk-section').forEach(sec => sec.classList.add('hidden'));
        const targetSection = document.getElementById(`fields-${insurance.toLowerCase()}`);
        if (targetSection) targetSection.classList.remove('hidden');
    }

    function goToStep3() {
        const insurance = document.getElementById('insurance_type').value;
        if (!insurance) {
            showToast('Veuillez sélectionner une branche d\'assurance à l\'étape 1.', 'warning');
            return;
        }

        // Débloque (et confirme) l'accès à l'étape 3 pour cette branche.
        step3UnlockedForBranch = insurance;

        document.getElementById('wizardStep1').classList.add('hidden');
        document.getElementById('wizardStep2').classList.add('hidden');
        document.getElementById('wizardStep3').classList.remove('hidden');
        document.getElementById('stepTitle').textContent = "Étape 3 : Pièces & Envoi";
        document.getElementById('badgeStep1').className = BADGE_INACTIVE;
        document.getElementById('badgeStep2').className = BADGE_INACTIVE;
        refreshStep3BadgeState();
        document.getElementById('badgeStep3').className = BADGE_ACTIVE;
        document.getElementById('badgeStep3').textContent = '3. Pièces & Envoi';
    }

    // -----------------------------------------------------------------
    // RECHERCHE VIN APPROXIMATIVE — déclenchée quand le décodage exact
    // échoue. État partagé entre les fonctions ci-dessous.
    // -----------------------------------------------------------------
    let vinSearchResults = [];
    let vinSearchPage = 0;
    const VIN_SEARCH_PAGE_SIZE = 5;

    async function vinSearchCascade(vinPartiel) {
        try {
            const response = await fetch(`/api/vin/cascade/${encodeURIComponent(vinPartiel)}`);
            const data = await response.json();

            if (!data.trouve || !data.resultats || data.resultats.length === 0) {
                showToast('Aucun véhicule approchant trouvé dans notre catalogue, même en recherche élargie.\n\nVous pouvez continuer en renseignant les caractéristiques manuellement.', 'warning');
                return;
            }

            vinSearchResults = data.resultats;
            vinSearchPage = 0;

            const agrege = data.agrege || {};
            const summary = document.getElementById('vin-search-summary');
            if (summary) {
                const suggestion = [agrege.marque, agrege.modele].filter(Boolean).join(' ');
                summary.textContent = `${data.nb_references} véhicule(s) trouvé(s) (préfixe "${data.prefixe}", ${data.niveau_trouve} 1ers caractères)` +
                    (suggestion ? ` — Le plus probable : ${suggestion} (${agrege.marque_confiance || 0}% sur la marque)` : '') +
                    ' — choisissez ci-dessous, ou indiquez qu\'aucun ne correspond :';
            }

            vinSearchRenderPage();
            document.getElementById('vin-search-panel')?.classList.remove('hidden');
        } catch (err) {
            console.error('Erreur recherche cascade VIN:', err);
            showToast('Erreur lors de la recherche dans le catalogue. Réessayez, ou renseignez les caractéristiques manuellement.', 'error');
        }
    }

    function vinSearchRenderPage() {
        const container = document.getElementById('vin-search-results');
        if (!container) return;

        const start = vinSearchPage * VIN_SEARCH_PAGE_SIZE;
        const pageItems = vinSearchResults.slice(start, start + VIN_SEARCH_PAGE_SIZE);

        container.innerHTML = pageItems.map((item, i) => {
            const idx = start + i;
            const hasConfiance = item.confiance_pct !== undefined && item.confiance_pct !== null;
            const estMeilleur = hasConfiance && item.confiance_pct >= 95;
            const badge = hasConfiance
                ? `<span class="ml-1 text-[10px] font-bold ${estMeilleur ? 'text-emerald-600' : 'text-gray-400'}">${item.confiance_pct}%</span>`
                : '';
            const styleCarte = estMeilleur
                ? 'border-emerald-400 bg-emerald-50 hover:border-emerald-500'
                : 'border-gray-300 bg-white hover:border-purple-500 hover:bg-purple-50';
            return `<button type="button" onclick="vinSearchSelect(${idx})" class="w-full flex items-center justify-between gap-2 text-left text-xs border-2 ${styleCarte} rounded-lg px-3 py-2.5 transition shadow-sm">
                <span>
                    <strong>${item.marque || '?'} ${item.modele || ''}</strong>${badge}<br>
                    <span class="text-gray-500 text-[10px]">${item.energie || '-'} · ${item.puissance || '-'} · VIN: ${item.vin}</span>
                </span>
                <span class="text-navy text-base shrink-0">›</span>
            </button>`;
        }).join('');

        const totalPages = Math.max(1, Math.ceil(vinSearchResults.length / VIN_SEARCH_PAGE_SIZE));
        const indicator = document.getElementById('vin-search-page-indicator');
        if (indicator) indicator.textContent = `Page ${vinSearchPage + 1} / ${totalPages}`;

        const prevBtn = document.getElementById('vin-search-prev');
        const nextBtn = document.getElementById('vin-search-next');
        if (prevBtn) prevBtn.disabled = vinSearchPage === 0;
        if (nextBtn) nextBtn.disabled = (start + VIN_SEARCH_PAGE_SIZE) >= vinSearchResults.length;
    }

    function vinSearchChangePage(delta) {
        vinSearchPage += delta;
        vinSearchRenderPage();
    }

    function vinSearchSelect(idx) {
        const item = vinSearchResults[idx];
        if (!item) return;

        const vinField = document.getElementById('auto_vin');
        if (vinField && item.vin) vinField.value = item.vin;

        const marqueField = document.getElementById('auto_marque');
        if (marqueField && item.marque) marqueField.value = item.marque;

        const modeleField = document.getElementById('auto_modele');
        if (modeleField && item.modele) modeleField.value = item.modele;

        const energyField = document.getElementById('auto_energy');
        if (energyField && item.energie) {
            const optionExists = Array.from(energyField.options).some((o) => o.value === item.energie);
            energyField.value = optionExists ? item.energie : 'Autre / Non précisé';
        }

        if (item.puissance) {
            const match = String(item.puissance).match(/[\d.]+/);
            const powerValueField = document.getElementById('auto_power_value');
            const powerUnitField = document.getElementById('auto_power_unit');
            if (match && powerValueField) {
                powerValueField.value = match[0];
                if (powerUnitField) powerUnitField.value = 'CV';
            }
        }

        document.getElementById('vin-search-panel')?.classList.add('hidden');
        saveFormDraft();
    }

    function vinSearchRejectAll() {
        document.getElementById('vin-search-panel')?.classList.add('hidden');
        vinSearchResults = [];
    }

    // -----------------------------------------------------------------
    // NOTIFICATIONS TOAST — remplace alert(). Empilables, se referment
    // seules après quelques secondes ou au clic.
    // -----------------------------------------------------------------
    const TOAST_STYLES = {
        info:    { icon: 'fa-circle-info',        bg: 'bg-navy',      },
        success: { icon: 'fa-circle-check',       bg: 'bg-emerald-600' },
        warning: { icon: 'fa-triangle-exclamation', bg: 'bg-amber-500' },
        error:   { icon: 'fa-circle-exclamation', bg: 'bg-red-600'    },
    };

    function showToast(message, type = 'info', duration = 5000) {
        const container = document.getElementById('toast-container');
        if (!container) { console.warn('Toast (conteneur absent) :', message); return; }

        const style = TOAST_STYLES[type] || TOAST_STYLES.info;
        const toast = document.createElement('div');
        toast.className = `${style.bg} text-white text-xs rounded-xl shadow-lg px-4 py-3 flex items-start gap-2.5 opacity-0 translate-x-4 transition-all duration-300`;
        toast.innerHTML = `
            <i class="fa-solid ${style.icon} mt-0.5 shrink-0"></i>
            <span class="flex-1 leading-relaxed whitespace-pre-line">${message}</span>
            <button type="button" aria-label="Fermer la notification" class="shrink-0 opacity-70 hover:opacity-100">
                <i class="fa-solid fa-xmark"></i>
            </button>
        `;
        container.appendChild(toast);

        // Anime l'entrée
        requestAnimationFrame(() => {
            toast.classList.remove('opacity-0', 'translate-x-4');
        });

        const dismiss = () => {
            toast.classList.add('opacity-0', 'translate-x-4');
            setTimeout(() => toast.remove(), 300);
        };
        toast.querySelector('button').addEventListener('click', dismiss);
        if (duration > 0) setTimeout(dismiss, duration);
    }

    // -----------------------------------------------------------------
    // BOÎTE DE DIALOGUE DE CONFIRMATION — remplace confirm(). Retourne
    // une Promise<boolean>, à utiliser avec await, ex:
    //   const ok = await showConfirmDialog('Voulez-vous continuer ?');
    // -----------------------------------------------------------------
    function showConfirmDialog(message) {
        return new Promise((resolve) => {
            const overlay = document.getElementById('confirm-dialog-overlay');
            const messageEl = document.getElementById('confirm-dialog-message');
            const btnOk = document.getElementById('confirm-dialog-ok');
            const btnCancel = document.getElementById('confirm-dialog-cancel');

            messageEl.textContent = message;
            overlay.classList.remove('hidden');

            const cleanup = (result) => {
                overlay.classList.add('hidden');
                btnOk.removeEventListener('click', onOk);
                btnCancel.removeEventListener('click', onCancel);
                resolve(result);
            };
            const onOk = () => cleanup(true);
            const onCancel = () => cleanup(false);

            btnOk.addEventListener('click', onOk);
            btnCancel.addEventListener('click', onCancel);
        });
    }

    async function lookupVIN(evt) {
        const vinInput = document.getElementById('auto_vin');
        const vin = vinInput ? vinInput.value.trim().toUpperCase() : '';
        const btn = evt ? evt.target : event.target;

        if (!vin) {
            showToast(currentLang === 'en' ? 'Please enter a VIN / Chassis Number' : 'Veuillez renseigner un numéro de châssis', 'warning');
            return;
        }

        const originalLabel = btn.textContent;
        btn.disabled = true;
        btn.textContent = '...';

        try {
            const response = await fetch(`/api/vin/${vin}`, {
                headers: { 'ngrok-skip-browser-warning': 'true' }
            });
            const data = await response.json();

            if (!data.valid) {
                // Le champ Notes n'est PAS modifié automatiquement ici. Si des
                // informations partielles existent, c'est à l'utilisateur de
                // décider s'il les prend (via confirmation explicite) ou non.
                const partial = data.partial_data;
                const hasPartial = partial && (partial.make || partial.model || partial.model_year);

                if (hasPartial) {
                    const summary = [partial.make, partial.model, partial.model_year].filter(Boolean).join(' / ');
                    const takeIt = await showConfirmDialog(
                        `VIN non validé : ${data.message || data.error || 'erreur inconnue'}\n\n` +
                        `Informations partielles trouvées (non fiables, à vérifier) : ${summary}\n\n` +
                        `Voulez-vous les ajouter aux Notes malgré tout, pour vérification manuelle ?`
                    );
                    if (takeIt) {
                        const notesField = document.getElementById('modal_message');
                        if (notesField) {
                            const vinBlock = `[VIN] VIN: ${vin}; Marque: ${partial.make || '-'}; Modèle: ${partial.model || '-'}; Année: ${partial.model_year || '-'}; Statut: PARTIEL / NON VALIDÉ — à vérifier manuellement [/VIN]`;
                            const blockRegexNew = /\[VIN\][\s\S]*?\[\/VIN\]\n?/g;
                            const blockRegexOld = /\[DONNÉES VIN[\s\S]*?\[FIN DONNÉES VIN\]\n?/g;
                            const freeText = notesField.value
                                .replace(blockRegexNew, '')
                                .replace(blockRegexOld, '')
                                .trim();
                            notesField.value = freeText ? `${vinBlock}\n\n${freeText}` : vinBlock;
                            saveFormDraft();
                        }
                    }
                } else {
                    // Ce message de format invalide (I/O/Q, mauvaise longueur...) est
                    // volontairement conservé pour pousser à la vigilance — mais on
                    // propose maintenant une porte de sortie : chercher quand même
                    // dans notre catalogue camerounais, plutôt que de bloquer sec.
                    const chercherQuandMeme = await showConfirmDialog(
                        'VIN invalide ou non reconnu : ' + (data.message || data.error || 'erreur inconnue') +
                        '\n\n(Base centrée sur le marché nord-américain — les véhicules d\'origine européenne/chinoise sont parfois mal reconnus. Vérifiez bien votre saisie.)' +
                        '\n\nVoulez-vous que nous recherchions quand même des véhicules approchants dans notre catalogue local ?'
                    );
                    if (chercherQuandMeme) {
                        await vinSearchCascade(vin);
                    }
                }
                return;
            }

            // Remplissage automatique des champs disponibles
            const marqueField = document.getElementById('auto_marque');
            if (marqueField && data.make) marqueField.value = data.make;

            const modeleField = document.getElementById('auto_modele');
            if (modeleField && data.model) modeleField.value = data.model;

            const fuelFr = translateFuelType(data.engine && data.engine.fuel_type) ||
                (data.catalogue && data.catalogue.energie) || ''; // repli catalogue : déjà en français, aucune traduction nécessaire
            const energyField = document.getElementById('auto_energy');
            if (energyField && fuelFr) {
                const optionExists = Array.from(energyField.options).some((o) => o.value === fuelFr);
                energyField.value = optionExists ? fuelFr : 'Autre / Non précisé';
            }

            const valueField = document.getElementById('auto_value') || document.querySelector('[name="auto_value"]');
            if (valueField) {
                valueField.placeholder = `${data.make} ${data.model} ${data.model_year}${data.trim ? ' - ' + data.trim : ''}`;
            }

            // Puissance (CV fiscaux) — priorité à la vraie valeur du catalogue
            // Cameroun (exacte, pas une estimation) ; à défaut, on retombe sur
            // l'ancienne estimation à partir de la puissance HP (vPIC, base US).
            // Toujours en CV (fiscaux) dans les deux cas — l'unité est donc
            // fixée explicitement, seule la valeur numérique change.
            const powerValueField = document.getElementById('auto_power_value');
            const powerUnitField = document.getElementById('auto_power_unit');
            if (powerValueField && data.catalogue && data.catalogue.puissance_fiscale_cv) {
                // La valeur peut arriver sous forme "11 CV" (texte) — on extrait
                // juste le nombre pour le champ numérique.
                const numericMatch = String(data.catalogue.puissance_fiscale_cv).match(/[\d.]+/);
                if (numericMatch) {
                    powerValueField.value = numericMatch[0];
                    if (powerUnitField) powerUnitField.value = 'CV';
                }
            } else if (powerValueField && data.engine && data.engine.horsepower_from) {
                const estimatedCV = hpToFiscalCV(data.engine.horsepower_from, data.make, data.model, data.model_year);
                if (estimatedCV) {
                    powerValueField.value = estimatedCV;
                    if (powerUnitField) powerUnitField.value = 'CV';
                }
            }

            // Ajout des informations extraites dans "Notes / Précisions complémentaires"
            // sans écraser le texte libre déjà saisi, et sans créer de doublon si l'on
            // clique plusieurs fois ou si l'on change de VIN.
            // Format : [VIN] champ: valeur; champ: valeur; ... [/VIN] — une seule ligne,
            // séparateurs ";", pour rester robuste en cas de réutilisation (CRM, email).
            const notesField = document.getElementById('modal_message');
            if (notesField) {
                const gvwrNote = data.weight_class
                    ? `${data.weight_class.gvwr_from || '?'} à ${data.weight_class.gvwr_to || '?'} (plage réglementaire US, pas un poids exact)`
                    : 'non disponible';
                const dispL = data.engine && data.engine.displacement_l ? parseFloat(data.engine.displacement_l).toFixed(1) + 'L' : '-';
                const transText = data.transmission ? `${data.transmission.style || '-'} ${data.transmission.speeds ? '- ' + data.transmission.speeds + ' vitesses' : ''}`.trim() : '-';
                const plantText = data.plant ? [data.plant.city, data.plant.state, data.plant.country].filter(Boolean).join(', ') : '-';

                const vinBlock = `[VIN] VIN: ${data.vin}; Marque: ${data.make || '-'}; Modèle: ${data.model || '-'}; Année: ${data.model_year || '-'}${data.trim ? '; Version: ' + data.trim : ''}; Type: ${data.vehicle_type || '-'}; Carrosserie: ${data.body_class || '-'}; Portes: ${data.doors || '?'}; Moteur: ${data.engine && data.engine.cylinders ? data.engine.cylinders + ' cyl.' : '-'} ${dispL}; Carburant: ${fuelFr || '-'}; Transmission: ${transText}; Poids (GVWR): ${gvwrNote}; Usine: ${plantText} [/VIN]`;

                // On retire tout bloc VIN précédent (ancien ou nouveau format) avant
                // d'insérer le nouveau, pour ne jamais créer de doublon.
                const blockRegexNew = /\[VIN\][\s\S]*?\[\/VIN\]\n?/g;
                const blockRegexOld = /\[DONNÉES VIN[\s\S]*?\[FIN DONNÉES VIN\]\n?/g;
                const freeText = notesField.value
                    .replace(blockRegexNew, '')
                    .replace(blockRegexOld, '')
                    .trim();

                notesField.value = freeText
                    ? `${vinBlock}\n\n${freeText}`
                    : vinBlock;

                saveFormDraft(); // on met à jour la sauvegarde immédiatement
            }

            // Message de confirmation robuste : n'affiche jamais "null" pour un
            // champ manquant (fréquent quand la donnée vient du catalogue
            // Cameroun, qui ne renseigne pas body_class/doors) — on omet
            // simplement la partie concernée plutôt que de l'afficher vide.
            const summaryParts = [];
            if (data.body_class) summaryParts.push(data.body_class);
            if (data.doors) summaryParts.push(`${data.doors} portes`);
            if (fuelFr) summaryParts.push(fuelFr);
            const summaryText = summaryParts.length ? ` — ${summaryParts.join(', ')}` : '';

            // Transparence sur l'origine des données, utile depuis la fusion
            // du catalogue Cameroun (rassure l'agent sur la fiabilité).
            const sourceNote = data.source === 'catalogue' ? ' [source : catalogue Cameroun]'
                : data.source === 'vpic+catalogue' ? ' [vPIC + catalogue Cameroun]'
                : '';

            showToast(`VIN décodé : ${data.make} ${data.model} (${data.model_year})${summaryText}${sourceNote}`, 'success');
        } catch (err) {
            console.error(err);
            showToast("Impossible de contacter le service de décodage VIN. Vérifiez que l'API est bien démarrée (PM2).", 'error');
        } finally {
            btn.disabled = false;
            btn.textContent = originalLabel;
        }
    }

    // -----------------------------------------------------------------
    // Traduction des types de carburant (vPIC, anglais US) → français local
    // "Gasoline" (anglais US) = Essence — jamais du Diesel/Gasoil.
    // "Diesel" est ici explicité "Diesel (Gasoil)" car chez nous les deux
    // termes sont souvent utilisés de façon interchangeable.
    // -----------------------------------------------------------------
    const FUEL_TYPE_FR = {
        'Gasoline': 'Essence',
        'Diesel': 'Diesel (Gasoil)',
        'Flexible Fuel Vehicle (FFV)': 'Flex-fuel (Essence/Éthanol)',
        'Electric': 'Électrique',
        'Compressed Natural Gas (CNG)': 'GNC (Gaz Naturel Comprimé)',
        'Liquefied Petroleum Gas (propane or LPG)': 'GPL (Gaz de Pétrole Liquéfié)',
        'Hybrid': 'Hybride (Essence-Électrique)',
        'Ethanol (E85)': 'Éthanol (E85)',
        'Hydrogen': 'Hydrogène',
        'Not Applicable': '',
    };

    function translateFuelType(vpicValue) {
        if (!vpicValue) return '';
        return FUEL_TYPE_FR[vpicValue] || ''; // valeur inconnue → laissée vide, jamais devinée
    }

    // -----------------------------------------------------------------
    // Suivi des pièces justificatives sélectionnées par l'utilisateur.
    // Les fichiers eux-mêmes ne sont PAS envoyés automatiquement à
    // l'agence (WhatsApp ne le permet pas via un simple lien wa.me) :
    // on se contente de lister leurs noms, pour rappel à l'utilisateur
    // et pour le mémo archivé.
    // -----------------------------------------------------------------
    // -----------------------------------------------------------------
    // Compression des images côté client (canvas natif, aucune dépendance).
    // Indispensable : une photo de pièce prise au téléphone peut peser
    // plusieurs Mo — inexploitable pour un envoi (WhatsApp, email, mémo
    // archivé). Redimensionne à 1600px max de côté et recompresse en JPEG
    // qualité 75%. Les PDF ne sont pas concernés (canvas ne s'applique pas
    // aux documents, seulement aux images) et restent inchangés.
    // -----------------------------------------------------------------
    function compressImageFile(file, maxDim = 1600, quality = 0.75) {
        if (!file.type.startsWith('image/')) {
            return Promise.resolve(file); // PDF ou autre : inchangé
        }
        return new Promise((resolve) => {
            const objectUrl = URL.createObjectURL(file);
            const img = new Image();

            img.onload = () => {
                let { width, height } = img;
                if (width > maxDim || height > maxDim) {
                    const scale = Math.min(maxDim / width, maxDim / height);
                    width = Math.round(width * scale);
                    height = Math.round(height * scale);
                }

                const canvas = document.createElement('canvas');
                canvas.width = width;
                canvas.height = height;
                const ctx = canvas.getContext('2d');
                ctx.drawImage(img, 0, 0, width, height);
                URL.revokeObjectURL(objectUrl);

                canvas.toBlob((blob) => {
                    if (!blob) { resolve(file); return; } // échec de compression → on garde l'original
                    const compressed = new File(
                        [blob],
                        file.name.replace(/\.\w+$/, '.jpg'),
                        { type: 'image/jpeg', lastModified: Date.now() }
                    );
                    // Ne garder la version compressée que si elle est vraiment plus légère
                    resolve(compressed.size < file.size ? compressed : file);
                }, 'image/jpeg', quality);
            };

            img.onerror = () => {
                URL.revokeObjectURL(objectUrl);
                resolve(file); // format non supporté par le navigateur → fichier original conservé
            };

            img.src = objectUrl;
        });
    }

    // Limites RÉELLES depuis la migration vers Nodemailer (serveur SMTP LWS) :
    // - MAX_FILE_SIZE_MB : plafond dur par fichier, doit correspondre exactement
    //   à la limite "multer" configurée côté serveur (voir server.js) — sinon
    //   un fichier accepté ici serait rejeté silencieusement à l'envoi.
    // - TOTAL_SIZE_WARNING_MB : simple avertissement (pas un blocage), les
    //   serveurs SMTP plafonnant souvent la taille totale d'un message autour
    //   de 20-25 Mo, tous fichiers confondus.
    const MAX_FILE_SIZE_MB = 15;
    const TOTAL_SIZE_WARNING_MB = 20;

    function getTotalDocsSizeMB() {
        let totalBytes = 0;
        document.querySelectorAll('.doc-upload').forEach((input) => {
            if (input.files && input.files.length > 0) totalBytes += input.files[0].size;
        });
        return totalBytes / 1024 / 1024;
    }

    // Vérification proactive avant tout envoi d'email : prévient l'échec
    // plutôt que de laisser l'utilisateur découvrir un message d'erreur
    // technique après coup. Laisse le choix de continuer quand même (le
    // reste du message part, seules les pièces jointes échoueront) ou
    // d'annuler pour retirer/réduire des pièces d'abord.
    async function warnIfAttachmentsTooLarge() {
        const totalMB = getTotalDocsSizeMB();
        if (totalMB <= TOTAL_SIZE_WARNING_MB) return true;
        return await showConfirmDialog(
            `Vos pièces jointes pèsent environ ${totalMB.toFixed(1)} Mo au total, au-dessus du seuil recommandé de ${TOTAL_SIZE_WARNING_MB} Mo — certains serveurs de messagerie peuvent refuser un message aussi volumineux.\n\n` +
            `Le mémo WhatsApp n'est pas concerné par cette limite.\n\n` +
            `Continuer quand même ?`
        );
    }

    // Traduit les erreurs techniques d'envoi email en messages compréhensibles,
    // avec la vraie cause quand elle est connue (ex. taille des pièces).
    function explainEmailError(err) {
        if (err && err.status === 413) {
            return `Une pièce jointe dépasse la limite de ${MAX_FILE_SIZE_MB} Mo autorisée par notre serveur. Votre demande a néanmoins été transmise par WhatsApp — pensez à y joindre vos pièces manuellement.`;
        }
        if (err && err.status === 400) {
            return "Échec de l'envoi de l'email : configuration du service de messagerie incorrecte. Merci de nous contacter directement.";
        }
        if (err && err.status === 0) {
            return "Échec de l'envoi de l'email : impossible de joindre le service (vérifiez votre connexion internet). Veuillez réessayer.";
        }
        return "Échec de l'envoi de l'email (le mémo WhatsApp a néanmoins été préparé). Veuillez réessayer ou nous contacter directement.";
    }

    // -----------------------------------------------------------------
    // Envoi réel du devis par email — via NOTRE serveur (Nodemailer/SMTP
    // LWS), plus EmailJS. FormData(form) capture automatiquement tous les
    // champs nommés du formulaire, fichiers inclus (même principe que
    // emailjs.sendForm, mais vers notre propre backend, sans plafond de
    // 50 Ko ni dépendance à un service tiers).
    // -----------------------------------------------------------------
    // Construit le contenu HTML du message de contact (Nom/Email/Téléphone/
    // Ville/Sujet/Message), placé dans le champ caché "memo_complet" juste
    // avant l'envoi — même mécanisme que le formulaire de devis, pour
    // réutiliser tel quel l'endpoint /api/send-quote-email (Nodemailer).
    function buildContactMemoText(form) {
        const get = (name) => {
            const el = form.elements[name];
            return el ? (el.value || '').trim() : '';
        };
        const prefixSelect = form.querySelector('.devis-prefix-select');
        const prefix = prefixSelect ? prefixSelect.value : '';

        // Le libellé lisible du sujet (pas juste le numéro), pour un mémo
        // clair côté destinataire.
        const sujetSelect = form.elements['sujet_categorie'];
        const sujetLabel = sujetSelect && sujetSelect.selectedIndex > 0
            ? sujetSelect.options[sujetSelect.selectedIndex].textContent
            : '(non précisé)';

        const lines = [
            'NOUVEAU MESSAGE — Formulaire de contact du site', '',
            `Nom : ${get('user_name')}`,
            `Email : ${get('user_email')}`,
            `Téléphone : ${prefix} ${get('user_phone')}`,
            `Ville : ${get('user_city')}`,
            `Objet : ${get('subject')}`,
            `Sujet : ${sujetLabel}`,
            `Situation du compte : ${situationCompteLigne(get('user_email'))}`, '',
            '--- MESSAGE ---',
            get('message'),
        ];
        return lines.join('<br>');
    }

    // -----------------------------------------------------------------
    // TABLE DE ROUTAGE — formulaire de contact, par sujet précis. Chaque
    // entrée : destinataire principal (to) + copie(s) (cc). Construite
    // avec Roger le 04/08/2026, sur la base des boîtes email réellement
    // créées (contact@, devis@, direction@, production@, sinistres@,
    // comptablite@).
    // -----------------------------------------------------------------
    const CONTACT_ROUTING = {
        '1':  { to: 'devis@mutuelleproassurances.com',       cc: ['direction@mutuelleproassurances.com'] },
        '2':  { to: 'production@mutuelleproassurances.com',  cc: ['direction@mutuelleproassurances.com'] },
        '3':  { to: 'contact@mutuelleproassurances.com',     cc: ['direction@mutuelleproassurances.com', 'production@mutuelleproassurances.com'] },
        '4':  { to: 'sinistres@mutuelleproassurances.com',   cc: ['direction@mutuelleproassurances.com', 'production@mutuelleproassurances.com'] },
        '5':  { to: 'sinistres@mutuelleproassurances.com',   cc: ['direction@mutuelleproassurances.com'] },
        '6':  { to: 'sinistres@mutuelleproassurances.com',   cc: ['direction@mutuelleproassurances.com'] },
        '7':  { to: 'production@mutuelleproassurances.com',  cc: ['direction@mutuelleproassurances.com'] },
        '8':  { to: 'production@mutuelleproassurances.com',  cc: ['direction@mutuelleproassurances.com'] },
        '9':  { to: 'production@mutuelleproassurances.com',  cc: ['direction@mutuelleproassurances.com'] },
        '10': { to: 'production@mutuelleproassurances.com',  cc: ['direction@mutuelleproassurances.com'] },
        '11': { to: 'production@mutuelleproassurances.com',  cc: ['direction@mutuelleproassurances.com'] },
        '12': { to: 'production@mutuelleproassurances.com',  cc: ['comptablite@mutuelleproassurances.com'] },
        '13': { to: 'production@mutuelleproassurances.com',  cc: ['comptablite@mutuelleproassurances.com'] },
        '14': { to: 'direction@mutuelleproassurances.com',   cc: ['contact@mutuelleproassurances.com'] },
        '15': { to: 'direction@mutuelleproassurances.com',   cc: ['contact@mutuelleproassurances.com'] },
        '16': { to: 'direction@mutuelleproassurances.com',   cc: ['contact@mutuelleproassurances.com'] },
        '17': { to: 'direction@mutuelleproassurances.com',   cc: ['contact@mutuelleproassurances.com'] },
        '18': { to: 'direction@mutuelleproassurances.com',   cc: ['contact@mutuelleproassurances.com'] },
        '19': { to: 'direction@mutuelleproassurances.com',   cc: ['contact@mutuelleproassurances.com'] },
        '20': { to: 'direction@mutuelleproassurances.com',   cc: ['contact@mutuelleproassurances.com'] },
        '21': { to: 'direction@mutuelleproassurances.com',   cc: ['contact@mutuelleproassurances.com'] },
        '22': { to: 'direction@mutuelleproassurances.com',   cc: ['contact@mutuelleproassurances.com'] },
    };
    const CONTACT_ROUTING_FALLBACK = { to: 'contact@mutuelleproassurances.com', cc: [] };

    async function sendQuoteEmail(form, cc, recipientOverride) {
        const formData = new FormData(form);
        const ccString = Array.isArray(cc) ? cc.filter(Boolean).join(',') : cc;
        if (ccString) formData.append('cc', ccString);
        if (recipientOverride) formData.append('recipient', recipientOverride);

        const response = await fetch('/api/send-quote-email', {
            method: 'POST',
            body: formData,
        });

        if (!response.ok) {
            const err = new Error('Échec de l\'envoi de l\'email.');
            err.status = response.status;
            try {
                const body = await response.json();
                if (body && body.error) err.message = body.error;
            } catch (e) { /* réponse non-JSON, on garde le message par défaut */ }
            throw err;
        }
        return response.json();
    }

    // -----------------------------------------------------------------
    // Création du ticket backend (site.tickets, Helpdesk Volet 2) — en
    // PARALLÈLE du flux WhatsApp/email existant, jamais à sa place. Un
    // échec ici ne doit JAMAIS bloquer ni retarder l'envoi WhatsApp/email,
    // qui reste le canal principal déjà éprouvé : on avale l'erreur en
    // silence (log console uniquement), le ticket "Mon espace" est un
    // bénéfice additionnel, pas une dépendance critique du parcours.
    // -----------------------------------------------------------------
    async function creerTicketBackend(type, email, telephone, contenu) {
        try {
            const resp = await fetch('/api/tickets', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ type, email, telephone, contenu }),
            });
            const data = await resp.json();
            if (!data.succes) {
                console.warn('Ticket backend non créé :', data.erreurs);
                return null;
            }
            return data;
        } catch (err) {
            console.warn('Ticket backend non créé (erreur réseau) :', err);
            return null;
        }
    }

    // -----------------------------------------------------------------
    // Pré-vérification de l'existence d'un compte pour un email/téléphone
    // donné — alimente la ligne "Situation du compte" du mémo/WhatsApp/
    // email. Volontairement déclenchée AVANT la soumission (au blur du
    // champ email), car au moment du clic sur "Envoyer", l'ouverture de
    // WhatsApp doit rester synchrone (sinon le navigateur bloque le
    // popup) — impossible d'attendre une réponse réseau à ce moment-là.
    // Résultat mis en cache ; si jamais non résolu au moment de l'envoi
    // (cas limite, ex. soumission très rapide), on retombe sur un texte
    // neutre plutôt que de bloquer quoi que ce soit.
    // -----------------------------------------------------------------
    const compteExistantCache = {};
    function preCheckCompteExistant(identifiant) {
        if (!identifiant || compteExistantCache[identifiant] !== undefined) return;
        compteExistantCache[identifiant] = null; // marque "en cours" pour éviter les doublons
        fetch(`/api/verifier-compte?identifiant=${encodeURIComponent(identifiant)}`)
            .then((r) => r.json())
            .then((data) => {
                compteExistantCache[identifiant] = data.succes ? data.existe : null;
            })
            .catch(() => {
                compteExistantCache[identifiant] = null;
            });
    }

    // Ligne "Situation du compte" combinée (existence du compte + statut
    // initial du ticket, toujours "Reçu" à ce stade puisque le ticket
    // vient d'être créé — voir site.statut_ticket, id=1 par défaut).
    function situationCompteLigne(identifiant) {
        const t = translations[currentLang] || translations.fr;
        const existe = compteExistantCache[identifiant];
        const compteLabel = existe === true ? t['espace.statutClientInscrit']
            : existe === false ? t['espace.statutNouveauContact']
            : t['espace.statutAVerifier'];
        return `${compteLabel} — ${t['espace.statutRecu']}`;
    }

    async function handleDocUploadChange(evt) {
        const input = evt.target;
        if (!input.files || input.files.length === 0) return;

        // Sélection groupée recto+verso : si ce champ est un "recto" relié à
        // un champ "verso" (data-pair-verso) et que l'utilisateur a
        // sélectionné exactement 2 fichiers d'un coup, le 1er reste ici, le
        // 2nd est transféré automatiquement vers le champ verso associé.
        if (input.files.length === 2 && input.dataset.pairVerso) {
            const versoInput = document.querySelector(`[data-doc-key="${input.dataset.pairVerso}"]`);
            if (versoInput) {
                const secondFile = input.files[1];
                const dtRecto = new DataTransfer();
                dtRecto.items.add(input.files[0]);
                input.files = dtRecto.files;

                const dtVerso = new DataTransfer();
                dtVerso.items.add(secondFile);
                versoInput.files = dtVerso.files;
                // Déclenche le traitement (compression, affichage) du verso
                // exactement comme s'il avait été choisi séparément.
                versoInput.dispatchEvent(new Event('change'));
            }
        } else if (input.files.length > 2) {
            showToast(`"${input.dataset.docLabel}" : vous avez sélectionné ${input.files.length} fichiers, mais seuls 2 maximum sont acceptés ici (recto + verso). Seul le premier sera conservé.`, 'warning');
            const dt = new DataTransfer();
            dt.items.add(input.files[0]);
            input.files = dt.files;
        }

        const original = input.files[0];
        const summary = document.getElementById('doc-summary');
        if (summary) {
            summary.classList.remove('hidden');
            summary.textContent = `⏳ Compression de "${input.dataset.docLabel}" en cours...`;
        }

        const compressed = await compressImageFile(original);
        // input → bouton déclencheur → fileRow (dans cet ordre exact,
        // voir initDocClearButtons) — on saute donc un cran de plus
        // qu'avant pour atteindre la ligne 2.
        const fileRow = input.nextElementSibling ? input.nextElementSibling.nextElementSibling : null;

        // Rejet si, même après compression, le fichier dépasse la limite
        // réelle du serveur (15 Mo, voir MAX_FILE_SIZE_MB) — évite un échec
        // silencieux au moment de l'envoi, bien plus tard dans le parcours.
        if (compressed.size > MAX_FILE_SIZE_MB * 1024 * 1024) {
            showToast(`"${input.dataset.docLabel}" est trop volumineux (${(compressed.size / 1024 / 1024).toFixed(1)} Mo, même après compression) — la limite est de ${MAX_FILE_SIZE_MB} Mo par pièce. Choisissez un fichier plus léger, ou une photo plutôt qu'un scan haute résolution.`, 'error');
            input.value = '';
            if (fileRow) fileRow.classList.add('hidden');
            updateDocSummary();
            return;
        }

        if (compressed !== original) {
            // Remplace le fichier sélectionné par la version compressée
            // (un <input type="file"> ne peut être réassigné directement :
            // on passe par un DataTransfer, seule méthode standard).
            const dt = new DataTransfer();
            dt.items.add(compressed);
            input.files = dt.files;

            const beforeKB = (original.size / 1024).toFixed(0);
            const afterKB = (compressed.size / 1024).toFixed(0);
            console.log(`📎 ${input.dataset.docLabel} compressé : ${beforeKB} Ko → ${afterKB} Ko`);
        }

        // Révèle la ligne 2 (croix + nom du fichier) — jamais affiché deux
        // fois : c'est la SEULE zone où le nom apparaît, plus rien à côté
        // du bouton lui-même (dont le texte natif est rendu invisible).
        if (fileRow) {
            const filenameSpan = fileRow.querySelector('.doc-filename');
            if (filenameSpan) filenameSpan.textContent = input.files[0].name;
            fileRow.classList.remove('hidden');
        }

        updateDocSummary();
    }

    // Restructure chaque champ de pièce jointe en 2 lignes, conformément à
    // la maquette validée :
    //   Ligne 1 : uniquement le bouton "Choisir un fichier", aligné à droite
    //             (le nom natif du navigateur est rendu invisible ici pour
    //             ne JAMAIS l'afficher en double avec la ligne 2)
    //   Ligne 2 : n'apparaît qu'une fois un fichier choisi — grosse croix
    //             de suppression (colonne gauche, bien visible, PAS discrète)
    //             + nom du fichier (colonne droite)
    function initDocClearButtons() {
        document.querySelectorAll('.doc-upload').forEach((input) => {
            if (input.dataset.restructured) return; // déjà fait
            input.dataset.restructured = '1';

            // Masque COMPLÈTEMENT l'input natif (sr-only : invisible mais
            // toujours fonctionnel et accessible) plutôt que de tenter de
            // rendre son texte transparent — cette dernière méthode ne
            // fonctionne pas de façon fiable sur tous les sélecteurs de
            // fichiers mobiles, d'où le nom de fichier qui apparaissait
            // encore en double. Un bouton 100% personnalisé déclenche
            // l'input via JS : plus aucun rendu natif ne peut interférer.
            input.classList.add('sr-only');

            const triggerBtn = document.createElement('button');
            triggerBtn.type = 'button';
            triggerBtn.className = 'doc-trigger-btn block ml-auto bg-brandPurple hover:bg-purple-700 text-white text-[10px] font-bold px-3 py-1.5 rounded-lg cursor-pointer transition';
            triggerBtn.textContent = 'Choisir un fichier';
            triggerBtn.addEventListener('click', () => input.click());

            const fileRow = document.createElement('div');
            fileRow.className = 'doc-file-row hidden grid grid-cols-[auto_1fr] gap-2 items-center mt-1.5';

            const clearBtn = document.createElement('button');
            clearBtn.type = 'button';
            clearBtn.className = 'text-red-600 hover:text-red-800 text-2xl leading-none shrink-0 transition';
            clearBtn.innerHTML = '<i class="fa-solid fa-circle-xmark"></i>';
            clearBtn.setAttribute('aria-label', 'Retirer ce fichier');
            clearBtn.title = 'Retirer ce fichier';

            const filenameSpan = document.createElement('span');
            filenameSpan.className = 'doc-filename text-[10px] text-emerald-700 font-semibold truncate text-left';

            clearBtn.addEventListener('click', () => {
                input.value = '';
                fileRow.classList.add('hidden');
                filenameSpan.textContent = '';
                updateDocSummary();
            });

            fileRow.appendChild(clearBtn);
            fileRow.appendChild(filenameSpan);
            input.insertAdjacentElement('afterend', triggerBtn);
            triggerBtn.insertAdjacentElement('afterend', fileRow);
        });
    }

    function getSelectedDocuments() {
        const docs = [];
        document.querySelectorAll('.doc-upload').forEach((input) => {
            if (input.files && input.files.length > 0) {
                docs.push({
                    label: input.dataset.docLabel || input.dataset.docKey,
                    fileName: input.files[0].name,
                });
            }
        });
        return docs;
    }

    function updateDocSummary() {
        const summary = document.getElementById('doc-summary');
        if (!summary) return;
        const docs = getSelectedDocuments();
        if (docs.length === 0) {
            summary.classList.add('hidden');
            summary.textContent = '';
            return;
        }
        const totalMB = getTotalDocsSizeMB();
        const overLimit = totalMB > TOTAL_SIZE_WARNING_MB;
        summary.classList.remove('hidden');
        summary.className = `text-[10px] font-semibold ${overLimit ? 'text-amber-600' : 'text-emerald-700'}`;
        const sizeNote = overLimit
            ? ` — ~${totalMB.toFixed(1)} Mo (au-dessus du seuil recommandé de ${TOTAL_SIZE_WARNING_MB} Mo)`
            : ` — ~${(totalMB * 1024).toFixed(0)} Ko`;
        summary.textContent = `${docs.length} pièce(s) prête(s) : ${docs.map(d => d.label).join(', ')}${sizeNote}`;
    }

    // Référence de dossier lisible, générée une seule fois par ouverture de
    // la modale, réutilisée par le mémo WhatsApp ET le mémo archivé (PDF)
    // pour qu'ils portent exactement la même référence.
    let currentQuoteRef = null;

    function generateQuoteRef() {
        const insuranceSelect = document.getElementById('insurance_type');
        const branchCode = insuranceSelect && insuranceSelect.value ? insuranceSelect.value.toUpperCase() : 'DEVIS';
        const now = new Date();
        const pad = (n) => String(n).padStart(2, '0');
        const stamp = `${now.getFullYear()}${pad(now.getMonth() + 1)}${pad(now.getDate())}-${pad(now.getHours())}${pad(now.getMinutes())}`;
        return `${branchCode}-${stamp}`;
    }

    // -----------------------------------------------------------------
    // Envoi de la demande de cotation par WhatsApp à l'agence la plus proche
    // (déterminée via resolveNearestAgencyForQuote), avec repli automatique
    // sur le siège si la géolocalisation échoue ou est refusée.
    //
    // Présenté comme un mémo — clair, structuré, mais volontairement FUN
    // (émoticônes) plutôt qu'austère : c'est le premier tour d'une
    // conversation, pas un export de données. Le destinataire est nommé
    // explicitement (le numéro WhatsApp d'une agence peut être réaffecté),
    // et deux questions concrètes sont posées pour amorcer un vrai échange
    // plutôt qu'un message qui reste sans réponse.
    // -----------------------------------------------------------------
    // -----------------------------------------------------------------
    // TEXTES FIXES DU MÉMO (WhatsApp + email) — bilingue, ajouté le
    // 13/08/2026. Séparé du dictionnaire `translations` général (qui
    // gère l'affichage du site) car ce contenu est généré dynamiquement
    // dans des chaînes JS, pas posé directement dans le HTML.
    // Principe rappelé par Roger : le mémo suit la langue choisie par le
    // VISITEUR sur le site (currentLang) — le rédacteur doit comprendre
    // ce qu'il envoie, le destinataire est libre de répondre dans la
    // langue de son interlocuteur.
    // -----------------------------------------------------------------
    const MEMO_I18N = {
        fr: {
            locale: 'fr-FR',
            greetingMorning: 'Bonjour', greetingEvening: 'Bonsoir',
            defaultInsurance: 'assurance', defaultInsuranceCap: 'Assurance', defaultName: 'un client',
            waTitle: '📄✨ *MÉMO DE COTATION* — MutuellePro ✨📄',
            waRef: '🆔 Réf. :', waFor: '📍 Pour :',
            waIntro: (greeting, branche) => `${greeting} 👋 Voici ma demande de devis *${branche}* — tout est ci-dessous pour éviter les allers-retours 😊`,
            waClient: '👤 *CLIENT*',
            waDetails: '🔎 *DÉTAILS DU RISQUE*', waNotes: '📝 *NOTES*',
            waDocsTitle: '📎 *PIÈCES À JOINDRE DANS CETTE CONVERSATION*',
            waDocsHint: '_(à joindre juste après ce message, WhatsApp ne permet pas l\'envoi automatique)_',
            waAskQuality: '❓ Pouvez-vous me confirmer si les pièces jointes sont de bonne qualité et exploitables ?',
            waAskAck: '✅ Merci d\'accuser réception de ce message dès que possible 🙏',
            waArchived: (ref) => `📌 Ce mémo (Réf. ${ref}) est archivé de mon côté — je reste disponible pour la suite !`,
            memoTitlePrefix: 'MÉMO - DEMANDE COTATION -',
            introWithVin: (branche, vin) => `Bien vouloir recevoir ma demande de devis ${branche} pour mon véhicule immatriculé ${vin}.`,
            introSansVin: (branche) => `Bien vouloir recevoir ma demande de devis ${branche}.`,
            from: 'De :', to: 'À : MutuellePro Assurance –', date: 'Date :', ref: 'Référence :',
            objet: 'Objet : Demande de cotation', risque: 'Risque :',
            greeting: 'Bonjour,', detailsBelow: 'Pour les détails, tout est ci-dessous.',
            clientSection: '--- CLIENT ---', nom: 'Nom :', tel: 'Téléphone :', branche: 'Branche :',
            detailsSection: '--- DÉTAILS DU RISQUE ---', notesSection: '--- NOTES ---',
            docsSection: '--- PIÈCES JUSTIFICATIVES ---', noDocs: 'Aucune pièce sélectionnée.',
            autoGenerated: (ref) => `Document généré automatiquement par mutuellepro.cm — Réf. ${ref}`,
            archiveClient: 'Client', archiveDetails: 'Détails du risque', archiveNotes: 'Notes',
            archiveDocs: 'Pièces justificatives', archiveNoDocs: 'Aucune pièce jointe sélectionnée au moment de l\'archivage.',
            archiveFooter: 'Document généré automatiquement par mutuellepro.cm — à conserver comme trace de votre demande.',
            archiveFooter2: 'Ce mémo ne remplace pas une confirmation officielle de l\'agence.',
            archivePrint: '🖨️ Imprimer / Enregistrer en PDF',
            tableFrom: 'De', tableTo: 'À', tableDate: 'Date', tableRef: 'Référence', tableObjet: 'Objet', tableRisque: 'Risque',
            tableObjetValue: 'Demande de cotation',
        },
        en: {
            locale: 'en-US',
            greetingMorning: 'Good morning', greetingEvening: 'Good evening',
            defaultInsurance: 'insurance', defaultInsuranceCap: 'Insurance', defaultName: 'a client',
            waTitle: '📄✨ *QUOTE REQUEST MEMO* — MutuellePro ✨📄',
            waRef: '🆔 Ref.:', waFor: '📍 For:',
            waIntro: (greeting, branche) => `${greeting} 👋 Here is my *${branche}* quote request — everything is below to avoid back-and-forth 😊`,
            waClient: '👤 *CLIENT*',
            waDetails: '🔎 *RISK DETAILS*', waNotes: '📝 *NOTES*',
            waDocsTitle: '📎 *DOCUMENTS TO ATTACH IN THIS CONVERSATION*',
            waDocsHint: '_(to attach right after this message — WhatsApp does not support automatic sending)_',
            waAskQuality: '❓ Could you confirm the attached documents are clear and usable?',
            waAskAck: '✅ Please confirm receipt of this message as soon as possible 🙏',
            waArchived: (ref) => `📌 This memo (Ref. ${ref}) is archived on my end — I remain available for next steps!`,
            memoTitlePrefix: 'MEMO - QUOTE REQUEST -',
            introWithVin: (branche, vin) => `Please find my ${branche} quote request for my vehicle registered ${vin}.`,
            introSansVin: (branche) => `Please find my ${branche} quote request.`,
            from: 'From:', to: 'To: MutuellePro Assurance –', date: 'Date:', ref: 'Reference:',
            objet: 'Subject: Quote Request', risque: 'Risk:',
            greeting: 'Hello,', detailsBelow: 'Details are below.',
            clientSection: '--- CLIENT ---', nom: 'Name:', tel: 'Phone:', branche: 'Branch:',
            detailsSection: '--- RISK DETAILS ---', notesSection: '--- NOTES ---',
            docsSection: '--- SUPPORTING DOCUMENTS ---', noDocs: 'No documents selected.',
            autoGenerated: (ref) => `Document automatically generated by mutuellepro.cm — Ref. ${ref}`,
            archiveClient: 'Client', archiveDetails: 'Risk Details', archiveNotes: 'Notes',
            archiveDocs: 'Supporting Documents', archiveNoDocs: 'No documents were selected at the time of archiving.',
            archiveFooter: 'Document automatically generated by mutuellepro.cm — keep this as a record of your request.',
            archiveFooter2: 'This memo does not replace an official confirmation from the agency.',
            archivePrint: '🖨️ Print / Save as PDF',
            tableFrom: 'From', tableTo: 'To', tableDate: 'Date', tableRef: 'Reference', tableObjet: 'Subject', tableRisque: 'Risk',
            tableObjetValue: 'Quote Request',
        },
    };
    function memoT() {
        return MEMO_I18N[currentLang] || MEMO_I18N.fr;
    }

    function greetingByTime() {
        const h = new Date().getHours();
        const t = memoT();
        return h < 18 ? t.greetingMorning : t.greetingEvening;
    }

    function buildWhatsAppMessage(agence, ref) {
        const t = memoT();
        const get = (id) => {
            const el = document.getElementById(id);
            return el ? el.value.trim() : '';
        };

        const insuranceSelect = document.getElementById('insurance_type');
        const insuranceLabel = insuranceSelect && insuranceSelect.selectedIndex >= 0
            ? insuranceSelect.options[insuranceSelect.selectedIndex].text.replace(/^\d+\.\s*/, '')
            : t.defaultInsurance;

        const nom = get('modal_user_name') || t.defaultName;
        const email = get('modal_user_email') || '-';
        const telephone = `${get('modal_phone_prefix')}${get('modal_user_phone')}`;

        const now = new Date();
        const dateStr = now.toLocaleDateString(t.locale, { day: '2-digit', month: 'long', year: 'numeric' });
        const timeStr = now.toLocaleTimeString(t.locale, { hour: '2-digit', minute: '2-digit' });

        const SEP = '┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈';

        const details = [];
        const activeSection = document.querySelector('.risk-section:not(.hidden)');
        if (activeSection) {
            activeSection.querySelectorAll('input, select, textarea').forEach((field) => {
                if (field.dataset.skipMemo) return; // ex: alias/état bruts — remplacés par le libellé Genre reconstruit
                if (field.type === 'hidden' && !field.dataset.showInMemo) return; // ex: auto_categorie — code technique, pas destiné à l'affichage
                if (!field.value || !field.value.trim()) return;
                const label = field.dataset.label
                    || field.closest('div')?.querySelector('label')?.textContent?.trim()
                    || (field.name || field.id);
                const labelText = label;
                details.push(`• ${labelText} : ${field.value.trim()}`);
            });
        }

        const notes = get('modal_message');
        const docs = getSelectedDocuments();

        const lines = [
            t.waTitle,
            SEP,
            `${t.waRef} ${ref}`,
            `🕐 ${dateStr} à ${timeStr}`,
            `${t.waFor} *${agence.nom}*, ${agence.adr}`,
            SEP,
            '',
            t.waIntro(greetingByTime(), insuranceLabel),
            '',
            t.waClient,
            `${nom}`,
            `📞 ${telephone}`,
            `📧 ${email}`,
            `🗂️ Situation du compte : ${situationCompteLigne(email)}`,
        ];

        if (details.length) {
            lines.push('', t.waDetails, ...details);
        }

        if (notes) {
            lines.push('', t.waNotes, notes);
        }

        if (docs.length) {
            lines.push(
                '',
                t.waDocsTitle,
                ...docs.map((d) => `🗂️ ${d.label} — ${d.fileName}`),
                t.waDocsHint
            );
        }

        lines.push(
            '',
            SEP,
            t.waAskQuality,
            t.waAskAck,
            '',
            t.waArchived(ref)
        );

        return lines.join('\n');
    }

    function sendQuoteToWhatsApp() {
        if (!currentQuoteRef) currentQuoteRef = generateQuoteRef();
        const agence = agencesData[resolvedAgencyKey] || agencesData['yaounde_siege'];
        const phoneDigits = agence.tel.replace(/\D/g, ''); // format wa.me : chiffres uniquement, indicatif inclus
        const message = buildWhatsAppMessage(agence, currentQuoteRef);
        const url = `https://wa.me/${phoneDigits}?text=${encodeURIComponent(message)}`;
        const opened = window.open(url, '_blank');
        // Si le navigateur bloque le popup, window.open renvoie null — on le
        // signale explicitement plutôt que de laisser l'utilisateur croire,
        // à tort, que le message WhatsApp est parti (l'email, lui, reste
        // indépendant et part quand même).
        return { agenceNom: agence.nom, ref: currentQuoteRef, docs: getSelectedDocuments(), whatsappBlocked: !opened };
    }

    // -----------------------------------------------------------------
    // Construction du contenu du mémo — SOURCE UNIQUE, utilisée à la fois
    // par l'archivage (PDF) et par la soumission du formulaire (email).
    // Garantit que les deux chemins produisent toujours un contenu
    // strictement identique, et que le champ email n'est jamais vide,
    // même si l'utilisateur n'a jamais cliqué sur "Archiver".
    // -----------------------------------------------------------------
    function buildMemoData() {
        const t = memoT();
        if (!currentQuoteRef) currentQuoteRef = generateQuoteRef();
        const agence = agencesData[resolvedAgencyKey] || agencesData['yaounde_siege'];
        const get = (id) => {
            const el = document.getElementById(id);
            return el ? el.value.trim() : '';
        };

        const insuranceSelect = document.getElementById('insurance_type');
        const insuranceLabel = insuranceSelect && insuranceSelect.selectedIndex >= 0
            ? insuranceSelect.options[insuranceSelect.selectedIndex].text.replace(/^\d+\.\s*/, '')
            : t.defaultInsuranceCap;
        const insuranceValue = insuranceSelect ? insuranceSelect.value : '';

        const nom = get('modal_user_name') || '-';
        const email = get('modal_user_email') || '-';
        const telephone = `${get('modal_phone_prefix')}${get('modal_user_phone')}`;
        const notes = get('modal_message');
        const docs = getSelectedDocuments();
        const now = new Date();
        const dateStr = now.toLocaleDateString(t.locale, { day: '2-digit', month: 'long', year: 'numeric' });
        const timeStr = now.toLocaleTimeString(t.locale, { hour: '2-digit', minute: '2-digit' });
        const dateStrSlash = now.toLocaleDateString(t.locale);

        const vin = get('auto_vin');
        const titleSuffix = insuranceValue === 'Auto' && vin ? ` - ${vin}` : '';
        const memoTitle = `${t.memoTitlePrefix} ${insuranceLabel.toUpperCase()}${titleSuffix}`;
        const introLine = insuranceValue === 'Auto' && vin
            ? t.introWithVin(insuranceLabel, vin)
            : t.introSansVin(insuranceLabel);

        const detailsData = [];
        const activeSection = document.querySelector('.risk-section:not(.hidden)');
        if (activeSection) {
            activeSection.querySelectorAll('input, select, textarea').forEach((field) => {
                if (field.dataset.skipMemo) return; // ex: alias/état bruts — remplacés par le libellé Genre reconstruit
                if (field.type === 'hidden' && !field.dataset.showInMemo) return; // ex: auto_categorie — code technique, pas destiné à l'affichage
                // Un champ marqué "à fusionner avec un autre" (ex: l'unité de
                // puissance) ne génère jamais sa propre ligne — sa valeur est
                // rattachée au champ principal ci-dessous.
                if (field.dataset.combineWith) return;
                if (!field.value || !field.value.trim()) return;

                let value = field.value.trim();
                if (field.dataset.combineSuffix) {
                    const suffixField = document.getElementById(field.dataset.combineSuffix);
                    if (suffixField && suffixField.value.trim()) value += ` ${suffixField.value.trim()}`;
                }

                // Priorité à un data-label explicite (fiable, indépendant de la
                // structure HTML) ; à défaut, on retombe sur le <label> du div
                // englobant le plus proche, comme avant.
                const label = field.dataset.label
                    || field.closest('div')?.querySelector('label')?.textContent?.trim()
                    || field.name || field.id;
                detailsData.push({ label, value });
            });
        }

        return {
            ref: currentQuoteRef, agence, nom, email, telephone, notes, docs, now,
            dateStr, timeStr, dateStrSlash, insuranceLabel, insuranceValue, vin,
            memoTitle, introLine, detailsData,
        };
    }

    function buildMemoPlainText(data) {
        const t = memoT();
        const lines = [
            data.memoTitle, '',
            `${t.from} ${data.nom}`,
            `${t.to} ${data.agence.nom}`,
            `${t.date} ${data.dateStrSlash}`,
            `${t.ref} ${data.ref}`,
            t.objet,
            `${t.risque} « ${data.insuranceLabel} »`, '',
            t.greeting,
            data.introLine,
            t.detailsBelow, '',
            t.clientSection,
            `${t.nom} ${data.nom}`,
            `Email : ${data.email}`,
            `${t.tel} ${data.telephone}`,
            `${t.branche} ${data.insuranceLabel}`,
            `Situation du compte : ${situationCompteLigne(data.email)}`,
        ];
        if (data.detailsData.length) {
            lines.push('', t.detailsSection, ...data.detailsData.map((d) => `${d.label} : ${d.value}`));
        }
        if (data.notes) {
            lines.push('', t.notesSection, data.notes);
        }
        lines.push(
            '', t.docsSection,
            data.docs.length ? data.docs.map((d) => `📎 ${d.label} — ${d.fileName}`).join('<br>') : t.noDocs,
            '', t.autoGenerated(data.ref)
        );
        return lines.join('<br>');
    }

    // Remplit le champ caché "memo_complet" à partir de la source unique.
    // Appelée systématiquement avant TOUT envoi d'email (archivage ET
    // soumission normale) — plus jamais d'email vide.
    function populateMemoCompletField(data) {
        const memoField = document.getElementById('memo_complet_field');
        if (memoField) memoField.value = buildMemoPlainText(data);
    }

    // -----------------------------------------------------------------
    // Archivage du mémo : génère une version imprimable (donc "Enregistrer
    // en PDF" via la boîte d'impression du navigateur, sans dépendance
    // externe). C'est la brique de dématérialisation : chaque demande
    // devient un document réel que le client peut conserver, indépendant
    // des bribes de messages échangées sur WhatsApp.
    // -----------------------------------------------------------------
    async function archiverMemo() {
        const data = buildMemoData();
        const t = memoT();
        const { ref: currentQuoteRefLocal, agence, nom, telephone, notes, docs, dateStrSlash, insuranceLabel, memoTitle, introLine, detailsData } = data;

        const details = detailsData.map((d) => `<tr><td>${d.label}</td><td>${d.value}</td></tr>`);

        const docsHtml = docs.length
            ? `<ul>${docs.map(d => `<li>📎 ${d.label} — <em>${d.fileName}</em></li>`).join('')}</ul>`
            : `<p><em>${t.archiveNoDocs}</em></p>`;

        const html = `<!DOCTYPE html>
<html lang="${currentLang}">
<head>
<meta charset="UTF-8">
<title>${memoTitle}</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
<style>
    body { font-family: 'Roboto', 'Segoe UI', Arial, sans-serif; color: #1a1a2e; max-width: 700px; margin: 30px auto; padding: 0 20px; font-size: 13.5px; line-height: 1.5; }
    h1 { font-size: 17px; letter-spacing: 0.5px; border-bottom: 3px solid #5B21B6; padding-bottom: 8px; text-transform: uppercase; }
    .header-table { border-collapse: collapse; margin: 12px 0 16px; font-size: 12.5px; }
    .header-table td { padding: 2px 0; vertical-align: top; }
    .header-table td:first-child { font-weight: bold; width: 105px; color: #333; }
    .header-table td:nth-child(2) { width: 14px; }
    .intro { font-size: 13px; line-height: 1.5; margin-bottom: 14px; }
    .ref { color: #5B21B6; font-weight: bold; }
    table.data { width: 100%; border-collapse: collapse; margin: 6px 0; font-size: 12.5px; }
    table.data td { padding: 4px 8px; border-bottom: 1px solid #eee; }
    table.data td:first-child { font-weight: bold; width: 40%; color: #333; }
    h2 { font-size: 13.5px; color: #5B21B6; margin: 14px 0 5px; }
    ul { font-size: 12.5px; margin: 5px 0; line-height: 1.5; }
    .footer { margin-top: 16px; font-size: 10px; color: #888; border-top: 1px solid #eee; padding-top: 8px; }
    /* @page pilote réellement les marges d'impression (contrairement à la
       marge du <body>, qui ne contrôle que le contenu, pas la page physique).
       Gauche/droite à 2,5 cm comme d'habitude ; marges haut/bas resserrées
       pour garantir une seule page même avec un dossier bien rempli. */
    @page {
        size: A4;
        margin: 12mm 25mm 15mm 25mm;
    }
    @media print {
        body { margin: 0; max-width: none; padding: 0; }
        .no-print { display: none !important; }
    }
</style>
</head>
<body>
    <h1>${memoTitle}</h1>

    <table class="header-table">
        <tr><td>${t.tableFrom}</td><td>:</td><td>${nom}</td></tr>
        <tr><td>${t.tableTo}</td><td>:</td><td>MutuellePro Assurance – ${agence.nom}</td></tr>
        <tr><td>${t.tableDate}</td><td>:</td><td>${dateStrSlash}</td></tr>
        <tr><td>${t.tableRef}</td><td>:</td><td class="ref">${currentQuoteRef}</td></tr>
        <tr><td>${t.tableObjet}</td><td>:</td><td>${t.tableObjetValue}</td></tr>
        <tr><td>${t.tableRisque}</td><td>:</td><td>« ${insuranceLabel} »</td></tr>
    </table>

    <div class="intro">
        ${t.greeting}<br><br>
        ${introLine}<br>
        ${t.detailsBelow}
    </div>

    <h2>👤 ${t.archiveClient}</h2>
    <table class="data">
        <tr><td>${t.nom}</td><td>${nom}</td></tr>
        <tr><td>${t.tel}</td><td>${telephone}</td></tr>
        <tr><td>${t.branche}</td><td>${insuranceLabel}</td></tr>
    </table>

    ${details.length ? `<h2>🔎 ${t.archiveDetails}</h2><table class="data">${details.join('')}</table>` : ''}

    ${notes ? `<h2>📝 ${t.archiveNotes}</h2><p>${notes.replace(/\n/g, '<br>')}</p>` : ''}

    <h2>📎 ${t.archiveDocs}</h2>
    ${docsHtml}

    <div class="footer">
        ${t.archiveFooter}<br>
        ${t.archiveFooter2}
    </div>

    <div class="no-print" style="margin-top:24px; text-align:center;">
        <button onclick="window.print()" style="background:#5B21B6; color:#fff; border:none; padding:10px 20px; border-radius:8px; font-weight:bold; font-size:13px; cursor:pointer;">
            ${t.archivePrint}
        </button>
    </div>
</body>
</html>`;

        const blob = new Blob([html], { type: 'text/html' });
        const blobUrl = URL.createObjectURL(blob);
        const printWindow = window.open(blobUrl, '_blank');

        if (!printWindow) {
            showToast("Le navigateur a bloqué l'ouverture de la fenêtre d'archivage. Autorisez les pop-ups pour ce site puis réessayez.", 'warning');
            URL.revokeObjectURL(blobUrl);
            return;
        }

        // Déclenchement automatique de l'impression une fois la page chargée
        // (plus fiable que document.write, notamment sur mobile). Si
        // l'impression automatique ne se déclenche pas sur votre appareil,
        // le bouton visible dans la page reste disponible en secours.
        printWindow.addEventListener('load', () => {
            try { printWindow.print(); } catch (e) { console.warn('Impression automatique indisponible :', e); }
        });

        // Libère la mémoire du blob après un délai large (le temps que
        // l'utilisateur imprime ou enregistre en PDF).
        setTimeout(() => URL.revokeObjectURL(blobUrl), 60000);

        // -------------------------------------------------------------
        // Envoi automatique du mémo complet par email, pièces jointes
        // incluses, à chaque clic sur "Archiver". Le contenu complet est
        // placé dans le champ caché "memo_complet" via la fonction
        // partagée (source unique, voir buildMemoData/buildMemoPlainText
        // plus haut), pour garantir qu'il est identique à la version PDF.
        // Envoyé via NOTRE serveur (Nodemailer), plus EmailJS — plus de
        // plafond de 50 Ko, les pièces jointes partent réellement.
        // -------------------------------------------------------------
        populateMemoCompletField(data);
        const devisForm = document.getElementById('devisFormNormalized');

        if (devisForm && await warnIfAttachmentsTooLarge()) {
            sendQuoteEmail(devisForm, null)
                .then(() => console.log('📧 Mémo envoyé par email (Réf. ' + currentQuoteRef + ')'))
                .catch((err) => {
                    console.error('Échec envoi email du mémo archivé :', err);
                    showToast(explainEmailError(err), 'error');
                });
        }
    }

    // -----------------------------------------------------------------
    // Conversion HP (vPIC, puissance US "SAE net") → CV fiscaux (Cameroun/CEMAC)
    //
    // Faute de barème officiel publié pour la fiscalité automobile CEMAC,
    // on utilise la formule française simplifiée qui ne nécessite pas la
    // donnée CO2 (celle appliquée par défaut aux véhicules électriques,
    // utilisée ici comme meilleure approximation disponible) :
    //   PF = 1,8 × (kW/100)² + 3,87 × (kW/100) + 1,34
    // Le résultat est marqué "(estim.)" dans le formulaire et reste
    // éditable manuellement par l'agent. La table ci-dessous permet
    // d'enregistrer des corrections constatées sur le terrain, pour
    // affiner la correspondance marque par marque au fil du temps.
    // -----------------------------------------------------------------
    const CV_CALIBRATION_OVERRIDES = {
        // 'HONDA|Accord|2003': 11,  // exemple : à renseigner si écart constaté avec le barème réel
    };

    function hpToFiscalCV(hpValue, make, model, year) {
        const hp = parseFloat(hpValue);
        if (isNaN(hp) || hp <= 0) return null;

        const overrideKey = `${(make || '').toUpperCase()}|${model || ''}|${year || ''}`;
        if (CV_CALIBRATION_OVERRIDES[overrideKey]) {
            return CV_CALIBRATION_OVERRIDES[overrideKey];
        }

        const kW = hp * 0.7457; // 1 hp (SAE) ≈ 0,7457 kW
        const pf = 1.8 * Math.pow(kW / 100, 2) + 3.87 * (kW / 100) + 1.34;
        return Math.round(pf);
    }

    // -----------------------------------------------------------------
    // RÉMANENCE DU FORMULAIRE (localStorage)
    // Les utilisateurs sont souvent indécis, y compris après plusieurs
    // soumissions : le contenu du formulaire est donc sauvegardé en
    // continu et restauré automatiquement, sans être effacé après envoi.
    // -----------------------------------------------------------------
    const DRAFT_STORAGE_KEY = 'mutuellepro_devis_draft';

    function fieldKey(el) {
        return el.id || el.name;
    }

    function saveFormDraft() {
        const form = document.getElementById('devisFormNormalized');
        if (!form) return;
        const draft = {};
        Array.from(form.elements).forEach((el) => {
            const key = fieldKey(el);
            if (!key || el.type === 'submit' || el.type === 'button' || el.type === 'file') return;
            draft[key] = el.value;
        });
        try {
            localStorage.setItem(DRAFT_STORAGE_KEY, JSON.stringify(draft));
        } catch (e) {
            console.warn('Sauvegarde du brouillon impossible :', e);
        }
    }

    function restoreFormDraft() {
        const form = document.getElementById('devisFormNormalized');
        if (!form) return;
        let draft;
        try {
            draft = JSON.parse(localStorage.getItem(DRAFT_STORAGE_KEY) || '{}');
        } catch (e) {
            return;
        }
        Array.from(form.elements).forEach((el) => {
            const key = fieldKey(el);
            if (!key || !(key in draft) || el.type === 'submit' || el.type === 'button') return;
            if (el.hasAttribute('readonly')) return; // ex : préfixe téléphone fixe
            // Un <input type="file"> ne peut JAMAIS être rempli par script, pour
            // des raisons de sécurité (seule la chaîne vide est autorisée) — un
            // navigateur lève une erreur bloquante si on l'ignore. Cette seule
            // ligne manquante interrompait silencieusement TOUTE l'initialisation
            // qui suit ce point au chargement de la page (animations, boutons de
            // pièces jointes, etc.) — d'où plusieurs bugs en apparence sans lien.
            if (el.type === 'file') return;
            el.value = draft[key];
        });

        // Réaffiche la bonne section de risque si une branche était déjà sélectionnée
        const insuranceSelect = document.getElementById('insurance_type');
        if (insuranceSelect && insuranceSelect.value) {
            insuranceSelect.dispatchEvent(new Event('change'));
        }
    }

    // LISTENERS DOM ATTACHMENTS
    document.addEventListener('DOMContentLoaded', async () => {
        // Chargement des agences DEPUIS POSTGRESQL, en tout premier -- tout le
        // reste de l'initialisation (carte, résolution de la plus proche,
        // etc.) dépend de cette donnée étant disponible.
        await loadAgencesData();
        try {
            loadListesReference();
        } catch (e) {
            console.error('Erreur lors du chargement des listes de référence (ignorée) :', e);
        }
        try {
            initTomSelect('sujet_categorie', { placeholder: '-- Choisissez un sujet --', create: false });
        } catch (e) {
            console.error('Erreur lors de l\'initialisation de Tom Select sur Sujet (ignorée) :', e);
        }
        // initPartnersBar() et initPartnersAnimation() ne sont plus appelées
        // ici (15/08/2026) : elles dépendent désormais des données réelles
        // chargées depuis la base (site.partenaires_assurance), et sont donc
        // déclenchées depuis loadListesReference() une fois ces données
        // effectivement disponibles — les appeler ici, avant, les aurait
        // fait tourner sur un tableau vide.
        try {
            ajusterHauteurSectionPartenaires();
            // Réajuste sur redimensionnement/rotation d'écran (anti-rebond
            // 200ms -- évite de recalculer à chaque pixel pendant un
            // redimensionnement en cours, reste léger en CPU).
            let partnersResizeTimer = null;
            window.addEventListener('resize', () => {
                clearTimeout(partnersResizeTimer);
                partnersResizeTimer = setTimeout(ajusterHauteurSectionPartenaires, 200);
            });
        } catch (e) {
            console.error('Erreur lors de l\'ajustement de hauteur de la section partenaires (ignorée) :', e);
        }

        // Bloc SÉPARÉ du précédent (20/08/2026) : une panne éventuelle sur
        // Partenaires ne doit jamais empêcher Assistance de s'ajuster, et
        // inversement -- même principe déjà appliqué ailleurs sur ce site.
        try {
            ajusterHauteurSectionAssistance();
            let assistanceResizeTimer = null;
            window.addEventListener('resize', () => {
                clearTimeout(assistanceResizeTimer);
                assistanceResizeTimer = setTimeout(ajusterHauteurSectionAssistance, 200);
            });
        } catch (e) {
            console.error('Erreur lors de l\'ajustement de hauteur de la section assistance (ignorée) :', e);
        }

        // initMap() dépend de la librairie Leaflet chargée depuis un CDN externe.
        // Si le CDN est inaccessible (pas d'internet, bloqué...), on ne doit pas
        // laisser une erreur ici interrompre tout le reste de l'initialisation
        // (restauration du formulaire, gestion des soumissions, etc.).
        try {
            initMap();
        } catch (err) {
            console.warn('Carte indisponible (Leaflet non chargé) :', err);
        }

        // Résolution de l'agence la plus proche demandée dès l'ouverture du
        // site (autorisation demandée immédiatement), pour que le résultat
        // soit déjà disponible bien avant qu'un devis ne soit soumis — évite
        // la course de vitesse entre géolocalisation et soumission.
        resolveNearestAgencyForQuote();

        // Si l'utilisateur clique directement sur une case du mini-formulaire
        // (plutôt que via un bouton "Multirisque Pro" etc.), on oublie la
        // branche mémorisée détournée — sinon elle resterait périmée.
        // .checked = true (programmatique, via selectDevisRisk) ne déclenche
        // pas cet événement 'change', seul un vrai clic utilisateur le fait.
        document.querySelectorAll('input[name="risque"]').forEach((radio) => {
            radio.addEventListener('change', () => { heroFormIntendedBranch = null; });
        });

        // Restauration du brouillon sauvegardé + sauvegarde continue à chaque saisie
        // Encapsulé : une erreur ici ne doit plus JAMAIS pouvoir interrompre
        // silencieusement le reste de l'initialisation de la page (c'est
        // exactement ce qui s'est produit avec le bug des champs fichier).
        try {
            restoreFormDraft();
        } catch (e) {
            console.error('Erreur lors de la restauration du brouillon (ignorée, le reste de la page continue) :', e);
        }
        try {
            setupWhatsAppEscort();
        } catch (e) {
            console.error('Erreur lors de l\'initialisation de l\'escorte WhatsApp (ignorée) :', e);
        }
        try {
            setupHeroSlideshow();
        } catch (e) {
            console.error('Erreur lors de l\'initialisation du diaporama du hero (ignorée) :', e);
        }
        try {
            initCookieConsent();
        } catch (e) {
            console.error('Erreur lors de l\'initialisation du consentement cookies (ignorée) :', e);
        }
        initDocClearButtons();
        const devisFormForDraft = document.getElementById('devisFormNormalized');
        if (devisFormForDraft) {
            devisFormForDraft.addEventListener('input', saveFormDraft);
            devisFormForDraft.addEventListener('change', saveFormDraft);
        }

        // Date de naissance : ouvrir le calendrier près d'un âge adulte
        // plausible (J-15 ans) plutôt qu'à la date du jour — évite à
        // l'utilisateur de faire défiler des dizaines d'années en arrière.
        // N'écrase jamais une valeur déjà restaurée depuis le brouillon.
        const dobField = document.getElementById('modal_user_dob');
        if (dobField) {
            const today = new Date();
            dobField.max = today.toISOString().split('T')[0];
            if (!dobField.value) {
                const fifteenYearsAgo = new Date(today.getFullYear() - 15, today.getMonth(), today.getDate());
                dobField.value = fifteenYearsAgo.toISOString().split('T')[0];
            }
        }

        const insuranceSelect = document.getElementById('insurance_type');
        if (insuranceSelect) {
            insuranceSelect.addEventListener('change', function() {
                document.querySelectorAll('.risk-section').forEach(sec => sec.classList.add('hidden'));
                const target = document.getElementById(`fields-${this.value.toLowerCase()}`);
                if (target) target.classList.remove('hidden');

                const docAutoSection = document.getElementById('doc-auto-section');
                if (docAutoSection) {
                    docAutoSection.classList.toggle('hidden', this.value !== 'Auto');
                }

                // Changement de branche = les infos "Détails du Risque" ne sont
                // plus à jour tant qu'on n'y est pas repassé : on reverrouille
                // l'accès direct à l'étape 3.
                if (step3UnlockedForBranch !== this.value) {
                    step3UnlockedForBranch = null;
                    refreshStep3BadgeState();
                    // Si on était sur l'étape 3 au moment du changement (cas
                    // limite), on ramène vers l'étape 2 pour re-confirmer les
                    // détails de la nouvelle branche.
                    const step3El = document.getElementById('wizardStep3');
                    if (step3El && !step3El.classList.contains('hidden')) {
                        goToStep2();
                    }
                }
            });
        }

        // Suivi des pièces justificatives sélectionnées, avec compression
        // automatique des images (ne persistent PAS en rechargement de
        // page : les fichiers ne peuvent pas être stockés en localStorage —
        // l'utilisateur devra les resélectionner si la page est rechargée
        // avant soumission).
        document.querySelectorAll('.doc-upload').forEach((input) => {
            input.addEventListener('change', handleDocUploadChange);
        });

        const sections = document.querySelectorAll('section[id]');
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    setActiveMenuLink(entry.target.id);
                    const idx = scrollPageOrder.indexOf(entry.target.id);
                    if (idx !== -1) {
                        currentSectionIndex = idx;
                        updateScrollArrows();
                    }
                }
            });
        }, { threshold: 0.4 });
        sections.forEach(section => observer.observe(section));

        // Flèches de défilement : réagissent au mouvement/toucher/scroll
        ['mousemove', 'touchstart', 'scroll'].forEach((evt) => {
            window.addEventListener(evt, resetArrowsInactivityTimer, { passive: true });
        });
        resetArrowsInactivityTimer(); // état initial : flèches visibles quelques secondes

        // Micro-animation : apparition en fondu des cartes au défilement
        // (réutilisable pour d'autres sections à l'avenir en ajoutant
        // simplement la classe "fade-in-section" + les classes de transition).
        const fadeInObserver = new IntersectionObserver((entries) => {
            entries.forEach((entry) => {
                if (entry.isIntersecting) {
                    // Retire les deux valeurs possibles (translate-y-4 ET
                    // translate-y-6 sont toutes deux utilisées selon les
                    // sections) -- retirer une classe absente ne fait rien,
                    // donc aucun risque à toujours tenter les deux.
                    entry.target.classList.remove('opacity-0', 'translate-y-4', 'translate-y-6');
                    entry.target.classList.add('opacity-100', 'translate-y-0');
                    fadeInObserver.unobserve(entry.target);
                }
            });
        }, { threshold: 0.2 });
        document.querySelectorAll('.fade-in-section').forEach((el) => fadeInObserver.observe(el));

        const contactForm = document.getElementById('contact-form');
        if (contactForm) {
            contactForm.addEventListener('submit', function(event) {
                event.preventDefault();
                const submitBtn = document.getElementById('submit-btn');
                const originalBtnContent = submitBtn.innerHTML;
                submitBtn.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i> Envoi en cours...';
                submitBtn.disabled = true;

                const memoField = document.getElementById('contact_memo_complet');
                if (memoField) memoField.value = buildContactMemoText(this);

                // Ticket backend (Helpdesk Volet 2, site.tickets, type
                // "info") — en parallèle, jamais bloquant.
                const contactEmail = this.elements['user_email'] ? this.elements['user_email'].value.trim() : '';
                const contactPrefixEl = this.querySelector('.devis-prefix-select');
                const contactPhone = this.elements['user_phone'] ? this.elements['user_phone'].value.trim() : '';
                const contactTelephone = `${contactPrefixEl ? contactPrefixEl.value : ''}${contactPhone}`;
                const contactSujetSelect = this.elements['sujet_categorie'];
                const contactSujetLabel = contactSujetSelect && contactSujetSelect.selectedIndex > 0
                    ? contactSujetSelect.options[contactSujetSelect.selectedIndex].textContent
                    : undefined;
                creerTicketBackend('info', contactEmail, contactTelephone, {
                    sujet: contactSujetLabel,
                    objet: this.elements['subject'] ? this.elements['subject'].value.trim() : undefined,
                    message: this.elements['message'] ? this.elements['message'].value.trim() : undefined,
                });

                // Routage par sujet — voir CONTACT_ROUTING plus haut. Repli sur
                // contact@ si, par un cas limite, aucun sujet n'était sélectionné
                // (le champ est "required", donc ça ne devrait jamais arriver).
                const sujetValue = this.elements['sujet_categorie'] ? this.elements['sujet_categorie'].value : '';
                const routing = CONTACT_ROUTING[sujetValue] || CONTACT_ROUTING_FALLBACK;

                sendQuoteEmail(this, routing.cc, routing.to)
                    .then(() => {
                        showToast('Votre message a bien été transmis ! Un conseiller vous recontactera sous 24h.', 'success');
                        contactForm.reset();
                    }, (error) => {
                        showToast(explainEmailError(error), 'error');
                        console.error('Erreur envoi email (Nodemailer) :', error);
                    })
                    .finally(() => {
                        submitBtn.innerHTML = originalBtnContent;
                        submitBtn.disabled = false;
                    });
            });
        }

        const devisFormNormalized = document.getElementById('devisFormNormalized');
        if (devisFormNormalized) {
            devisFormNormalized.addEventListener('submit', async function(event) {
                event.preventDefault();
                const submitBtn = document.getElementById('normalizedSubmitBtn');
                const originalBtnContent = submitBtn.innerHTML;
                submitBtn.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i> Envoi en cours...';
                submitBtn.disabled = true;

                // Déclenché en tout premier, de façon synchrone, pour rester dans le
                // geste utilisateur (clic) — sinon certains navigateurs bloquent
                // l'ouverture de l'onglet WhatsApp si elle intervient après un délai
                // asynchrone (ex. après la réponse du serveur emailjs).
                const { agenceNom, ref, docs, whatsappBlocked } = sendQuoteToWhatsApp();

                // Remplit le champ email AVANT l'envoi — corrige le bug de
                // l'email vide : auparavant, ce champ n'était rempli que
                // lors d'un clic sur "Archiver", jamais sur une soumission
                // directe. Utilise la même source unique que l'archivage,
                // donc les deux contenus sont désormais toujours identiques.
                const memoData = buildMemoData();
                populateMemoCompletField(memoData);

                // Ticket backend (Helpdesk Volet 2, site.tickets) — voir
                // commentaire de creerTicketBackend : en parallèle, jamais
                // bloquant. Le champ email du formulaire alimente à la fois
                // l'email Nodemailer existant et ce nouveau ticket.
                const userEmail = document.getElementById('modal_user_email')
                    ? document.getElementById('modal_user_email').value.trim()
                    : '';
                creerTicketBackend('devis', userEmail, memoData.telephone, {
                    branche: memoData.insuranceLabel,
                    vin: memoData.vin || undefined,
                    details: memoData.detailsData,
                    notes: memoData.notes || undefined,
                    ref_memo: memoData.ref,
                });

                if (!(await warnIfAttachmentsTooLarge())) {
                    submitBtn.innerHTML = originalBtnContent;
                    submitBtn.disabled = false;
                    return;
                }

                // Copie à l'agence UNIQUEMENT si WhatsApp a échoué (règle
                // anti-doublon déjà câblée côté serveur) — utilise désormais
                // la vraie adresse de l'agence actuellement sélectionnée,
                // les 11 boîtes créées le 04/08/2026.
                const selectedAgencyKey = document.getElementById('select-agences')
                    ? document.getElementById('select-agences').value
                    : null;
                const selectedAgency = selectedAgencyKey ? agencesData[selectedAgencyKey] : null;
                const agencyEmailForCC = whatsappBlocked && selectedAgency ? selectedAgency.email : null;

                sendQuoteEmail(this, agencyEmailForCC)
                    .then(() => {
                        const docsReminder = docs.length
                            ? `\n\nN'oubliez pas de joindre manuellement dans WhatsApp : ${docs.map(d => d.label).join(', ')} (WhatsApp ne permet pas leur envoi automatique).`
                            : '';
                        const whatsappNote = whatsappBlocked
                            ? `\n\nLe navigateur a bloqué l'ouverture de WhatsApp. Votre demande a bien été transmise par email — pour l'envoyer aussi sur WhatsApp, réessayez ou autorisez les pop-ups pour ce site.`
                            : `— mémo envoyé sur WhatsApp à l'agence : ${agenceNom} !`;
                        showToast(`Votre demande de cotation (Réf. ${ref}) a été transmise avec succès ${whatsappNote} Un conseiller prend en charge votre dossier.${docsReminder}\n\nVos informations restent enregistrées si vous souhaitez les corriger ou soumettre une nouvelle demande.`, 'success', 8000);
                        closeDevisModal();
                        // Le formulaire n'est volontairement PAS réinitialisé : les
                        // utilisateurs indécis doivent pouvoir corriger et resoumettre.
                    }, (error) => {
                        showToast(explainEmailError(error), 'error');
                        console.error('Erreur envoi email (Nodemailer) :', error);
                    })
                    .finally(() => {
                        submitBtn.innerHTML = originalBtnContent;
                        submitBtn.disabled = false;
                    });
            });
        }
    });
    

    <!-- BANDEAU DE CONSENTEMENT COOKIES -->
    <div id="cookie-banner" class="hidden fixed bottom-0 inset-x-0 z-[100] bg-navy text-white p-4 sm:p-5 shadow-2xl border-t border-white/10">
        <div class="max-w-5xl mx-auto flex flex-col sm:flex-row items-center gap-4">
            <p class="text-xs sm:text-sm text-gray-200 flex-1">
                <span data-i18n="cookies.text">Nous utilisons des cookies pour mesurer l'audience de ce site (Google Analytics). Vos données ne sont utilisées qu'à cette fin. Consultez notre</span>
                <a href="politique-confidentialite.html" class="underline text-purple-300 hover:text-purple-200" data-i18n="cookies.link">politique de confidentialité</a>.
            </p>
            <div class="flex gap-3 shrink-0">
                <button onclick="handleCookieChoice(false)" class="text-xs sm:text-sm px-4 py-2 rounded-lg border border-white/20 hover:bg-white/10 transition" data-i18n="cookies.refuse">Refuser</button>
                <button onclick="handleCookieChoice(true)" class="text-xs sm:text-sm px-5 py-2 rounded-lg bg-brandPurple hover:bg-purple-700 font-bold transition" data-i18n="cookies.accept">Accepter</button>
            </div>
        </div>
    </div>

    <!-- Conteneur des notifications toast — empile plusieurs messages,
         remplace les alert() natifs disgracieux. -->
    <div id="toast-container" class="fixed top-4 right-4 z-[200] flex flex-col gap-2 max-w-xs sm:max-w-sm w-[calc(100%-2rem)] sm:w-auto"></div>

    <!-- Boîte de dialogue de confirmation personnalisée — remplace les
         confirm() natifs, avec le même habillage que le reste du site. -->
    <div id="confirm-dialog-overlay" class="hidden fixed inset-0 bg-black/50 z-[210] flex items-center justify-center p-4">
        <div class="bg-white rounded-2xl shadow-2xl max-w-sm w-full p-5">
            <p id="confirm-dialog-message" class="text-sm text-gray-700 leading-relaxed whitespace-pre-line mb-5"></p>
            <div class="flex gap-3">
                <button type="button" id="confirm-dialog-cancel" class="flex-1 text-xs font-bold text-gray-500 border-2 border-gray-200 rounded-lg py-2.5 hover:bg-gray-50 transition">Annuler</button>
                <button type="button" id="confirm-dialog-ok" class="flex-1 text-xs font-bold text-white bg-navy rounded-lg py-2.5 hover:bg-purple-900 transition">Confirmer</button>
            </div>
        </div>
    </div>

    <!-- BULLE WHATSAPP FLOTTANTE -->
    <div id="whatsapp-bubble" class="fixed bottom-5 right-5 z-30">
        <span class="absolute inline-flex h-14 w-14 rounded-full bg-emerald-400 opacity-60 animate-ping"></span>
        <a href="https://wa.me/237697717334" target="_blank" rel="noopener"
           class="relative bg-emerald-500 hover:bg-emerald-600 text-white w-14 h-14 rounded-full shadow-2xl flex items-center justify-center text-2xl transition-transform hover:scale-110"
           aria-label="Discuter sur WhatsApp">
            <i class="fa-brands fa-whatsapp"></i>
        </a>
    </div>

    <!-- BOUTON D'INSTALLATION PWA (masqué par défaut, révélé si le navigateur le permet) -->
    <button id="pwa-install-btn" onclick="installPWA()" style="display:none;"
            class="fixed bottom-5 left-5 z-30 bg-navy hover:bg-purple-900 text-white text-xs font-bold px-4 py-3 rounded-full shadow-2xl flex items-center gap-2 transition-transform hover:scale-105">
        <i class="fa-solid fa-mobile-screen-button"></i> Installer l'application
    </button>

    <!-- FLÈCHES DE DÉFILEMENT ENTRE SECTIONS -->
    <div id="scroll-arrows" class="fixed right-3 sm:right-6 top-1/2 -translate-y-1/2 z-40 flex flex-col gap-6">
        <button id="scroll-arrow-up" onclick="scrollToAdjacentSection(-1)" aria-label="Section précédente"
                style="display:none;" class="text-navy text-2xl transition-opacity duration-700">
            <i class="fa-solid fa-chevron-up"></i>
        </button>
        <button id="scroll-arrow-down" onclick="scrollToAdjacentSection(1)" aria-label="Section suivante"
                style="display:none;" class="text-navy text-2xl transition-opacity duration-700">
            <i class="fa-solid fa-chevron-down"></i>
        </button>
    </div>
</body>
</html>
